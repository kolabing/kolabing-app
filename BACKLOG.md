# BACKLOG

> **Single source of truth for outstanding work.** CLAUDE.md requires this file to
> be read at the start of every session and kept in sync. See "Maintenance rules"
> at the bottom — they are mandatory, not optional.
>
> Last updated: 2026-08-27 (FX-52: the community cover uploads, reports success and changes nothing — the development API is running code older than kolabing-v2#239, which emits no `cover_photo` key at all; the app side no longer claims success when the server drops the field. Prior 2026-08-26 (IF-38, extended — Manage → Rewards opened the attendee's own points wallet and Manage → Events pushed the whole old Community page; both leader surfaces are extracted into their own screens. IF-38: **one community page — seeing it and running it are the same page now** — issue #174. A leader had their community across two screens that disagreed about their job: the Community tab showed it with management scattered behind twenty `canManage` gates, while everything editable sat on a separate profile page. Now one page: cover hero → identity → Invite link · New event → **Manage** → the profile sections. Business deliberately untouched. Prior 2026-08-25 (IF-37: **Multi-Kolab mobile revived onto master** — issue #171. The mobile feature was never missing: ~19,900 lines across 102 files sat on `feat/multi-kolab-event-mvp`, cut from a `master` 11 PRs old. Merged onto today's master, reconciled with the current Explore card and ~1,000 new l10n keys, and it compiles and passes: **716 pass / 10 pre-existing failures**, 0 analyze errors. Backend was already live (kolabing-v2#141, merged 2026-08-19). Prior 2026-08-25 (**IF-36 — events feed'i Explore ile aynı karta geçti** (Volkan, 2026-08-25). İki göz atma yüzeyi arasındaki tek yapısal fark kartın kendisiydi: Explore'da bir Kolab **16:10 fotoğraf kartı** (carousel + nokta göstergeleri + isim + meta + çipler + "View Details ›"), events feed'inde ise bir event **72px küçük resimli satırdı**. Başlık, segment sekmeleri ve filtre çipleri zaten ikisinde de vardı. Yeni `EventFeedCard` `ExploreSwipeCard`'ın anatomisini birebir alarak `Event`'e uyguluyor; `EventTimeline` bir `variant` aldı (`row` varsayılan, `card` opt-in), böylece **topluluk sayfaları hiç değişmedi** — sadece attendee feed'inin *What's on* bölümü kart çiziyor. **Tarih başlıkları kaldı**: bir events feed'i program gibi okunur, deck gruplayamaz — Explore bir eşleştirme yüzeyi (tek tek karar ver), events bir gezinme yüzeyi. **Explore'un kopyalanmayan tek yanı:** o kartın gövdesi koşulsuz beyaz, bu yüzden çiplerini `forceLightSurface`'e zorlamak zorunda; bu kart `context.colors.surface` boyuyor, yani dark mode'da doğru ve çipleri override istemiyor. Kilitli event hala fotoğrafını göstermiyor (blur + lock hint), mekânı olmayan event satırı yer tutucu basmıyor. `flutter analyze` **0 error**; 5 yeni test. Önceki: 2026-08-25 (**IF-35 / FX-50 — a community or a business is a link now, wherever it appears.** Reported by Volkan from the Kolab drawer: the sheet's header shows a logo, a name and a `Community` badge and none of it did anything. The cause was not that one sheet — **`/profile/:id` was reachable from four screens in the whole app**, so the page the product treats as its centre was the page almost nobody could get to. New `ProfileLink` widget wraps an identity block and opens it, and it owns the destination rule that was previously copied into `event_detail_screen` and `explore_screen`: a business viewer keeps the profile-id-keyed page (its Send-Kolab flow starts there), everyone else gets the community-keyed one, and a `communities.id` is never pushed into `/profile/{id}` (it would 404 every profile endpoint). Wired into the Kolab drawer header, the saved-Kolab sheet, the reviews list (ROLES §4.2 asks for a review to carry "the reviewer's name, avatar **and a link to their profile**" — the link was the missing third), Discover communities (whose rows had **no tap target at all**, so the only thing you could do with a community was follow it sight unseen) and the chat header. **FX-50, found on the way:** the saved-Kolab sheet never passed `hideCreatorIdentity`, so a free business that *saved* a community Kolab saw the name and logo the deck had blurred — saving was a way around §2.5. Now blurred there too, and the header link is null whenever the identity is blurred: a blur that ripples under the finger announces exactly what it is hiding. The chat header's link is also off during the §2.8 re-gate. **Deliberately not linked:** compact cards whose own tap already opens a screen that links the identity (dashboard upcoming, Kolab cards, the swipe deck, the event timeline's 16px host line) — a nested gesture there competes with the card and the profile is one tap away regardless. **Blocked on the backend:** `past_collaboration_card` shows `partnerName` and the payload carries no partner id. `flutter analyze` **0 errors**, 1154 issues vs a 1155 baseline; 5 new tests, all green. Önceki: 2026-08-25 (FX-49: **the Continue button landed on top of the city search field once the keyboard opened** — all three onboarding city steps, now on one keyboard-safe shell. Prior 2026-08-25 (FX-49: **the Continue button landed on top of the city search field once the keyboard opened** — all three onboarding city steps, now on one keyboard-safe shell. Prior 2026-08-23 (IF-29: **attendees see the events of communities they follow** — issue #142, app PR #143 + backend kolabing-v2#214. A follow was recorded and then read by nothing, so the tap had no consequence; the attendee feed now has an **All events / Following** scope. Following sends no city (a followed community is worth knowing about wherever it is) and no type, keeps the date filter, and keeps the city in state so switching back is free. Two empty states: follows-nobody vs followed-communities-with-nothing-on. **The backend half also closed a leak it turned up:** `GET /events?following=me` (merged in kolabing-v2#211, never called by the app) scoped by follow and nothing else, so a follower received a community's **member- and tier-only events, ids included** — now gated to `visibility=public` on that branch alone. That half-fixes IF-28's open decision (a): the listing is closed, `EventSignupService` is still not visibility-checked. — Also IF-30: **a peer challenge pays both people** (issue #140, app PR #141 + backend kolabing-v2#213): the verification QR is gone (one scan, not three), both sides earn, and `ChallengeTogetherScreen` is one screen both devices show. Leaderboard/room layer deferred by product decision. Both pairs of PRs are open, unmerged, and still need device QA + screenshots.) — Prior 2026-08-22 (IF-28: **community follower / member split** — issue #138. One relationship became two: `community_followers` (one tap, no approval) and application-gated `community_members`. Backend shipped in **kolabing-v2 PR #211** (3 additive tables, 41 tests, suite 2360 green); app side not started. Two decisions still open — a follower can still sign up to a `members`-visibility event (`EventSignupService` never checks visibility for a community's event), and for `open` communities `/join` bypasses the questions entirely. See IF-28. — Also 2026-08-17 (FX-46: **App Store rejection — referral-code field on the paywall reads as an alternate purchase mechanism (Guideline 3.1.1)**, from Daniel in the Kolabing Telegram group. Removed `ReferralCodeField` + its controller/error/helper state from both purchase surfaces — the modal `SubscriptionPaywall` and `SubscriptionScreen`'s subscribe CTA — and stopped passing `referralCode` from those call sites into `purchase()`/`createCheckoutSession()`. The register/signup referral field (`business_final_screen.dart`) is untouched. Branch `fix/paywall-remove-referral-code-field`, issue #123. `flutter analyze` clean (1 pre-existing `directives_ordering` info), 13/13 subscription tests green + 2 paywall tests updated to assert the field is gone, full suite's 10 pre-existing failures unrelated (verified via `git stash` against master). **Flagging, not fixing (separate, pre-existing gap):** the referral-code field on the REGISTER screen was already a no-op on the backend — `RegisterBusinessRequest` doesn't accept/store `referral_code` at all — so "the code entered at register carries through to purchase" isn't actually true today; the referral-at-purchase reward (`ReferralService::rewardFirstPaidSubscription`) is now unreachable from any UI since nothing passes a code anymore. That's a real product decision (persist register-time code server-side + auto-apply at first subscription, or accept referral rewards are dormant) — not resolved here, out of scope for the Apple-blocking fix. **Pending:** device QA (paywall renders clean without the field, subscribe flow still works), issue #123 add to Project 4 board (Clark's GitHub PAT lacks `read:project` scope), then resubmit.) — Prior 2026-08-14 (FX-45: **every subscription price is quoted in euro again** — product decision reversing FX-44's display half. The simulator (Xcode + `Configuration.storekit`, US test storefront) showed `$99.99 / $49.99`; a new `IAPState.formatEuro` now renders `displayPriceFor` / `priceString` / `perMonthEquivalent` from `rawPrice` with `€`, so the paywall reads **€99.99 / 3 months · ≈ €33.33/mo · €49.99 / month** — verified on the iPhone 17 Pro sim. Branch `fix/subscription-currency-and-cancel`, issue #119, 13/13 subscription tests green. **Trade-off resolved (Volkan, 2026-08-14): the euro stays, and App Store availability will be limited to euro-zone storefronts in ASC** — Apple then bills in euro too, so the paywall's `€` is literally correct and the 3.1.2 risk disappears. **Both fix branches are now merged to master:** PR **#116** (FX-43 permission fix, merge `af28da8`) then PR **#118** (FX-44/FX-45 subscription fix, retargeted to master, merge `0c54e83`); verified before merge — `flutter analyze` **0 errors** (1118 info/12 warning, all pre-existing) and **13/13** subscription tests green. Neither PR carried its device screenshots, merged on Volkan's explicit call. **Remaining before resubmit:** the ASC euro-zone availability change, #101 (attach the IAP products to the version), #100 (demo account with an expired subscription), #97 (privacy labels — drop "Used to Track You" + Coarse Location), device QA of the paywall/cancel/permission screens, then `flutter build ipa` for **1.5.1 (35)**.) — Prior 2026-08-14 (FX-43: **App Review 5.1.1(iv)** — the permission screen's pre-prompt button said "Allow" (Apple wants "Continue"/"Next"), and the Location row asked for a permission no shipped feature uses. Branch `fix/appreview-5-1-1-permission-copy` (#115): button/title copy fixed, Location row hidden behind new `kLocationPermissionPromptEnabled=false`, screen fully i18n'd (12 keys en/es/ca), unused `NSLocationAlwaysAndWhenInUseUsageDescription` dropped, accuracy → `reduced`, `Configuration.storekit` → 49.99, build 35. The same 2026-08-12 rejection also carried 5.1.2(i) (privacy labels — ASC only, corrected after the review date) and 2.1(b) (IAP products never submitted — #101/#100, still open).) — Prior 2026-08-07 (FX-42: monthly price corrected **€49 → €49.99** — the App Store Connect tier chosen is 49,99 €, so all user-visible fallbacks/strings (paywall Android/Stripe box, iOS fallback, plan-detail row, subscribe button; en/es/ca) + docs now match; branch `chore/pricing-49-99`. ASC price point set to 49,99 € the same day; iOS build **1.5.1 (34)** archived + uploaded (#113, which also synced `ios/Podfile.lock` by dropping the stale `app_links` pod). Backend `config/subscriptions.php` in kolabing-v2 still needs the same bump, and the paywall still needs an on-device walkthrough. — Also merged this day: PRs #109 (€49 pricing + App Review 2026-08-04 doc), #106 (FX-39 EULA/Privacy paywall footer), #93 (FX-41 redacted body logs), #111 (FX-42 €49.99), #113 (build 34).) — Prior 2026-08-07 (FX-39: App Store Guideline 3.1.2 rejection — added Terms of Use (EULA) + Privacy Policy links + auto-renew disclosure to the subscription paywall & screen via new `SubscriptionLegalFooter`; branch `fix/subscription-eula-privacy-links-3-1-2`, merged on top of the €49 pricing change. NOTE: metadata EULA link in App Store Connect + resubmit still required, and gen-l10n/analyze/format must be run on a Flutter machine — no SDK on the build box.) — Prior 2026-08-05 (FX-36: App Store rejection 2026-08-04 resolution prep — ASC privacy labels + Paid Apps Agreement, both account-level; €39.99→€49 across en/es/ca + paywall/status fallbacks; see docs/release/APP-REVIEW-2026-08-04-resolution.md) — Prior 2026-07-31 (IF-26: **UGC moderation — report / block / EULA** (App Review Guideline 1.2, #99) built on branch `feat/ugc-moderation` — new `lib/features/moderation/` (`ModerationService` self-gating on 404, `blockedProfilesProvider` optimistic `Notifier<Set<String>>`, `ReportSheet`, `ModerationMenu`), wired into public-profile / chat-thread overflow (Report+Block counterpart), Explore detail sheet (Report Kolab) and review rows (Report review); instant client-side filtering of blocked creators/authors/threads across the Explore deck, reviews list and chats list; `authNoToleranceNotice` EULA line above `TermsConsentCheckbox`; i18n en/es/ca + gen-l10n; analyze 0 new; 9 moderation tests green. Pending backend kolabing-v2 #115 + App-Review device recording. See Incomplete Features IF-26.) — Prior 2026-07-28 (FX-35: **Business self-view reputation block** — the owner now sees their own rating/reviews/partners/completed-Kolabs on their profile via a shared `ReputationSummaryCard`, reusing the public-profile endpoint; audit #13 remaining half, branch `feat/business-self-reputation` (#84). Partner-status badge deferred to PR #70/#14.) — Prior 2026-07-27 (FX-34: **P0 apply-flow block** — Apply now gated on date availability across the Explore deck, offer-detail sheet, apply modal, and backend; ✅ merged to master (app #83, backend #108). App branch `fix/apply-date-availability-gate` (#82) + backend `fix/apply-reject-closed-kolab` (kolabing-v2 #107).) — Prior 2026-07-19 (FX-32: **Trust-surface / review-CTA / i18n cleanup from mobile audit** built on branch `fix/p1-trust-review-i18n-cleanup` (#77) — four P1 findings from the in-progress product audit (`kolabing-v2/docs/audit-output/00-audit-progress-handoff.md`): (1) `collaborations_list_tab.dart` My Kolabs Finished/Active card title always led with the business name, so a long business name pushed the actual partner off the truncated 2-line title — a business viewer saw only their own name. Now leads with the counterpart, matching the photo logic. (2) `chats_screen.dart` `_titleFor` joined ALL participants including the viewer (e.g. "Real Run Club, Eixample 46 …") — now filters by the viewer's own `profileId`. **Left alone (backend gap):** the chat inbox's "Tap to open" placeholder — `GET /chats`/`ChatThread` only expose `last_message_at`, never message body text, so no real preview is possible without a new backend field. (3) Post-completion review CTA in `collaboration_detail_screen.dart` was a plain "Leave optional feedback" text row — rebuilt to match the primary Complete-Kolab CTA's visual weight (title+body+solid button), new copy "Rate this Kolab"/"Leave a review", new ARB keys en/es/ca. (4) ~18 hardcoded English strings moved out of `business_dashboard_screen.dart` + the hardcoded `'Chats'` nav label in both main screens into ARB keys (reused existing `chatsTitle`); also localized a stray `'Something went wrong'` in `collaborations_list_tab.dart` via `commonErrorGeneric`. (5) Removed the dead compile-time `_kDashboardVariant` A/B/C switcher + 2 unused variant builders from `community_dashboard_screen.dart`. `flutter analyze` 0 new issues, `flutter gen-l10n` regenerated clean for en/es/ca. Full test suite: 17 pre-existing failures, none in touched files (verified via `git stash` against clean master). PENDING: screenshots + device e2e.) — Prior 2026-07-12 (AUTH: **Terms of Service + Privacy Policy consent flow** built on branch `feat/consent-flow` (#65, PR to open): mandatory `TermsConsentCheckbox` on all sign-up screens (attendee register + business/community final) gating the primary button (and attendee Google/Apple); email register sends `accepted_terms:true`; `UserTerms` parsed from `/auth/me`; full-screen blocking `ReconsentGate` in `_AuthSessionRedirector` (POST `/me/consent` → refresh → clear) + resume refresh via `WidgetsBindingObserver`; `LegalLinks` (ES localized, else EN); i18n en/es/ca; 6 consent unit tests green; analyze 0 new issues. PENDING: device screenshots + e2e. Backend endpoints already live.) — Prior 2026-07-12 (UI: **Community Members tab hidden** on branch `feat/hide-community-members-tab` (#61) — community detail now shows only **Rewards + Events**; the Members tab + its view are gated behind a new `kCommunityMembersTabEnabled=false` flag (`lib/config/feature_flags.dart`), with TabController length/initialIndex and the attendee "See all →" `_eventsTabIndex` derived from the flag so Events shifts 2→1 cleanly. Flip to re-enable. Analyze 0 new issues.) — Prior 2026-07-12 (UI: **Attendee not-ready gating** on branch `feat/attendee-coming-soon` (#59) — "Choose your path" attendee card now shows a **"Coming soon"** badge and is disabled; its label renamed **"I'm an attendee" → "I'm a member"** (EN/ES/CA). In-app gamification hidden behind a new `kGamificationSetupEnabled` flag (`lib/config/feature_flags.dart`): the challenge-setup + QR check-in cards on the collaboration detail screen and the "Scan Check-Ins" action on the event hub. Flip the flag to re-enable. Analyze 0 new issues.) — Prior 2026-07-04 (FX-31: **Notification tap (app killed) doesn't open target screen** fixed on branch `fix/notification-tap-stuck-loading` — cold-start splash race; new `NotificationNavGate` defers the tap past splash's stack-replacing `context.go` and replays it. Analyze 0 new issues, 6 gate tests green, pending device e2e. #55.) — Prior 2026-06-27 (IF-23: **My Kolabs — Bolder filter/action redesign** built on branch `feat/my-kolabs-bolder-redesign` — new reusable `KolabingSegmentedControl` (primary OFFERS/REQUEST/ACTIVE/FINISHED row + secondary PUBLISHED/DRAFT and SENT/RECEIVED rows, both now driven by it) and `KolabingCardActionBar`/`KolabingIconPillButton` replacing the floating circular card icon buttons. Presentation-only, all copy/counts/callbacks unchanged. `flutter analyze` 0 new issues, relevant tests green. PENDING: full on-device visual QA (confirmed via simulator screenshot for the primary control on iOS; the Requests-tab secondary-control fix and Android not yet visually confirmed).) — Prior 2026-06-27 (NF-22: **Saved Kolabs** ticketed + planned — "Save for later" is a dead stub and there's no Saved surface; add a **Saved** tab (Recommended | All | Saved) + make saving real. Backend has zero support → app **#43** blocked on backend **kolabing-v2#61**; plan `docs/plans/2026-06-27-saved-kolabs.md`. Both issues on Project 4 "Kolabing Engineering".) — Prior 2026-06-22 (FX-30 / IF-2 / IF-5: Kolab completion fixed — feedback-first flow. "Yes, complete Kolab" no longer instantly errors; the sheet now POSTs the gate endpoint `/feedback` (role-aware payload) BEFORE `/complete`, the provider parses `error_code`+`message`, and `awaiting_partner_feedback` is shown as a soft success ("your feedback is in, completes once {partner} confirms") instead of a red generic error. Branch `fix/kolab-completion-flow`, analyze 0 new errors, not pushed.) — Prior 2026-06-21 (FX-29: chat "View opportunity" now shows the community's submission to the business creator instead of an APPLY NOW page on their own kolab. #34. — FX-28: community-dashboard error-card crash fixed — `WalletNotifier`/`GalleryNotifier` `late final` reassigned on invalidate→rebuild; now lazy getters. #32.) — Prior 2026-06-18 (IF-22: **dev/prod environment separation (#17)** built on `feat/env-dev-prod-flavors` — single `Environment` source of truth, env chosen at build time via `--dart-define=APP_ENV=dev|prod` (safe default: prod in release, dev in debug), `Makefile` build targets; iOS-only "one app, two builds", Android dropped, no native changes; analyze 0, env tests 8/8. Pending PR + dev TestFlight upload + e2e.) — Prior 2026-06-11 (IF-21: **attendee Google + Apple social login** built on branch `feat/attendee-social-login` — Google live now (backend `/auth/google` already honors `user_type`); Apple now **enabled** (App Store Guideline 4.8 requires Sign in with Apple alongside Google) with backend `/auth/apple` `user_type` as a **hard pre-merge dependency** (kolabing-v2 PR #24); register screen redesigned so Google+Apple sit side-by-side and the whole page fits the viewport with no scroll; new social attendees route into the 4-step onboarding with name prefilled; `analyze` 0, 187 tests green. App PR #9 + backend PR #24 open. Spec/plan in `docs/superpowers/{specs,plans}/2026-06-11-attendee-social-login*.md`. — FX-25: attendee community-profile — event host-community tap now opens a real community-keyed screen (events + state-aware join CTA) instead of the mock business `PublicProfileScreen`; new `/community/:id/profile` route, `Community.isMember`/`joinPolicy`/`myJoinRequestStatus`, self-gated join-requests. FX-24: discovered-event detail (`EventDetailScreen`/`/event/:id`) gained RSVP + tappable host-community link. — NF-20 → IF-20: event location now uses the existing Google Places autocomplete; city auto-derived from the picked venue; manual city-picker removed. App built on `fixes-attendee`, `flutter analyze` 0 errors, NOT committed, pending e2e. Prior: `fixes-attendee` bug pass — see FX-10..FX-15: signup-500 Postgres-lock fixed in `kolabing-v2` [pending deploy]; community-paywall cleanup; gallery/past-events wired live; community-create photo-wipe fixed backend [pending deploy]; attendee sign-up card wired reachable; chat-visibility diagnosed [held for decision]. New attendee-track build order set: NF-13 social-hub → NF-9 languages → NF-17 friends → NF-14 DMs. #6 attendee-profile-broken NOT reproduced — left for Daniel to confirm. Prior 2026-06-09: NF-16 B4 → IF-18: real-time chat **Part B (Flutter client) BUILT** on branch `feat/realtime-chat-reverb-client` — `dart_pusher_channels` over self-hosted Reverb; `RealtimeChatService` + `RealtimeConfig` (self-gating, dormant until `REVERB_APP_KEY`) wired into `ChatThreadScreen` (live append + dedup + cancel-on-dispose; poll-on-open kept as fallback). Contract verified against backend `NewChatMessage` (`broadcastAs message.sent`, payload `{message: ChatMessageResource}`, channel `private-chat.thread.{id}`, root `/broadcasting/auth`). `flutter analyze` 0 errors, 166 tests green. **Part A (ops)** left as a ticket in `kolabing-v2/docs/tickets/2026-06-09-reverb-realtime-chat-PART-A-ops.md` — code already exists (event + channel auth + broadcasting route); only env + Reverb daemon + queue worker + nginx/TLS remain. Handoff: backend returns `REVERB_APP_KEY` → app flips live, no further code change. — NF-ANALYTICS → IF-17: PostHog product analytics **MERGED to master** (PR #8, merge `163a1af`) — curated (no autocapture/replay), identify-by-user-id with role props (no PII), 19 events (auth, paywall KPIs kolab_published/application_submitted, collaboration lifecycle, community/tier/join, events create/RSVP/check-in, messaging, subscription_started). `posthog_flutter ^5.26.0`, EU project 195047, key in `lib/config/constants/analytics.dart`; fail-safe service; ingestion verified live. Doc: `.agent/documentations/analytics-events.md`. — Prior 2026-06-05: NF-16 follow-ups built on `community-member-flow`: (1) i18n — mandatory-i18n rule added to CLAUDE.md + memory; full sweep localized all community/chat screen literals (207 keys, en/es/ca @ 1,516); new event UI born translated. (2) Event DELETE — backend `event_cancelled` fan-out to going/waitlisted on delete; app overflow→Delete with scope (this/following/series for recurring). (3) RECURRING EVENTS Phase 1 (hybrid "publish 3 months, extend later") — backend `event_series` (weekly/biweekly/monthly, multi-day byweekday e.g. Tue+Thu, rolling materialize + `extend`, scoped delete, chat_mode, end rules) wired into POST /events `recurrence`, `DELETE /events/{id}?scope=`, `POST /event-series/{id}/extend`; app create-form Repeat section (freq chips, locale-aware weekday multi-select, ends never/after-N/on-date, per-event vs shared-series chat) + 🔁 list badge + Event.seriesId. Backend 863 green (committed 4043404, NOT yet pushed/deployed); app 166 green (commits 57656b8, 1ffef63). DEFERRED sub-items (not built): edit→recurring (convert one-off on PUT), scoped EDIT, app "Extend series" button (endpoint exists), shared-series-chat *delivery* (chat_mode stored but createEventChat still per-occurrence), events→Kolab bridge (ticket: docs/tickets/2026-06-05-events-to-kolab-bridge.md). PENDING: push+deploy backend; e2e verify on sim. — Prior shipped + deployed: create-event blank fixed [OutlinedButton-in-Row vs full-width theme]; event list now refreshes instantly after create/edit/RSVP [`communityUpcomingEventsProvider` → Notifier+reload, same lesson as the tier-instant-refresh bug]; tier-gated events now LOCK for ineligible members [backend `EventResource.can_access` + app 🔒 tile, no open into details]; per-chat tier gating UI [backend `ChatThreadResource.slug` + app per-chat "Access" picker writing each tier's `permissions.chat_channels`]. App 166 + backend 856 green; backend `c75d660` pushed to master. NF-16 → IF-16: app B1–B3 built on `feat/nf16-events-attendee-app` — event management hub + create/edit form + notification settings; B4 real-time deferred, ops-gated. Prior: added NF-11 Chat Phase 3 [events RSVP lifecycle + event chats + Reverb real-time — backend ticket written], NF-12 chat/community IA restructure [chats in nav, community-detail tabs, profile→hidden], NF-13 attendee social-hub profile [Friends + uploadable cover], NF-14 member DMs [pending decision]. Prior: merged origin/master i18n es/ca; FX-9 stale-chat fixed + confirmed; NF-CHAT Phase 1+2 live; IF-7 community members+tiers app done.) — Also IF-27: **the event-challenge QR loop is now playable end to end** — issue #132, branch `feat/event-challenge-qr-gamification`. One scanner reads all three Kolabing codes (`QrPayload.parse`: opaque check-in token · `/u/{profileId}` peer · `/qr/verify/{completionId}`), a check-in opens a local `ActiveEventSession` that scopes peer pairing to the right event, picking a challenge calls `POST /challenges/initiate` with the scanned member as verifier, and the verifier scanning the challenger's QR calls `verify`, which writes `point_ledger`. **Zero backend work** — every endpoint was already live and already wrapped; the gap was app wiring. Three previously unreachable things are now reachable: `EventQRCodeScreen` (route existed, nothing pushed it), the scanner (was on the leader's side, where `POST /checkin` would have checked the leader in), and verification (service existed, no UI). `kGamificationSetupEnabled` flipped to `true`. See IF-27; NF-21 narrowed to the surfacing half. — Also 2026-08-17 (FX-46: **App Store rejection — referral-code field on the paywall reads as an alternate purchase mechanism (Guideline 3.1.1)**, from Daniel in the Kolabing Telegram group. Removed `ReferralCodeField` + its controller/error/helper state from both purchase surfaces — the modal `SubscriptionPaywall` and `SubscriptionScreen`'s subscribe CTA — and stopped passing `referralCode` from those call sites into `purchase()`/`createCheckoutSession()`. The register/signup referral field (`business_final_screen.dart`) is untouched. Branch `fix/paywall-remove-referral-code-field`, issue #123. `flutter analyze` clean (1 pre-existing `directives_ordering` info), 13/13 subscription tests green + 2 paywall tests updated to assert the field is gone, full suite's 10 pre-existing failures unrelated (verified via `git stash` against master). **Flagging, not fixing (separate, pre-existing gap):** the referral-code field on the REGISTER screen was already a no-op on the backend — `RegisterBusinessRequest` doesn't accept/store `referral_code` at all — so "the code entered at register carries through to purchase" isn't actually true today; the referral-at-purchase reward (`ReferralService::rewardFirstPaidSubscription`) is now unreachable from any UI since nothing passes a code anymore. That's a real product decision (persist register-time code server-side + auto-apply at first subscription, or accept referral rewards are dormant) — not resolved here, out of scope for the Apple-blocking fix. **Pending:** device QA (paywall renders clean without the field, subscribe flow still works), issue #123 add to Project 4 board (Clark's GitHub PAT lacks `read:project` scope), then resubmit.) — Prior 2026-08-14 (FX-45: **every subscription price is quoted in euro again** — product decision reversing FX-44's display half. The simulator (Xcode + `Configuration.storekit`, US test storefront) showed `$99.99 / $49.99`; a new `IAPState.formatEuro` now renders `displayPriceFor` / `priceString` / `perMonthEquivalent` from `rawPrice` with `€`, so the paywall reads **€99.99 / 3 months · ≈ €33.33/mo · €49.99 / month** — verified on the iPhone 17 Pro sim. Branch `fix/subscription-currency-and-cancel`, issue #119, 13/13 subscription tests green. **Trade-off resolved (Volkan, 2026-08-14): the euro stays, and App Store availability will be limited to euro-zone storefronts in ASC** — Apple then bills in euro too, so the paywall's `€` is literally correct and the 3.1.2 risk disappears. **Both fix branches are now merged to master:** PR **#116** (FX-43 permission fix, merge `af28da8`) then PR **#118** (FX-44/FX-45 subscription fix, retargeted to master, merge `0c54e83`); verified before merge — `flutter analyze` **0 errors** (1118 info/12 warning, all pre-existing) and **13/13** subscription tests green. Neither PR carried its device screenshots, merged on Volkan's explicit call. **Remaining before resubmit:** the ASC euro-zone availability change, #101 (attach the IAP products to the version), #100 (demo account with an expired subscription), #97 (privacy labels — drop "Used to Track You" + Coarse Location), device QA of the paywall/cancel/permission screens, then `flutter build ipa` for **1.5.1 (35)**.) — Prior 2026-08-14 (FX-43: **App Review 5.1.1(iv)** — the permission screen's pre-prompt button said "Allow" (Apple wants "Continue"/"Next"), and the Location row asked for a permission no shipped feature uses. Branch `fix/appreview-5-1-1-permission-copy` (#115): button/title copy fixed, Location row hidden behind new `kLocationPermissionPromptEnabled=false`, screen fully i18n'd (12 keys en/es/ca), unused `NSLocationAlwaysAndWhenInUseUsageDescription` dropped, accuracy → `reduced`, `Configuration.storekit` → 49.99, build 35. The same 2026-08-12 rejection also carried 5.1.2(i) (privacy labels — ASC only, corrected after the review date) and 2.1(b) (IAP products never submitted — #101/#100, still open).) — Prior 2026-08-07 (FX-42: monthly price corrected **€49 → €49.99** — the App Store Connect tier chosen is 49,99 €, so all user-visible fallbacks/strings (paywall Android/Stripe box, iOS fallback, plan-detail row, subscribe button; en/es/ca) + docs now match; branch `chore/pricing-49-99`. ASC price point set to 49,99 € the same day; iOS build **1.5.1 (34)** archived + uploaded (#113, which also synced `ios/Podfile.lock` by dropping the stale `app_links` pod). Backend `config/subscriptions.php` in kolabing-v2 still needs the same bump, and the paywall still needs an on-device walkthrough. — Also merged this day: PRs #109 (€49 pricing + App Review 2026-08-04 doc), #106 (FX-39 EULA/Privacy paywall footer), #93 (FX-41 redacted body logs), #111 (FX-42 €49.99), #113 (build 34).) — Prior 2026-08-07 (FX-39: App Store Guideline 3.1.2 rejection — added Terms of Use (EULA) + Privacy Policy links + auto-renew disclosure to the subscription paywall & screen via new `SubscriptionLegalFooter`; branch `fix/subscription-eula-privacy-links-3-1-2`, merged on top of the €49 pricing change. NOTE: metadata EULA link in App Store Connect + resubmit still required, and gen-l10n/analyze/format must be run on a Flutter machine — no SDK on the build box.) — Prior 2026-08-05 (FX-36: App Store rejection 2026-08-04 resolution prep — ASC privacy labels + Paid Apps Agreement, both account-level; €39.99→€49 across en/es/ca + paywall/status fallbacks; see docs/release/APP-REVIEW-2026-08-04-resolution.md) — Prior 2026-07-31 (IF-26: **UGC moderation — report / block / EULA** (App Review Guideline 1.2, #99) built on branch `feat/ugc-moderation` — new `lib/features/moderation/` (`ModerationService` self-gating on 404, `blockedProfilesProvider` optimistic `Notifier<Set<String>>`, `ReportSheet`, `ModerationMenu`), wired into public-profile / chat-thread overflow (Report+Block counterpart), Explore detail sheet (Report Kolab) and review rows (Report review); instant client-side filtering of blocked creators/authors/threads across the Explore deck, reviews list and chats list; `authNoToleranceNotice` EULA line above `TermsConsentCheckbox`; i18n en/es/ca + gen-l10n; analyze 0 new; 9 moderation tests green. Pending backend kolabing-v2 #115 + App-Review device recording. See Incomplete Features IF-26.) — Prior 2026-07-28 (FX-35: **Business self-view reputation block** — the owner now sees their own rating/reviews/partners/completed-Kolabs on their profile via a shared `ReputationSummaryCard`, reusing the public-profile endpoint; audit #13 remaining half, branch `feat/business-self-reputation` (#84). Partner-status badge deferred to PR #70/#14.) — Prior 2026-07-27 (FX-34: **P0 apply-flow block** — Apply now gated on date availability across the Explore deck, offer-detail sheet, apply modal, and backend; ✅ merged to master (app #83, backend #108). App branch `fix/apply-date-availability-gate` (#82) + backend `fix/apply-reject-closed-kolab` (kolabing-v2 #107).) — Prior 2026-07-19 (FX-32: **Trust-surface / review-CTA / i18n cleanup from mobile audit** built on branch `fix/p1-trust-review-i18n-cleanup` (#77) — four P1 findings from the in-progress product audit (`kolabing-v2/docs/audit-output/00-audit-progress-handoff.md`): (1) `collaborations_list_tab.dart` My Kolabs Finished/Active card title always led with the business name, so a long business name pushed the actual partner off the truncated 2-line title — a business viewer saw only their own name. Now leads with the counterpart, matching the photo logic. (2) `chats_screen.dart` `_titleFor` joined ALL participants including the viewer (e.g. "Real Run Club, Eixample 46 …") — now filters by the viewer's own `profileId`. **Left alone (backend gap):** the chat inbox's "Tap to open" placeholder — `GET /chats`/`ChatThread` only expose `last_message_at`, never message body text, so no real preview is possible without a new backend field. (3) Post-completion review CTA in `collaboration_detail_screen.dart` was a plain "Leave optional feedback" text row — rebuilt to match the primary Complete-Kolab CTA's visual weight (title+body+solid button), new copy "Rate this Kolab"/"Leave a review", new ARB keys en/es/ca. (4) ~18 hardcoded English strings moved out of `business_dashboard_screen.dart` + the hardcoded `'Chats'` nav label in both main screens into ARB keys (reused existing `chatsTitle`); also localized a stray `'Something went wrong'` in `collaborations_list_tab.dart` via `commonErrorGeneric`. (5) Removed the dead compile-time `_kDashboardVariant` A/B/C switcher + 2 unused variant builders from `community_dashboard_screen.dart`. `flutter analyze` 0 new issues, `flutter gen-l10n` regenerated clean for en/es/ca. Full test suite: 17 pre-existing failures, none in touched files (verified via `git stash` against clean master). PENDING: screenshots + device e2e.) — Prior 2026-07-12 (AUTH: **Terms of Service + Privacy Policy consent flow** built on branch `feat/consent-flow` (#65, PR to open): mandatory `TermsConsentCheckbox` on all sign-up screens (attendee register + business/community final) gating the primary button (and attendee Google/Apple); email register sends `accepted_terms:true`; `UserTerms` parsed from `/auth/me`; full-screen blocking `ReconsentGate` in `_AuthSessionRedirector` (POST `/me/consent` → refresh → clear) + resume refresh via `WidgetsBindingObserver`; `LegalLinks` (ES localized, else EN); i18n en/es/ca; 6 consent unit tests green; analyze 0 new issues. PENDING: device screenshots + e2e. Backend endpoints already live.) — Prior 2026-07-12 (UI: **Community Members tab hidden** on branch `feat/hide-community-members-tab` (#61) — community detail now shows only **Rewards + Events**; the Members tab + its view are gated behind a new `kCommunityMembersTabEnabled=false` flag (`lib/config/feature_flags.dart`), with TabController length/initialIndex and the attendee "See all →" `_eventsTabIndex` derived from the flag so Events shifts 2→1 cleanly. Flip to re-enable. Analyze 0 new issues.) — Prior 2026-07-12 (UI: **Attendee not-ready gating** on branch `feat/attendee-coming-soon` (#59) — "Choose your path" attendee card now shows a **"Coming soon"** badge and is disabled; its label renamed **"I'm an attendee" → "I'm a member"** (EN/ES/CA). In-app gamification hidden behind a new `kGamificationSetupEnabled` flag (`lib/config/feature_flags.dart`): the challenge-setup + QR check-in cards on the collaboration detail screen and the "Scan Check-Ins" action on the event hub. Flip the flag to re-enable. Analyze 0 new issues.) — Prior 2026-07-04 (FX-31: **Notification tap (app killed) doesn't open target screen** fixed on branch `fix/notification-tap-stuck-loading` — cold-start splash race; new `NotificationNavGate` defers the tap past splash's stack-replacing `context.go` and replays it. Analyze 0 new issues, 6 gate tests green, pending device e2e. #55.) — Prior 2026-06-27 (IF-23: **My Kolabs — Bolder filter/action redesign** built on branch `feat/my-kolabs-bolder-redesign` — new reusable `KolabingSegmentedControl` (primary OFFERS/REQUEST/ACTIVE/FINISHED row + secondary PUBLISHED/DRAFT and SENT/RECEIVED rows, both now driven by it) and `KolabingCardActionBar`/`KolabingIconPillButton` replacing the floating circular card icon buttons. Presentation-only, all copy/counts/callbacks unchanged. `flutter analyze` 0 new issues, relevant tests green. PENDING: full on-device visual QA (confirmed via simulator screenshot for the primary control on iOS; the Requests-tab secondary-control fix and Android not yet visually confirmed).) — Prior 2026-06-27 (NF-22: **Saved Kolabs** ticketed + planned — "Save for later" is a dead stub and there's no Saved surface; add a **Saved** tab (Recommended | All | Saved) + make saving real. Backend has zero support → app **#43** blocked on backend **kolabing-v2#61**; plan `docs/plans/2026-06-27-saved-kolabs.md`. Both issues on Project 4 "Kolabing Engineering".) — Prior 2026-06-22 (FX-30 / IF-2 / IF-5: Kolab completion fixed — feedback-first flow. "Yes, complete Kolab" no longer instantly errors; the sheet now POSTs the gate endpoint `/feedback` (role-aware payload) BEFORE `/complete`, the provider parses `error_code`+`message`, and `awaiting_partner_feedback` is shown as a soft success ("your feedback is in, completes once {partner} confirms") instead of a red generic error. Branch `fix/kolab-completion-flow`, analyze 0 new errors, not pushed.) — Prior 2026-06-21 (FX-29: chat "View opportunity" now shows the community's submission to the business creator instead of an APPLY NOW page on their own kolab. #34. — FX-28: community-dashboard error-card crash fixed — `WalletNotifier`/`GalleryNotifier` `late final` reassigned on invalidate→rebuild; now lazy getters. #32.) — Prior 2026-06-18 (IF-22: **dev/prod environment separation (#17)** built on `feat/env-dev-prod-flavors` — single `Environment` source of truth, env chosen at build time via `--dart-define=APP_ENV=dev|prod` (safe default: prod in release, dev in debug), `Makefile` build targets; iOS-only "one app, two builds", Android dropped, no native changes; analyze 0, env tests 8/8. Pending PR + dev TestFlight upload + e2e.) — Prior 2026-06-11 (IF-21: **attendee Google + Apple social login** built on branch `feat/attendee-social-login` — Google live now (backend `/auth/google` already honors `user_type`); Apple now **enabled** (App Store Guideline 4.8 requires Sign in with Apple alongside Google) with backend `/auth/apple` `user_type` as a **hard pre-merge dependency** (kolabing-v2 PR #24); register screen redesigned so Google+Apple sit side-by-side and the whole page fits the viewport with no scroll; new social attendees route into the 4-step onboarding with name prefilled; `analyze` 0, 187 tests green. App PR #9 + backend PR #24 open. Spec/plan in `docs/superpowers/{specs,plans}/2026-06-11-attendee-social-login*.md`. — FX-25: attendee community-profile — event host-community tap now opens a real community-keyed screen (events + state-aware join CTA) instead of the mock business `PublicProfileScreen`; new `/community/:id/profile` route, `Community.isMember`/`joinPolicy`/`myJoinRequestStatus`, self-gated join-requests. FX-24: discovered-event detail (`EventDetailScreen`/`/event/:id`) gained RSVP + tappable host-community link. — NF-20 → IF-20: event location now uses the existing Google Places autocomplete; city auto-derived from the picked venue; manual city-picker removed. App built on `fixes-attendee`, `flutter analyze` 0 errors, NOT committed, pending e2e. Prior: `fixes-attendee` bug pass — see FX-10..FX-15: signup-500 Postgres-lock fixed in `kolabing-v2` [pending deploy]; community-paywall cleanup; gallery/past-events wired live; community-create photo-wipe fixed backend [pending deploy]; attendee sign-up card wired reachable; chat-visibility diagnosed [held for decision]. New attendee-track build order set: NF-13 social-hub → NF-9 languages → NF-17 friends → NF-14 DMs. #6 attendee-profile-broken NOT reproduced — left for Daniel to confirm. Prior 2026-06-09: NF-16 B4 → IF-18: real-time chat **Part B (Flutter client) BUILT** on branch `feat/realtime-chat-reverb-client` — `dart_pusher_channels` over self-hosted Reverb; `RealtimeChatService` + `RealtimeConfig` (self-gating, dormant until `REVERB_APP_KEY`) wired into `ChatThreadScreen` (live append + dedup + cancel-on-dispose; poll-on-open kept as fallback). Contract verified against backend `NewChatMessage` (`broadcastAs message.sent`, payload `{message: ChatMessageResource}`, channel `private-chat.thread.{id}`, root `/broadcasting/auth`). `flutter analyze` 0 errors, 166 tests green. **Part A (ops)** left as a ticket in `kolabing-v2/docs/tickets/2026-06-09-reverb-realtime-chat-PART-A-ops.md` — code already exists (event + channel auth + broadcasting route); only env + Reverb daemon + queue worker + nginx/TLS remain. Handoff: backend returns `REVERB_APP_KEY` → app flips live, no further code change. — NF-ANALYTICS → IF-17: PostHog product analytics **MERGED to master** (PR #8, merge `163a1af`) — curated (no autocapture/replay), identify-by-user-id with role props (no PII), 19 events (auth, paywall KPIs kolab_published/application_submitted, collaboration lifecycle, community/tier/join, events create/RSVP/check-in, messaging, subscription_started). `posthog_flutter ^5.26.0`, EU project 195047, key in `lib/config/constants/analytics.dart`; fail-safe service; ingestion verified live. Doc: `.agent/documentations/analytics-events.md`. — Prior 2026-06-05: NF-16 follow-ups built on `community-member-flow`: (1) i18n — mandatory-i18n rule added to CLAUDE.md + memory; full sweep localized all community/chat screen literals (207 keys, en/es/ca @ 1,516); new event UI born translated. (2) Event DELETE — backend `event_cancelled` fan-out to going/waitlisted on delete; app overflow→Delete with scope (this/following/series for recurring). (3) RECURRING EVENTS Phase 1 (hybrid "publish 3 months, extend later") — backend `event_series` (weekly/biweekly/monthly, multi-day byweekday e.g. Tue+Thu, rolling materialize + `extend`, scoped delete, chat_mode, end rules) wired into POST /events `recurrence`, `DELETE /events/{id}?scope=`, `POST /event-series/{id}/extend`; app create-form Repeat section (freq chips, locale-aware weekday multi-select, ends never/after-N/on-date, per-event vs shared-series chat) + 🔁 list badge + Event.seriesId. Backend 863 green (committed 4043404, NOT yet pushed/deployed); app 166 green (commits 57656b8, 1ffef63). DEFERRED sub-items (not built): edit→recurring (convert one-off on PUT), scoped EDIT, app "Extend series" button (endpoint exists), shared-series-chat *delivery* (chat_mode stored but createEventChat still per-occurrence), events→Kolab bridge (ticket: docs/tickets/2026-06-05-events-to-kolab-bridge.md). PENDING: push+deploy backend; e2e verify on sim. — Prior shipped + deployed: create-event blank fixed [OutlinedButton-in-Row vs full-width theme]; event list now refreshes instantly after create/edit/RSVP [`communityUpcomingEventsProvider` → Notifier+reload, same lesson as the tier-instant-refresh bug]; tier-gated events now LOCK for ineligible members [backend `EventResource.can_access` + app 🔒 tile, no open into details]; per-chat tier gating UI [backend `ChatThreadResource.slug` + app per-chat "Access" picker writing each tier's `permissions.chat_channels`]. App 166 + backend 856 green; backend `c75d660` pushed to master. NF-16 → IF-16: app B1–B3 built on `feat/nf16-events-attendee-app` — event management hub + create/edit form + notification settings; B4 real-time deferred, ops-gated. Prior: added NF-11 Chat Phase 3 [events RSVP lifecycle + event chats + Reverb real-time — backend ticket written], NF-12 chat/community IA restructure [chats in nav, community-detail tabs, profile→hidden], NF-13 attendee social-hub profile [Friends + uploadable cover], NF-14 member DMs [pending decision]. Prior: merged origin/master i18n es/ca; FX-9 stale-chat fixed + confirmed; NF-CHAT Phase 1+2 live; IF-7 community members+tiers app done.)))))

---

## 🆕 New Features
_Planned work that does not exist yet._

> **Community-attendee feature track — build order (Daniel, 2026-06-10):**
> **1. Social-hub profile (NF-13) → 2. Languages for attendees (NF-9) → 3. Friends
> (NF-17) → 4. Member-to-member DMs (NF-14).** Build in this sequence after the
> 2026-06-10 bug pass is verified. Friends (NF-17) is split out of NF-13's B-1
> sub-piece so it can be sequenced on its own; it gates NF-14 DMs.

| # | Feature | Notes | Status |
|---|---------|-------|--------|
| NF-1 | **Community member update** | Let communities add/edit/manage their member roster (the people inside a community). Scope, data model, and endpoints TBD. | Not started |
| NF-2 | **Map update** | Refresh/expand the map experience (discovery on a map, pins for businesses/communities/events). Scope TBD. | Not started |
| NF-3 | **Geolock check-in** | Location-gated check-in at a kolab/event — attendee must be physically at the venue (geofence) to check in / scan the QR. Ties into the existing `qr_code_url` + challenges flow. | Not started |
| NF-4 | **Profile photo scale/zoom on pick** | When choosing a profile picture, let the user pinch-to-zoom / pan to crop and frame the image before saving (interactive scale in/out). Applies to the image_picker flow used for avatars. | Not started |
| NF-5 | **Admin-managed gamification economy (backend)** | The whole reward economy is hardcoded in the app and can silently disagree with what the server awards. Two backend prompts written for `kolabing-v2`: (a) `docs/tickets/2026-06-01-admin-challenges-prompt.md` — challenge catalogue CRUD + audience + role-based defaults + per-collab business bonuses; (b) `docs/tickets/2026-06-01-admin-xp-economy-prompt.md` — XP levels, per-action XP awards (`xp_earn_rules` drives BOTH `point_ledger` and display), badge requirements, referral/withdrawal economics, all exposed via new `GET /gamification/config`. **App-side follow-up (after endpoint ships):** delete `xp_level.dart` + `xp_tier.dart` ladders, replace hardcoded `+10/+50` mission labels + badge requirements + `3`/`€75`/per-point literals with config values. | Prompts ready — backend not started || NF-7 | **Community Premium plan** | Monetisation for the community side (today only businesses pay). Free = **one** community per leader. Premium (paid) = create **multiple** communities + advanced community-management features (TBD: deeper tier customisation, analytics, broadcast, etc.). New subscription product + paywall on the community side, distinct from the Business €29/mo. Pairs with NF-6. | Not started |
| NF-8 | **Enterprise managed-partnerships plan (businesses)** | A paid enterprise tier where a business **hires Kolabing to run their partnership/referral collaborations for them** (managed service, Awin-style: brand outsources operating its referral/affiliate collaborations rather than self-serving). Distinct from the self-serve €29/mo business subscription. Scope, pricing, and ops model TBD. | Not started |
| NF-9 | **Languages for attendees** | Extend the app's localization (en / es / ca, from the i18n work) across the **attendee / Community Member** surfaces — home, Scan, profile, Communities, chats, gamification (XP/badges/leaderboard) — and add a **per-user language preference** so a Community Member can use the app in their own language (Greek-life / multi-region chapters mix languages). Ties into the existing `AppLocalizations` + locale handling. **[VERIFY with Daniel]:** which languages to include, and per-user preference vs device-locale only. | Not started |
| NF-10 | **SMS notification system** | Send transactional notifications over SMS (in addition to the existing push/OneSignal channel) — e.g. application accepted, kolab scheduled/reminder, check-in confirmation. Needs a backend SMS provider integration (Twilio/Vonage/etc.) + per-user phone capture & verification + notification-preference toggle (push vs SMS vs both). Mostly backend (new `kolabing-v2` endpoints + provider); app side = phone-number collection/verify UI + a channel toggle in notification settings. **[VERIFY with Daniel]:** provider, which events trigger SMS, opt-in vs default-on, and cost ceiling. | Not started |
| NF-11 | **Chat Phase 3 — Events RSVP lifecycle + event chats + real-time** | Give events a full lifecycle: **upcoming (one-tap "I'm going" sign-up, with capacity + auto-promoting waitlist, optional tier-gate) → live (existing QR check-in/XP) → past (existing gallery showcase, normalized to the same `events` rows)**. Sign-up gates the **event chat** (`POST /events/{id}/chat`, going-only). Real-time via **Laravel Reverb** (self-hosted, scale-cheap vs Pusher): broadcast `NewChatMessage` on private `chat.thread.{id}` channels w/ Sanctum channel-auth = participant rules. Reuse `collaborations.event_id` for kolab-linked events. Keep the existing **retroactive past-event** creation (add an event already hosted, showcase-only, no RSVP). **Backend ticket:** `docs/tickets/2026-06-04-chat-phase3-events-rsvp-realtime-backend.md`. App: event-signup UI, event-chat surfacing, Reverb/Echo client. Decisions locked (binary going; all-members + optional tier-gate; capacity+waitlist now). | Spec ready — backend not started |
| NF-12 | **Chat + community IA restructure** | Promote chat to a **bottom-nav tab** per role (attendee: Home·Communities·Chats·Scan; leader: Home·Explore·MyKolabs·Chats·Community; business: Home·Explore·MyKolabs·Chats·Profile). Community/attendee Chats tab is **organized per community** (chip/sub-tab each). Tapping a community → **tabbed Community detail**: Chats · Events · Members · **Details** (= the community profile: cover/photo, about, **gallery**, **past events**, your tier). **Drop the standalone Profile tab** for community/attendee — the user's profile becomes a **hidden/pushed screen** reached from the **top app-bar avatar** (any tab) **and** the Community area. Business keeps its Profile tab. ASCII/flows pitched 2026-06-04. | Designed — not started |
| NF-13 | **Attendee social-hub profile** (Friends + uploadable cover + redesign) | Redesign the (now hidden) attendee profile into a Squad-style social hub: photo-forward header (**uploadable cover image** + large real photo + edit pencil), 4 social stats (**Chats / Events / Friends / Groups**), "My Communities" cards w/ Join-Create, recent chats inline, and the **Edit Profile** screen (photo/cover/name/city/socials via existing `PUT /me/profile`). Two new backend pieces: **Friends/connections graph** (B-1) and **`cover_image` field + upload** (B-2). Also wires today's dead profile buttons (Notifications → existing screen; Help → URL) and renders the real `profilePhoto` (currently initials-only). Parked behind chats. | Designed — backend (Friends, cover) not started |
| NF-14 | **Member-to-member DMs** | ✅ IN SCOPE (Daniel 2026-06-04). The competitor's "Personal Chats / Create Chat +" are 1:1 DMs between members; Kolabing chat is community/kolab/event-scoped only. Adds a new **`direct`/`dm` `ChatThreadType`** + endpoints (start/find DM with a member, list DMs), likely **friend-gated** (pairs with NF-13 Friends graph). Surfaces in the Chats nav (a "Personal" / DMs section) + reuses the generic `/chats/{thread}/messages` + Reverb real-time. **Chats screen must get a SUB-TAB split (Daniel 2026-06-12): "Groups" vs "Direct" (DMs)** so 1:1 DMs are not mixed into / lost among the community/event group chats. Needs its own backend ticket. | In scope — backend ticket TBD |
| NF-15 | **Backend scale audit & query optimization** | Audit + fix the API for **hundreds–thousands** per community before scaling up. Several list endpoints issue **O(N) queries/page** today (correct, not yet optimized): `EventResource` runs ~3 count queries **per event** (`GET /events` ×50 → ~150 queries); `ChatService::visibleThreads` loops an unread COUNT **per thread** + `GET /chats` is unpaginated; the `GET /events` `COALESCE(ends_at,starts_at,event_date)` time filter isn't index-friendly; notification/Reverb fan-out must be chunked. Approach: instrument query counts (Telescope/`DB::listen`), seed at scale, load-test (k6) for p95 + query counts, then fix via `withCount`/eager/**batched grouped counts**, **cursor pagination + list caps**, **covering indexes** (EXPLAIN), and chunked fan-out. **Acceptance:** list endpoints query-bounded (no per-row queries), p95 within budget at 1000s, index usage on hot queries, unbounded lists paginated, load-test report committed. Surfaced during NF-CHAT Phase 3 (PR kolabing-v2#18). **Backend ticket:** `docs/tickets/2026-06-05-backend-scale-audit-optimization.md`. | Spec ready — not started |
| NF-17 | **Friends / connections graph** (split from NF-13 B-1) | A member-to-member friend graph: send / accept / decline / remove friend requests; a "Friends" list surfaced on the social-hub profile (NF-13's Friends stat + list). New backend pieces: a `friendships`/`connections` table (pair + status: pending/accepted/blocked), endpoints (`POST /friends/{profile}` request, accept/decline, `DELETE`, `GET /me/friends`, pending count), and a friend-status field on profile responses. **Gates NF-14 DMs** (DMs are friend-gated). Build order: **#3 in the attendee track** (after NF-13 social-hub profile and NF-9 languages, before NF-14 DMs). Needs its own backend ticket. | Designed — backend not started (build #3 in attendee track) |
| NF-18 | **Replace Edit Profile with inline editing + Discover-events-by-city** (Daniel 2026-06-10, **PITCH PENDING**) | Drop the standalone Edit Profile screen. Instead: (a) change the **profile picture by tapping the avatar directly** on the profile; (b) make **name + @handle editable inline, just below the avatar**; (c) move **city** out of a profile field into a **Discover (events) section** where an attendee browses **events by city** — so when they travel / are somewhere in the city, they pick a location and see what's on. City becomes a browse context, changed there. Pairs with the events-discovery feed (cf. NF-11 events lifecycle, `/communities/discover` pattern). **Pitch the flow to Daniel before building.** | Pitch pending |
| NF-19 | **Attendee events discovery (real, not placeholder)** (Daniel 2026-06-10, **PITCH PENDING**) | The home "NEARBY EVENTS" feed is a placeholder (always "No Events Nearby"). **Initial version:** show **all events in the attendee's city** (not just geofenced radius), with **filter chips: (1) Today, (2) event type**. Keep a radius/city control (ties into NF-18 city-as-browse-context). Needs a backend public-events-by-city endpoint (cf. `/events/discover` already used at home — verify it filters by city + type/date) + the app feed/chips. Also surfaces the FX-21/FX-23 items (readable controls + an always-on "Explore communities" CTA). **Pitch before building.** | Mostly built 2026-06-11: city discover + public/future + visibility + date dropdown + event `city_id`. **Remaining:** NF-20 below. |
| NF-22 | **Saved Kolabs (Recommended \| All \| Saved)** (user report 2026-06-27) | A "Save for later" button exists but is a **dead stub** (`public_profile_screen.dart:1188` `_SendKolabBottomBar` → only `Navigator.maybePop()`), and there is **no place to view saved kolabs**. Add a **Saved** tab to the Explore feed segmented control (today only Recommended \| All — `explore_screen.dart:630-667` `_FeedToggle`, `DiscoveryFeed{recommended,all}`) and make saving real. **Backend has zero support** (no save/unsave/list endpoint, no pivot, no `is_saved`) → **blocked on kolabing-v2#61**. App ticket **#43**, plan `docs/plans/2026-06-27-saved-kolabs.md`. **APP IMPLEMENTED 2026-06-27** on `feat/saved-kolabs` against backend PR **kolabing-v2#62** (`POST/DELETE /kolabs/{id}/save`, `GET /kolabs?saved=1`, `is_saved`): `Opportunity.isSaved`, `OpportunityService.getOpportunities(savedOnly:)` + `setSaved()`, `savedKolabsProvider` + `savedKolabIdsProvider` (optimistic toggle), Explore **Saved** tab (`_SavedKolabsTab` from `/kolabs?saved=1`) + bookmark on the swipe deck; i18n en/es/ca. `flutter analyze` 0 new, tests +253/−16 (no new fails). Decision taken: save **kolabs** (not community profiles). | Implemented — pending review/merge + device e2e |
| NF-23 | **Pending challenge verifications + push** (v2 of IF-27) | IF-27 ships QR-only verification: the verifier confirms by scanning the challenger's screen. If they walk off first, the completion stays `pending` with no way to resolve it. Add a **pending-verifications** surface (`GET /me/challenge-completions` filtered to `verifier_profile_id = me`, verify/reject already exist in `ChallengeService`) and a push notification when a challenge is initiated (needs a new notification type in `kolabing-v2`). | Not started |
| NF-21 | **Attendee gamification surfacing** (Daniel 2026-06-11; narrowed 2026-08-22) | The **earn loop shipped** in IF-27 (#132): check-in, peer pairing, challenge, verification and XP now work end to end. What remains is **surfacing**: `BadgesScreen`, `RewardWalletScreen`, `SpinWheelScreen`, `StatsScreen` and `LeaderboardScreen` are still referenced only by their own definitions — no nav reaches any of them (the old note that the leaderboard was reachable from community detail is stale; there is no such reference left). Decide per screen: give it a home in a Rewards/Missions hub, or delete it. Also: the attendee profile has no Missions entry (`/missions` is reachable only from the business and community-leader profiles), and badge/level progress is not shown anywhere for attendees. Backend is READY. | Not started — earn loop done (IF-27), surfacing open |

---

## 🚧 Incomplete Features
_Started or partially shipped; not yet fully working end-to-end._

| # | Feature | What's done / what's missing | Status |
|---|---------|------------------------------|--------|
| IF-38 | **One community page: seeing it and running it are the same page** | Issue **#174**, approved by Volkan 2026-08-26 ("community sayfası çok kullanışsız"). A leader had the community split across `community_detail_screen.dart` (1429 lines — the Luma scroll from IF-31/IF-33, with management scattered through it behind **20 separate `canManage` gates**) and `community_profile_screen.dart` (1241 lines — About / details / contact / notifications / account, and **no management at all**). So you edited the community in one place and ran it in another, and neither page was a complete answer. **Now one page**, in the order a leader needs: full-bleed `CommunityCoverHero` (IF-33's real cover photos) → identity (`type · N members`) → an action row → **MANAGE** → the profile sections. Manage sits *third*, before the descriptive blocks: a leader does not open this page to read their own About text. The 20 scattered gates collapse into **one block that is present or absent** — a management row leaking to a member is a permission bug, so it is now one decision rather than twenty. New `community_manage_sections.dart`: `CommunityIdentity`, `CommunityActionRow`, `CommunityManageSection`. Manage routes into the surfaces that already worked — `RosterScreen`, `TierEditorScreen`, the rewards hub, and `CommunityDetailScreen`, which **stops being the tab but survives as the events sub-screen** (verified still reached by `my_communities_screen` and the attendee page, so deleting it would have broken both). **Spacing was an explicit ask** ("spacing buton boşluklarını düzgün ayarla"): one named scale at the top of the file (`_sectionGap`, `_sidePad`), the action buttons fixed-height so a longer es/ca label cannot make them different sizes, and dividers inset so four rows read as one list. **No dead controls:** an "Edit community" button was designed and then dropped — `CommunityService.updateCommunity()` exists but nothing in the app calls it, and a control that goes nowhere is the same trap the dead "Save for later" stub fell into; the row carries the Invite link alone — **"New event" came out** at Volkan's request ("new event i kaldir"), since creating an event is not why a leader opens this page and Manage → Events is one tap away. **Photos are editable from the page**: tapping the logo opens the profile-photo sheet, tapping the cover band opens a cover sheet; `cover_photo` is its own column (kolabing-v2#239) rather than the profile photo reused, and `ProfileService.updateProfilePhotoFile` takes a `fieldName` so one upload path serves both. **Manage now opens the surfaces themselves, not the old page**: Rewards pointed at `/rewards` = `PersonalRewardsScreen`, the *viewer's own* points wallet — wrong audience for a leader — and Events pushed the whole `CommunityDetailScreen`. The leader rewards admin (`_RewardsLeaderSection` + rows, 299 lines) and the events surface (`_EventsSection`, ~100 lines) were **private to that screen**, which is exactly why the rows had to point at it; both are extracted as `CommunityRewardsAdminPanel` / `CommunityEventsPanel` and hosted by new `CommunityRewardsAdminScreen` / `CommunityEventsScreen`, with `CommunityDetailScreen` rendering the same widgets (one implementation, two entry points; it shrank 1429 → 1027 lines). Four defects fixed in the extraction: a delete dialog confirming with **"Remove"** (`rosterRemove`), every goal showing an invented **"0 / target"** progress, every badge subtitled with the bare word **"Value"**, and a raw `e.toString()` shown to the user on a failed list. A **PAUSED** pill now marks an inactive goal/reward/badge, which previously looked identical to a live one. 7 more l10n keys in en/es/ca. 9 new l10n keys in en/es/ca. `flutter analyze lib test` 0 errors; 4 new tests. Verified on the simulator as a leader — hero, identity, equal-half action row and all four Manage rows render. **Not verified on screen:** the sections below the fold (About / details / gallery / past events / contact / notifications / account) — the simulator drag harness will not scroll, so they are argued from source and the unchanged builders. **Business is untouched**, per Volkan. | **Built — needs device QA (a leader with members/tiers, and a member to confirm Manage is absent), plus the below-fold scroll check** |
| IF-37 | **Multi-Kolab mobile — revived onto master, audited against the plan** | Volkan handed over a consolidated Multi-Kolab reimplementation plan (brief → roles → role applications → accepted canonical collaborations) asking for a mobile build. **The audit changed the job.** (a) **The backend is already live:** `kolabing-v2#141` merged 2026-08-19; `routes/api.php` exposes the whole surface, and the frozen contract is at `kolabing-v2:docs/superpowers/specs/2026-08-12-multi-kolab-event-api-contract.md`. Note there are **no** `roles/{role}/open|close` routes — role status moves via `PATCH multi-kolab-roles/{role}`, and the app already does exactly that. (b) **The mobile side was not greenfield:** `origin/feat/multi-kolab-event-mvp` (18 commits, tip `e6894f1`) already held **~19,900 lines across 102 files** — 11 models, 3 repositories (interface/API/mock), 4 provider files, **7 screens**, 6 widgets, the My Kolabs integration, and ~5,000 lines of tests with small-phone and Catalan goldens. Its own history already implements what the new plan lists as "later UX decisions": event-level *Who can apply?* removed (`86bf18f`), Partner not Sponsor (`e366961`), partner spots counted separately from role rows (`d64e9b8`), the standalone Multi-Kolab explore entry deleted (`92ae8f5`, `f19d54e`), small-phone readability (`d280fe5`), folded into ordinary My Kolabs (`0871bf5`). **So the work was reviving it, not rebuilding it** — its merge-base with master was `cf10f49`, eleven feature PRs and four large redesigns ago. Merged onto current master with 8 conflicts: 7 l10n (union of ~1,000 new keys) and one real one in `explore_swipe_card.dart`, where master had moved the chip colouring into `CategoryChip` while the branch still carried an inline copy — took master's structure plus the branch's empty-category separator guard. **Result: 0 analyze errors, 716 tests pass / 10 pre-existing failures (same four names), zero new failures — the whole MK suite including goldens is green on today's master.** Audited against the plan's §24: API layer calls only routes that exist; EN/ES/CA parity exact at **195 `multiKolab*` keys each**; roles vs partner spots kept distinct (`MultiKolabSpotCounts.openRoles`); "Sponsor" survives only in `multiKolabCompensationSponsoredInKind`, which the plan explicitly permits; no separate MK explore feed, chat or review surface; and every screen is reachable — the My Kolabs "+" opens `showCreateKolabChoiceSheet`, MK events render as `MyMultiKolabCard` in the ordinary lists, and the applicant path runs through `ExploreCardData.fromMultiKolabRole` in the shared Explore card. **One open item:** event-level `eligibleAccountType` still exists on `MultiKolabEvent` / `MultiKolabEventSummary` and its draft input. It is provably inert — nothing sets it, nothing renders it, every setter and renderer is `role.eligibleAccountType` — so it is documented as deliberately inert rather than deleted, because removal touches 15 test call-sites including an assertion. The plan permits "remove **or** explicitly ignore"; deleting it is a clean follow-up. | **Revived and green on master — needs device QA against the live backend (organizer entitlement required) and screenshots, then merge** |
| IF-36 | **Events feed'i Explore ile aynı kartı çiziyor** | Yeni `lib/features/event/widgets/event_feed_card.dart` — `ExploreSwipeCard`'ın anatomisi (16:10 foto alanı, çok fotoğraflu carousel + nokta göstergeleri, içerik bloğu: byline → başlık → meta → çipler → ayraç → "View Details ›") `Event` üzerine kurulmuş. `EventTimeline` `variant` aldı: `row` varsayılan (topluluk sayfaları aynen kaldı), `card` opt-in. **Hiyerarşi Explore'dan bilerek farklı:** Explore başlığa *kimi* koyar çünkü orada karşı tarafı değerlendiriyorsun; burada event'in adı başlık, host onun üstünde byline — timeline satırının zaten kullandığı sıra. **Yapılmadı:** *Your events* özeti ve `my_events_screen` satır olarak kaldı (ikisi de kişisel program, taranabilirlik kartın önünde); event kartında Explore'un Fit rozetinin karşılığı yok — kapasite rozeti çipler satırında duruyor. | **Built — cihaz QA'si gerekiyor (fotoğrafı olan, olmayan ve kilitli birer event; ayrıca bir topluluk sayfasının hala satır çizdiği)** |
| IF-35 | **Every business and community in the app links to its profile** | `ProfileLink` (`lib/widgets/profile_link.dart`) + `ProfileLink.open` as the single destination rule, replacing three hand-rolled copies of `context.push('/profile/…')` and the viewer-scoped branch that lived in `event_detail_screen._openHost`. **Done:** the Kolab detail sheet header (avatar + name + type badge), the saved-Kolab sheet, the profile reviews list, Discover communities, the chat header. **Paywall-aware:** the tap target does not exist when the identity is blurred (§2.5) or the viewer is inside the §2.8 re-gate — `enabled: false` renders the child untouched, no ripple. **Not linked on purpose:** compact cards that already open a screen which links the identity. **Missing:** `past_collaboration_card` (`PastCollaboration` carries `partnerName` and no partner id — needs a backend field), `kolab_completion_sheet` (only `partnerName` is plumbed in), and the event timeline's host line (nested gesture on a 16px row). | **Built — needs device QA as all three roles, including a FREE business on a community Kolab (the header must not respond)** |
| IF-34 | **The attendee feed, rebuilt on Luma's lines** | Design in `docs/superpowers/specs/2026-08-24-attendee-feed-luma-redesign-design.md` (approved by Volkan, 2026-08-24). Issue **#161**. `AttendeeHomeScreen` was 1175 lines that opened with data nobody needed: three `StatCard`s showing the **same** Points/Events numbers `AttendeeProfileScreen` already renders around the avatar, a "Welcome back" block, and an event card carrying three fields that were noise or wrong — a **distance badge that read "0 m" on every row** (city discovery sends no `lat`/`lng`, so `distance_km` came back `0`), a partner-type badge reading "Community" on all of them, and the legacy *showcase* `attendee_count` printed on a future event. Meanwhile no row showed the **start time** or the **venue**, and the page knew nothing about what the attendee had signed up for. **Now three sections, and almost all of it was data that already existed.** *Your events*: `GET /events?attendee=me&time=upcoming` — the endpoint the scanner's event picker had been calling all along — up to 3 rows with "Today, 19:30", the venue and a Going/Waitlist chip, "View all" → new `MyEventsScreen` (`/my-events`). *Communities you follow*: logo tiles from `GET /me/community-follows`, whose payload **has always nested a whole `community` object** that the app read the id out of and discarded — zero backend work, and the follow finally has a second consequence. *What's on*: the same city discovery, date-grouped. **The structural move:** the feed speaks `Event` (rich) instead of `DiscoveredEvent` (thin), so IF-31's Luma row moved to shared `event_timeline.dart` (`EventTimeline`/`EventTimelineRow` + `showDay` / `showHost` / `showVisibility`) and now serves the feed, both community pages and My events. `Event.fromJson` needs only `id/name/date/created_at`, all of which `/events/discover` already sends, so it parses today and gets richer as the backend ships. Day headers gained **Today/Tomorrow** everywhere. **Deleted:** `EventDiscoveryScreen` (521 lines of legacy GPS feed, reachable from nothing but a barrel export), `DiscoveredEventCard`, `DiscoveredEvent`, and 7 orphaned l10n keys from all three ARBs. 9 new keys in en/es/ca. `flutter analyze` 0 errors, 0 new warnings; suite **471 pass / 10 pre-existing failures** (baseline-verified identical); 8 new tests, two of which assert "0 m" and the "Community" badge can never come back. **Backend half:** `GET /events/discover` still returns a bespoke thin payload — **kolabing-v2#223** gives it `EventResource` parity and stops it sending `distance_km: 0` on a city query (`docs/tickets/2026-08-24-events-discover-resource-parity-backend.md`). Not blocking; the app self-gates and ships first. | **Built — needs device QA (an attendee with sign-ups + follows, and one with neither), then a PR** |
| IF-33 | **The community page, second pass — it has photographs now** | Design in `docs/superpowers/specs/2026-08-24-community-page-v2-design.md` (approved by Volkan, 2026-08-24). IF-31 gave the page Luma's shape; this gives it substance, and almost all of it was substance the backend already sent. **Photos:** a community had none, and rather than add a table, `communityPhotosProvider` reads the owner's **curated profile gallery** (`GET /profiles/{id}/gallery` — uploadable, reorderable and public since NF-2) and falls back to the community's **own event photos**, newest first, so an active community is never photoless. The cover band paints the first of them instead of a blurred copy of the logo, and a `CommunityPhotoStrip` opens the existing `PhotoViewerDialog`. **Socials:** the `SizedBox.shrink()` seam is wired to the owner's `instagram / tiktok / website`, and the meta line gained the city — all from `publicProfileProvider`. **Points and tier** now come from `my_points` / `my_tier` in the community payload itself; the standing row was making a second call to the rewards hub for something it had already been sent (`my_can_manage` parsed too). **Chips filter:** Upcoming / Past / Public / Members with real counts, and `Past` finally calls `communityPastEventsProvider`, which had existed since NF-6 with no caller — a community between events stops looking abandoned. **Event rows** gained a capacity badge (`Full`, `Near capacity`, `N left`; nothing when uncapped). Both community pages use all of it. `SocialLinksRow` and `HeroCircleAction` are now shared with the event page. 7 new l10n keys (and `eventPageWebsite` retired for `commonWebsite`). `flutter analyze` clean; suite **471 pass / 10 pre-existing failures**; 7 new tests. **Known cost:** the curated gallery belongs to the leader's *profile*, not the community — a leader running two would show the same photos on both; a real `community_photos` table is the fix and is deliberately not in this pass. | **Built — needs device QA (a community with a gallery, and one with only event photos)** |
| IF-32 | **One event page, and a ticket to show at the door** | Design in `docs/superpowers/specs/2026-08-24-event-page-design.md` (approved by Volkan, 2026-08-24). The app had **two** event screens: `EventDetailScreen` (from the feed) showed three facts and a green slab whose only function was to *undo* the RSVP, and `EventHubScreen` (from the community timeline) held check-in, the door QR, the roster and photo upload — the drift that produced "I can't find the event QR code". `EventHubScreen` is **deleted**; its two call sites push `EventDetailScreen.forEvent(...)`, which is now role-aware. New `event_page_sections.dart` (hero carousel, title block, host card, details, section label, nav row, sticky `EventActionBar`) plus `hero_circle_action.dart` shared with the community pages. **Three asks, three answers.** *My QR code*: the backend's per-attendee ticket (`event_signups.ticket_code`, `GET /me/tickets`, inline `qr_svg`) was fully built and the app had none of it — new `EventTicket` model + `TicketService` + `myTicketForEventProvider` + `MyTicketSheet` (big server-rendered QR, code, holder, `Admitted 18:04`). The app never draws its own QR: a locally generated code could disagree with the door. *Description*: `events` has **no description column**, so nothing was invented — the page instead surfaces what the API already sent and the model silently dropped (`ends_at`, `address` distinct from the venue `location`, `city_name`, `tier_gate`, `community_name`), and uses the community's about text as event context. *Socials*: the host's `community_profiles` instagram / tiktok / website via `publicProfileProvider`, self-gated per handle. 16 new l10n keys in en/es/ca. `flutter analyze` clean; suite **464 pass / 10 pre-existing failures** (same names). 14 new tests in `test/features/event/widgets/event_page_sections_test.dart`. | **Built — needs device QA (attendee + host, and a real ticket), then a PR** |
| IF-31 | **Community pages rebuilt on one Luma-style scroll** | Product direction: Volkan, 2026-08-24, with Luma's community page as the reference — its **layout**, Kolabing's palette (cream page, white surfaces, ink text, yellow accents; no dark mode). New shared file `lib/features/community/widgets/community_page_sections.dart`: `CommunityCoverHero` (cover band + overlapping 96px logo tile + translucent circle actions), `CommunityIdentityBlock` (name, tap-to-expand about, meta line), `CommunityNavRow`, `CommunityTagChip`, `CommunitySectionLabel`, `CommunityEventTimeline` (events grouped under a localised `6 September / Sunday` header, with time, location, Going/Waitlisted and visibility chips) and `CommunityEventVisibilityChip`. **Both** community pages now use it: `CommunityDetailScreen` lost its `TabBar` entirely (Rewards/Members/Events → one scroll; the tier-grouped roster moved behind a manager-only nav row and `initialTabIndex` is gone from the API and its two callers), and `AttendeeCommunityProfileScreen` lost the yellow gradient header, the About card and the horizontal event carousel. **No invented data:** `Community` has no cover-photo, city or social fields, so the hero blurs the avatar (brand gradient when there is none) and `_SocialLinks` stays the same do-nothing seam. Two new l10n keys (`communityEventBadgeGoing`, `communityEventBadgeWaitlisted`) in en/es/ca. `flutter analyze` clean (no new error or warning); suite 450 pass / 10 pre-existing failures (same names on a stashed tree). | **Built, uncommitted — needs device QA on both pages, then a branch + PR** |
| IF-30 | **A peer challenge pays both people** (#140) | Review in `docs/reviews/2026-08-23-challenge-flow-social-review.md`. App PR **#141**, backend **kolabing-v2#213**. Three scans became one: the verification QR is retired (`ChallengeVerifyQrScreen` + `VerifyCompletionSheet` deleted) and the partner's phone learns about the challenge itself via `pendingChallengeProvider` (4s poll, only while an active event session exists). `ChallengeTogetherScreen` is the one screen both devices show — same challenge, same "15 XP each" read from the server's `points_earned`, same reveal. Backend credits challenger AND verifier, with badges and `MissionTrigger::ChallengeCompleted` for both. **Missing:** both PRs unmerged; needs a **two-phone recording** (the whole point is what the two devices do at the same moment) and real devices (simulators have no camera). Leaderboard / room layer deliberately deferred (§4.5). | Built both sides — PRs open, device QA outstanding |
| IF-29 | **Attendees see followed communities' events** (#142) | App PR **#143** (base: #139), backend **kolabing-v2#214**. **All events / Following** on the attendee feed. `DiscoveryState.following`; `canQuery` is true on Following alone, so no city is needed; the city and type chips hide in that scope and the city stays in state. `GET /events/discover?following=1` resolves the viewer from the token, matches nothing when there is no viewer (never everything), and strips a falsy `following` so `?following=0` cannot list every public event. 7 i18n keys en/es/ca, 6 app tests, 10 backend tests. **Also fixed there:** the `GET /events?following=me` member-event leak (see the header). **Missing:** both PRs unmerged, #139 must land first, screenshots + device QA. | Built both sides — PRs open, device QA outstanding |
| IF-28 | **Community follower / member split** (#138) | Design + plan in `docs/plans/2026-08-22-community-follower-member-split-{design,backend}.md`. **✅ BACKEND SHIPPED** (kolabing-v2 PR #211, branch `feat/community-follower-split`): 3 additive tables (`community_followers`, `community_join_questions`, `community_join_answers`), `POST|DELETE /communities/{id}/follow`, `GET /me/community-follows`, join-questions CRUD (max 5 active, retired not deleted, prompt snapshotted onto the answer), and `join-requests` now carries `answers`. Followers deliberately live in their own table so no member-gated query can match them; `CommunityMemberAccessRegressionTest` locks both directions. Required answers are enforced only when the client sends an `answers` key, so shipped app builds keep working. Full suite 2360 green. **✅ APP BUILT** (PR #139, branch `feat/community-follower-member-split`): every **Join** became **Follow** (one tap, optimistic with rollback), community profiles gained **Become a member** which asks the leader's questions when there are any and joins directly when there are none, and `communityFollowsProvider` holds the follow set so no list needs a per-row check. **Still missing:** the leader's application queue with the answers on it, and device QA. **TWO OPEN DECISIONS:** (a) a follower can still sign up to a `members`-visibility event — `EventSignupService::signup` only checks visibility for events with no community, so the gate is client-side only today (pre-existing, not a regression); (b) for `open` communities — the default — `POST /communities/{id}/join` bypasses the questions, so the gate is cosmetic there unless `/join` starts refusing when active questions exist (a contract change with an app dependency). | **App built** (PR #139) — decision (b) resolved (`/join` now refuses when active questions exist), (a) half-closed by kolabing-v2#214; device QA + screenshots outstanding |

| IF-30 | **A peer challenge pays both people** (#140) | Review in `docs/reviews/2026-08-23-challenge-flow-social-review.md`. App PR **#141**, backend **kolabing-v2#213**. Three scans became one: the verification QR is retired (`ChallengeVerifyQrScreen` + `VerifyCompletionSheet` deleted) and the partner's phone learns about the challenge itself via `pendingChallengeProvider` (4s poll, only while an active event session exists). `ChallengeTogetherScreen` is the one screen both devices show — same challenge, same "15 XP each" read from the server's `points_earned`, same reveal. Backend credits challenger AND verifier, with badges and `MissionTrigger::ChallengeCompleted` for both. **Missing:** both PRs unmerged; needs a **two-phone recording** (the whole point is what the two devices do at the same moment) and real devices (simulators have no camera). Leaderboard / room layer deliberately deferred (§4.5). | Built both sides — PRs open, device QA outstanding |
| IF-29 | **Attendees see followed communities' events** (#142) | App PR **#143** (base: #139), backend **kolabing-v2#214**. **All events / Following** on the attendee feed. `DiscoveryState.following`; `canQuery` is true on Following alone, so no city is needed; the city and type chips hide in that scope and the city stays in state. `GET /events/discover?following=1` resolves the viewer from the token, matches nothing when there is no viewer (never everything), and strips a falsy `following` so `?following=0` cannot list every public event. 7 i18n keys en/es/ca, 6 app tests, 10 backend tests. **Also fixed there:** the `GET /events?following=me` member-event leak (see the header). **Missing:** both PRs unmerged, #139 must land first, screenshots + device QA. | Built both sides — PRs open, device QA outstanding |
| IF-28 | **Community follower / member split** (#138) | Design + plan in `docs/plans/2026-08-22-community-follower-member-split-{design,backend}.md`. **✅ BACKEND SHIPPED** (kolabing-v2 PR #211, branch `feat/community-follower-split`): 3 additive tables (`community_followers`, `community_join_questions`, `community_join_answers`), `POST|DELETE /communities/{id}/follow`, `GET /me/community-follows`, join-questions CRUD (max 5 active, retired not deleted, prompt snapshotted onto the answer), and `join-requests` now carries `answers`. Followers deliberately live in their own table so no member-gated query can match them; `CommunityMemberAccessRegressionTest` locks both directions. Required answers are enforced only when the client sends an `answers` key, so shipped app builds keep working. Full suite 2360 green. **✅ APP BUILT** (PR #139, branch `feat/community-follower-member-split`): every **Join** became **Follow** (one tap, optimistic with rollback), community profiles gained **Become a member** which asks the leader's questions when there are any and joins directly when there are none, and `communityFollowsProvider` holds the follow set so no list needs a per-row check. **Still missing:** the leader's application queue with the answers on it, and device QA. **TWO OPEN DECISIONS:** (a) a follower can still sign up to a `members`-visibility event — `EventSignupService::signup` only checks visibility for events with no community, so the gate is client-side only today (pre-existing, not a regression); (b) for `open` communities — the default — `POST /communities/{id}/join` bypasses the questions, so the gate is cosmetic there unless `/join` starts refusing when active questions exist (a contract change with an app dependency). | **App built** (PR #139) — decision (b) resolved (`/join` now refuses when active questions exist), (a) half-closed by kolabing-v2#214; device QA + screenshots outstanding |
| IF-28 | **Community follower / member split** (#138) | Design + plan in `docs/plans/2026-08-22-community-follower-member-split-{design,backend}.md`. **✅ BACKEND SHIPPED** (kolabing-v2 PR #211, branch `feat/community-follower-split`): 3 additive tables (`community_followers`, `community_join_questions`, `community_join_answers`), `POST|DELETE /communities/{id}/follow`, `GET /me/community-follows`, join-questions CRUD (max 5 active, retired not deleted, prompt snapshotted onto the answer), and `join-requests` now carries `answers`. Followers deliberately live in their own table so no member-gated query can match them; `CommunityMemberAccessRegressionTest` locks both directions. Required answers are enforced only when the client sends an `answers` key, so shipped app builds keep working. Full suite 2360 green. **MISSING (app):** Follow button, Communities list showing **Following** vs **Member** (today every row reads "Member" — the original report), the application form, and the leader's queue with answers. **TWO OPEN DECISIONS:** (a) a follower can still sign up to a `members`-visibility event — `EventSignupService::signup` only checks visibility for events with no community, so the gate is client-side only today (pre-existing, not a regression); (b) for `open` communities — the default — `POST /communities/{id}/join` bypasses the questions, so the gate is cosmetic there unless `/join` starts refusing when active questions exist (a contract change with an app dependency). | Backend done — app not started, 2 decisions open |
| IF-27 | **Event-challenge QR loop (check-in → peer scan → challenge → XP)** | Issue #132, branch `feat/event-challenge-qr-gamification`, design `docs/plans/2026-08-22-event-challenge-qr-gamification-design.md`, audit `docs/reviews/2026-08-22-attendee-qr-review.md`. **✅ BUILT (app only, zero backend work):** `QrPayload.parse` classifies the three code kinds by **path, not host** (`Environment.shareHost` differs prod/dev), replacing the old `length >= 10` guard that POSTed any scanned string to `/checkin`; `ActiveEventSession` (persisted, 12h TTL from the device clock, cleared on account switch) supplies the event context the backend cannot — there is no `GET /me/checkins` and events carry no `ends_at`; `AttendeeScannerScreen` routes check-in / pairing / verification and re-arms the camera between scans; `PeerChallengeSheet` lists that event's challenges and initiates with the scanned member as verifier; `ChallengeVerifyQrScreen` shows the verification QR, polls `GET /me/challenge-completions` and reveals the XP **the server** awarded; `VerifyCompletionSheet` names the challenge and challenger before asking the verifier to confirm. Entry points: QR hub on the attendee app bar (Scan / My QR), organizer "Show check-in QR" and member "Check in" on the event hub. `CheckinFailure` / `ChallengeFailure` kinds replace string-matching on backend English, so every error path is localized (en/es/ca). 51 new tests; `flutter analyze` 0 errors. **MISSING:** device QA on iOS + Android with two real accounts; **[VERIFY]** that `ChallengeCompletionService::verify()` credits the *challenger* in `point_ledger`; a stranded `pending` completion cannot be resolved if the verifier walks off before scanning (v2: pending-verifications list + push, see NF-23). | App built — needs device QA |
| IF-26 | **UGC moderation — report / block / EULA** (App Review Guideline 1.2, #99) | Branch `feat/ugc-moderation`. **✅ BUILT (app side):** new `lib/features/moderation/` — `ModerationService` (`report`, `block`, `unblock`, `blockedProfileIds`; built from `ApiConfig.baseUrl`, Bearer, **self-gates on HTTP 404** so it's safe before the backend deploys), `blockedProfilesProvider` (`Notifier<Set<String>>`, optimistic block/unblock + revert, loaded after sign-in, invalidated on session reset), `ReportSheet` (reason chips spam/harassment/inappropriate/other + optional note → success/error snackbar), `ModerationMenu` helper (Report / Block-with-confirm items). **Wired:** public profile overflow (Report/Block, only when viewing someone else), chat-thread app-bar overflow (Report/Block the 1:1 counterpart), Explore detail sheet (Report this Kolab), review row (Report review). **Client filtering (instant hide, watch `blockedProfilesProvider`):** Explore deck drops blocked creators (`filterExploreDeckItems`), reviews list drops blocked authors, chats list hides blocked 1:1 threads. **EULA:** ARB `authNoToleranceNotice` rendered above the `TermsConsentCheckbox` on all 3 sign-up screens. i18n en/es/ca (23 keys) via `flutter gen-l10n`; `flutter analyze` 0 new errors/warnings; 9 moderation tests green (provider optimistic add/remove+revert, ReportSheet submit, Explore deck excludes blocked creator). **MISSING:** backend endpoints (`GET/POST/DELETE /me/blocks`, `POST /reports`) — kolabing-v2 #115, merge first; App-Review device screen recording of EULA + report + block. | App built — pending backend deploy + App-Review recording |
| IF-25 | **Terms + Privacy consent flow** (#65) | Branch `feat/consent-flow`. **✅ BUILT:** `TermsConsentCheckbox` (tappable Terms/Privacy links, locale-aware URLs via `LegalLinks`) on the 3 sign-up screens, gating the primary button + attendee Google/Apple; email register sends `accepted_terms:true`; `UserTerms` parsed from `/auth/me` (`needs_acceptance`); full-screen blocking `ReconsentGate` overlay in `_AuthSessionRedirector` → `POST /me/consent` → refresh → auto-dismiss; `WidgetsBindingObserver` refreshes on resume. i18n en/es/ca; `flutter analyze` 0 new; 6 consent unit tests green. Backend endpoints already live. **MISSING:** device screenshots (UI change) + on-device e2e (sign-up gating, link open, re-consent block/accept, resume). | App built — pending screenshots + e2e |
| IF-24 | **Business Partner Status, next-best-action, monthly goal on dashboard** (audit #14/#15/#16) | Branch `feat/business-partner-status-dashboard-ui` (PR #70). `GET /me/dashboard` returns `partner_status` (New/Active/Trusted Partner, Community Favourite — from real collaboration outcomes, not points), `next_action` (server-provided title/body), `monthly_goal` (rolling calendar-month count, not a streak). `BusinessDashboard` parses all three; new `PartnerStatusBadge` (quiet header pill), `NextActionCard` (renders when non-null; tap wired for `create_first_offer`/`create_second_offer` only), `MonthlyGoalCard` (progress bar). i18n en/es/ca; analyze 0 new; 4 model tests green. Backend deps merged (kolabing-v2 #86/#90). The remaining `next_action` routes (`review_pending_applications`→Requests, `leave_review`→My Kolabs, `complete_profile`→Profile) were wired in **#92** (FX-39). **REMAINING:** screenshots + device e2e. | ✅ Merged to master 2026-07-28 (PR #70) — pending screenshots |
| IF-23 | **My Kolabs — Bolder filter/action redesign** | Branch `feat/my-kolabs-bolder-redesign`. **BUILT:** new `KolabingSegmentedControl` (primary OFFERS/REQUEST/ACTIVE/FINISHED full-width ink-fill row in `MyKolabsHubScreen`; secondary yellow-fill row reused for both PUBLISHED/DRAFT (`my_kollabs_screen.dart`) and SENT/RECEIVED (`applications_screen.dart`) via `MyKolabsSubTabs`) and `KolabingCardActionBar`/`KolabingIconPillButton` (stadium icon pills replacing the old floating circular `GlassIconButton` row on `MyKolabCard`). Presentation-only — labels/counts/callbacks stay bound to existing state. `flutter analyze` 0 new issues; `my_kollabs_screen_test.dart` + `my_kolabs_provider_test.dart` green. **MISSING:** on-device visual QA (primary control confirmed via iOS simulator screenshot mid-build; the Requests-tab secondary-control fix and Android not yet visually confirmed — blocked on manual Google OAuth login in the simulator). | App built — pending visual QA |
| IF-1 | **My Kolabs — Active & Finished tabs** | DONE: wired to `GET /collaborations` (Active = scheduled/active/pending_confirmation; Finished = completed/cancelled), real cards, pull-to-refresh. MISSING: visual QA on device; no "cancel collaboration" action yet (cancel→Finished path exists server-side but no UI trigger surfaced here). | In progress |
| IF-2 | **Kolab completion / feedback flow** | Backend supports `POST /collaborations/{id}/complete` + `/review`; detail screen has completion/review sheets. **Backend gate SHIPPED 2026-06-01** — `/complete` now requires feedback; see IF-5 + `docs/api/2026-06-01-feedback-completion-gate-backend-shipped.md` for the full contract. MISSING: end-to-end test with seeded data; confirm both-parties-confirm → completed transition (now driven by `viewer_must_submit_feedback` / `pending_feedback_from`). **2026-06-22 (FX-30): feedback-first sheet wired to `/feedback` + gated `/complete` with error-code branching — app side done, pending device e2e.** | In progress |
| IF-3 | **Reschedule collaboration** | Client calls `PATCH /collaborations/{id}` but the exact verb/path/fields are unconfirmed (see `TODO(backend)` in `collaboration_detail_provider.dart`). | Blocked on backend contract |
| IF-4 | ~~**Test data seeding (6 kolabs)**~~ | ✅ DONE 2026-05-31 — seeded directly into production Postgres (`main` db) via psql, the legitimate way around the API paywall (a DB write like the artisan seeder; never bypassing the paywall in client/API code). 6 collaborations between Real Run Club (community/creator) + Eixample 46 (business/applicant): **today-13h ×2 (2026-05-31), yesterday-20h ×2 (2026-05-30), tomorrow-10h ×2 (2026-06-01)**, all `status=scheduled`, **no reviews/feedback** — the exact state to test the completion/feedback flow. Each row chain (collab_opportunities → accepted applications → collaborations) matches the real schema (see docs/BACKEND-SCHEMA.md). VERIFIED via SQL **and** live API (`GET /collaborations` for Eixample46 = 6 scheduled + 1 pre-existing completed = 7). 12 orphan probe opps cleaned up. All tagged `[TEST]` — safe to delete. | Done |
| IF-5 | **Feedback / completion flow is wrong (high priority)** | AUDIT 2026-05-31. **What's built:** app `kolab_review_sheet.dart` POSTs `/collaborations/{id}/review` with only `rating`, `would_collaborate_again`, `note` → writes the *lesser* `collaboration_reviews` table. Completion (`kolab_completion_sheet.dart` → `markCollaborationCompleted` POST `/complete`) marks complete and shows the review CTA *afterwards* (visually wrong; not forced). **What's MISSING / wrong:** (1) Backend ALSO has `POST /collaborations/{id}/feedback` (verified live: 422 with required `rating, reviewer_role, expectation_match, would_recommend`; optional per DB `collaboration_feedback`: `posts_reels, stories_posted, revenue, benefits`) — the app never calls it, so the rich questions (stories/reels counts, revenue estimate, expectation match, would-recommend) are absent. (2) Feedback must be FORCED at close: completion should require feedback, and the collaboration should only move to completed once feedback is submitted — currently complete happens first, feedback optional after. (3) Review shows after completion instead of as the closing step. **Fix plan:** build a full feedback sheet (rating, expectation_match, would_recommend, stories_posted, posts_reels, revenue, benefits) bound to `POST /collaborations/{id}/feedback`; make Complete open it and only call `/complete` after a successful feedback submit; drop/merge the old `/review` sheet. Tables: `collaboration_feedback` (rich) vs `collaboration_reviews` (legacy) — see docs/BACKEND-SCHEMA.md. **✅ BACKEND SHIPPED 2026-06-01** (`kolabing-v2`) — full contract in `docs/api/2026-06-01-feedback-completion-gate-backend-shipped.md`: `POST`/`PUT /feedback`, **gated** `/complete` with error codes (`awaiting_own_feedback`, `awaiting_partner_feedback`, `feedback_locked`, `feedback_already_submitted`); new `CollaborationResource` fields (`viewer_must_submit_feedback`, `pending_feedback_from`, `own_feedback`, `partner_feedback` [cross-visible subset only], completion-metadata trio); legacy `/review` auto-mirrors stub feedback rows so the new build is non-breaking; daily 03:00 auto-complete after 7d + ≥1 feedback. **App build now unblocked** — wire the sheet to the new fields + branch on the four error codes. **✅ APP WIRED 2026-06-22 (FX-30, branch `fix/kolab-completion-flow`):** feedback-first sheet now POSTs `/feedback` (role-aware payload) before `/complete`; provider parses `error_code`+`message` (`CollaborationCompletionException`) and the sheet branches on success / `awaiting_partner_feedback` (soft success) / `cannot_complete` / `invalid_status_transition` / `awaiting_own_feedback`. Legacy `/review` sheet left intact (distinct flow). Pending device e2e. | App wired — pending e2e |
| IF-6 | **Gamification challenges use mock data** | AUDIT 2026-05-31. **Built:** backend `GET /challenges/system` works (200, real data); `PUT`/`POST /collaborations/{id}/challenges` documented + live; DB has `challenges` + `collaboration_challenges`; app `challenge_service.dart` has event-scoped methods + a `systemChallenges` getter; `Challenge.fromJson` matches the `/challenges/system` shape. **Wrong:** `collaboration_detail_provider.dart` exposes `availableChallengesProvider` returning hardcoded `_mockChallenges` (and it's UNUSED); the Gamification Setup card shows `collaboration.challenges` which the API returns as `[]`, so it's always empty; "Add custom challenge" is a stub ("coming soon"). **Fix plan:** wire the Gamification card to `GET /challenges/system` (real list) + persist selection via `PUT /collaborations/{id}/challenges`; delete `_mockChallenges`/`_mockCollaboration`. **Event Preparation** = just a static timeline label, no dedicated backend/feature. | Audited — needs build |
| IF-7 | **Community Members + customisable tiers** (was NF-6) | Generalise `attendee` → "Community Members" (UI label; wire value unchanged); Community Leader gets a **Community** tab to run one free community with leader-defined tiers (rules: `manual`/`xp_threshold`/`tenure`/`events_attended`), `can_manage` orthogonal to tier, members in many communities. **✅ BACKEND SHIPPED** (`kolabing-v2` `feat/community-members-tiers`): `communities`/`community_tiers`/`community_members` migrations, full endpoints (`/communities`, `/tiers`, `/members`, `/join`, `/me/communities`, `/me/memberships`), `CommunityPolicy` (owner/can_manage; never the business paywall), nightly `app:evaluate-community-tiers` + on-check-in hook, chapter leaderboard `?community_id=`, 1-free cap (`community_limit_reached` 422), `events.community_id` linkage. **✅ APP DONE** (`community-member-flow`): models + `CommunityService` + providers; Community Leader tab (hub, create-community→NF-7 upsell, tier editor, roster + member-edit/invite); member "My Communities" tab + chapter leaderboard; contract verified field-for-field + 8 parse tests. **PENDING:** (1) backend merge→master + deploy; (2) app merge `community-member-flow`→master + deploy; (3) end-to-end live verify against a real community. **Follow-ups:** content/chat gating UI (Phase 2, uses tier `permissions`), member search picker for invite (needs search endpoint), NF-7 paywall. Plan: `docs/plans/community-members-tiers-phase1.md`; ticket: `docs/tickets/2026-06-02-community-members-tiers-backend-prompt.md`. | Built both sides — pending deploy + e2e verify |
| IF-16 | **Events / attendee experience completion (app side, NF-16)** | Branch `feat/nf16-events-attendee-app` off `community-member-flow`. **✅ APP B1–B3 BUILT** (ticket `docs/tickets/2026-06-05-app-events-attendee-completion.md`): **B1** `EventHubScreen` replaces the standalone RSVP screen — leader sees attendees (going+waitlist via `GET /events/{id}/signups` → new `EventSignup`/`EventSignups` models + `EventService.getSignups` + `eventSignupsProvider` Notifier) + Edit/Add-photos, member sees RSVP + "Open event chat" (once going) via `ChatService.createEventChat`→`ChatThreadScreen`; wired into `community_hub_screen` (leader) + `community_detail_screen` (member). **B2** real create/**edit** form: name/starts/ends/location/capacity(+Unlimited)/tier-gate picker (All vs selected tiers from `communityTiersProvider`)/photos; `EventService.updateUpcomingEvent` (PUT) + `addEventPhotos` (multipart `photos[]`). **B3** `NotificationSettingsScreen` + `notificationSettingsProvider` bound to `GET`/`PUT /me/notification-preferences` (Messages/applications/collaborations/tips), entry tiles in community + attendee profiles; `NotificationPreferences` now reads/writes `message_notifications` (legacy `messages_enabled` fallback, default on). i18n en/es/ca added; `flutter analyze` 0 errors; +8 tests (165 total green). **DEFERRED B4** real-time (Echo→`/broadcasting/auth`→`private-chat.thread.{id}`): NOT built — `TODO(NF-16 B4)` left in `chat_thread_screen.dart`; ops-gated on PR #21 + Reverb. **ASSUMED-PARALLEL backend endpoints** (coded to contract, work once deployed): `POST /events/{id}/photos` (multipart→EventResource) and `message_notifications` in the prefs API. **PENDING:** merge branch; e2e verify against live backend once photos + message-pref ship. | App B1–B3 built — pending PR + backend photos/pref deploy + e2e |
| IF-19 | **Attendee onboarding + universal @handle + add-by-identifier (app side)** | Branch `fixes-attendee`. Contract `docs/tickets/2026-06-10-attendee-onboarding-identity-contract.md` + `docs/CANONICAL-LISTS.md`. **✅ APP BUILT (this pass):** (1) **4-step attendee onboarding** after register (You: name+`@handle` live-availability+photo · City `citiesProvider` · Interests multi-select from `communityTypesProvider` (slugs, NEVER the placeholder enum) · Join: discover, interest-ranked) → `PUT /onboarding/attendee` → `/permissions`/`/attendee`; new `AttendeeOnboardingService`/`-Provider`/`-State` + 4 step screens + shared scaffold; attendee register now routes into step 1. (2) **Edit Profile** gained an `@handle` field (live availability + format hint). (3) **Friends add-by-identifier** screen (email or `@handle` → `GET /profiles/lookup` → result card with Add/Pending/Friends/Self CTA → `POST /friends/{id}`), reachable from FriendsScreen app-bar + profile "Find friends". (4) **Leader roster add-member** accepts email OR `@handle` (extended `CommunityService.addMember`). Shared `IdentityService` (`GET /handle/available`, `GET /profiles/lookup`) + `HandleField` widget; `UserModel` gained `name`/`handle`/`interests`; `Community` gained raw `typeSlug` + `matched` hint (+ "For You" badge). All new endpoints self-gate (404→feature off, no crash). i18n en/es/ca (+~45 keys), `flutter gen-l10n` ok, `flutter analyze` **0 errors**, onboarding/community/auth/profile tests green. **PENDING:** backend deploy of the 3 new endpoints (`/handle/available`, `/profiles/lookup`, `/onboarding/attendee`) + member-add handle resolution + discover interest-ranking/`matched`; e2e verify on device. NOT committed. | App built — pending backend deploy + e2e |
| IF-20 | **Event location via Google Places → auto-detect city (NF-20)** | Branch `fixes-attendee`. **✅ APP BUILT (this pass):** `create_event_screen.dart` now replaces the free-text location field AND the manual `_CityField`/`_CityPickerSheet` with the **existing Google Places autocomplete** (same UX as `business_step5_screen.dart`): type → `placeSuggestionsProvider(query)` suggestions → tap → `OnboardingService.getPlaceDetails(placeId)`. On select: location text ← `PlaceDetailsImport.primaryVenue.formattedAddress`; `_cityId`/`_cityName` ← `PlaceDetailsImport.cityId`/`cityName` (optimistic city from the suggestion first, refined by details). Derived city shown read-only under the field ("City: Barcelona"); if no resolvable city, a note says it won't be city-discoverable but submit is NOT blocked. EDIT seeds the field from `event.location` and keeps existing `city_id` until a new place is picked. Still sends `location` + `city_id` to `EventService.create/updateUpcomingEvent` (those are the only address params EventService accepts — no lat/lng/address field exists, so none invented). Removed the manual city-picker code (`_pickCity`, `_CityField`, `_CityPickerSheet`) + now-unused `radius.dart`/`city.dart`/`city_list_item.dart` imports. i18n en/es/ca (+7 keys), `flutter gen-l10n` ok, `flutter analyze` **0 errors**. NOT committed. **PENDING:** e2e verify on device (search → pick → city auto-fills → save → event city-discoverable). | App built — pending e2e verify |
| IF-21 | **Attendee Google + Apple social login (app side)** | Branch `feat/attendee-social-login`. Spec `docs/superpowers/specs/2026-06-11-attendee-social-login-design.md`, plan `docs/superpowers/plans/2026-06-11-attendee-social-login.md`. **✅ APP BUILT:** attendee register screen offers **Google + Apple side-by-side in a single non-scrolling viewport** (screen redesigned to fit without scroll). Apple is iOS-only and `FeatureFlags.attendeeAppleSignupEnabled` is now **on** (App Store Guideline 4.8 requires Sign in with Apple wherever Google is offered). `signInWithGoogle/Apple` take a `UserType userTypeHint` → service forwards `user_type='attendee'` (`/auth/google` honors it today; `/auth/apple` ignores it until backend ships); Google now honors `is_new_user` (was hardcoded false). `resolveAuthDestination` routes a brand-new social attendee into the 4-step onboarding (name prefilled from the provider, best-effort). New `signInWithApple`/`authOrContinueWith` i18n (en/es/ca); `flutter analyze` **0 errors**; **187 tests green**. **Backend PR #24** in `kolabing-v2` (`/auth/apple` accept+honor `user_type`). **PENDING (hard pre-merge dependency):** backend PR #24 must merge+deploy before this merges — until then a brand-new Apple attendee is created with the backend's default role; verify Android `google-services.json` + Apple entitlement provisioning; e2e on device (new Google attendee → onboarding → dashboard). | App built; PR #9 open — Apple enabled, blocked on backend PR #24; e2e pending |
| IF-22 | **Dev/Prod environment separation (iOS, #17)** | Branch `feat/env-dev-prod-flavors`. Spec/plan `docs/superpowers/{specs,plans}/2026-06-18-dev-prod-flavors*.md`. **✅ APP BUILT:** new `lib/config/environment.dart` is the single source of truth for env-dependent hosts; `ApiConfig.baseUrl`, `realtime.dart` broadcast auth, share/QR host, Sentry `environment`, and a PostHog `environment` super-property all derive from it. Env resolved at compile time from **`--dart-define=APP_ENV=dev|prod`** (dev = `kolabing-v2-development-uhzrzd.laravel.cloud`, prod = `kolabing.com`); no define → **prod in release, dev in debug/profile** (so a bare release build can't ship the dev backend). `Makefile` targets `make run-dev/run-prod/ipa-dev/ipa-prod`. **Scope narrowed mid-build (2026-06-18):** Android dropped (not shipping Android); iOS "one app, two builds" — same bundle id / same Firebase / same App Store Connect app, dev distinguished by a separate TestFlight tester group. No Xcode/native changes (APP_ENV is a pure Dart const). `flutter analyze` 0 (no new errors), env tests 8/8 green. **PENDING:** open PR #? + `Closes #17`; produce a dev IPA via `make ipa-dev`, upload to a dev TestFlight group, e2e verify the dev build hits the dev backend and prod hits prod. | App built — pending PR + dev TestFlight upload + e2e |
| IF-8 | **App localization — Spanish + Catalan (en/es/ca)** | Branch `feature/i18n-spanish-catalan`. AUDIT 2026-06-02: no i18n existed (only `intl: any`, no delegates on `MaterialApp`). Backend does NOT localize dynamic content (no `Accept-Language`/`setLocale`; categories/cities/user copy are single-language) → this is **UI-chrome only**. Approach: **official `flutter_localizations` + gen-l10n (ARB)** + Riverpod `localeProvider`, in-app switcher + system default. **INFRA + BULK MIGRATION DONE 2026-06-02** (19 parallel agents, one per feature batch): pubspec (`flutter_localizations`, `generate: true`), `l10n.yaml`, `lib/l10n/app_{en,es,ca}.arb` (**1,309 keys each**), generated `AppLocalizations` (Flutter 3.41 → `lib/l10n/`, not synthetic `flutter_gen`), `LocaleService`+`localeProvider` (SharedPreferences), `MaterialApp.router` wired, `LanguageSelectorScreen` + `/settings/language` route, entry tiles in business/community/attendee profiles, **all 89 screen/widget files** migrated. `flutter analyze` = **0 errors**. es = European/Castilian (81 terms flagged where LatAm differs: Ajustes/Configuración, Aforo/Capacidad, Local/Lugar, Vídeos/Videos). **MISSING (real gaps needing a `BuildContext`-aware/locale-aware pattern):** (1) enum `displayName`/`label` getters in models (ProductType, DeliverableType, AvailabilityMode, ChallengeDifficulty, subscription.status, venue/intent labels); (2) hand-rolled date formatting (English month/`_dayNames` arrays) → switch to locale-aware `DateFormat`; (3) currency/price strings; (4) 64 deliberately-skipped literals (brand names, dynamic backend errors, punctuation/emoji — correctly left). Also: device QA; decide whether generated `lib/l10n/*.dart` are committed or gitignored. | In progress (all screen/widget chrome shipped; enum labels + date/locale formatting pending) |

---

## 🐛 Fixes
_Bugs to fix. Add when detected; move to a struck-through "confirmed fixed" line (or delete) once verified._

| # | Bug | Status |
|---|-----|--------|
| FX-52 | **The community cover uploads, reports success and changes nothing — the dev API is older than the field** — reported by Volkan 2026-08-27 ("profildeki background image guncellenmiyor upload oluyor ama arka plan degismiyor profilde"). App and backend source are both correct; the **development environment is running code that predates kolabing-v2#239**. Measured, not guessed: a debug build's raw `/me/profile` payload has **no `cover_photo` key at all** in `community_profile` (`... profile_photo, logo_url, is_featured ...`), while `CommunityProfileResource` on kolabing-v2 master (587b25f) emits it right after `logo_url`. That also rules out "the migration didn't run" — a missing column would still emit the key as null. So `PUT /me/profile` with `[multipart cover_photo]` answers **200 and drops the field**, and the app read that as success because it only asked "did it throw?" — the same shape as the community-dashboard outage, invisible to Sentry. **App side fixed** (kolabing-app#177, issue #176): three outcomes instead of two, so an accepted-and-dropped upload says "The cover was not saved"; plus `CommunityProfile.toJson()` now writes `cover_photo`, which it never did although `fromJson` reads it (latent — the only reader takes a fresh GET). **Still open and NOT ours to do:** redeploy the development environment from kolabing-v2 master, then verify `cover_photo` is present in `GET /me/profile`; same check on production before the cover is announced. | **App fixed — backend deploy outstanding (needs the Laravel Cloud console)** |
| FX-51 | **"Waiting for your partner" polled `/me/challenge-completions` 1302 times in one session** — bir simülatör oturumu 94 dakika açık kaldı ve tüm oturumda birden fazla çağrılan **tek** uç buydu (1302 istek; diğerleri: `events/discover` 1, `events` 1). Issue **#170**. 1302 × 3s ≈ 65 dakika kesintisiz polling. Kaynağı `CompletionWatchNotifier` (`interval: 3s`, `timeout: 2min`, `pageSize: 20` — loglardaki `limit=20` ile birebir). **Hesap tutmuyor:** tek bir watch sınırlı — `start()` `_running` ile korunuyor, `while` döngüsü start+2dk `deadline`'ı kontrol ediyor, timeout'ta `_running = false`. Yani tek watch en fazla ~40 poll. 1302, **~32 kez yeniden kurulduğu** anlamına geliyor; timeout sonrası `start()`'ı çağıran tek yer ise `_StoppedWatching`'in "keep waiting" **butonu** — kimse boşta 32 kez basmaz. Okumayla eledim: `myChallengeCompletionsProvider` hiçbir build metodunda `watch` edilmiyor (sadece `read`/`invalidate`), ve `completionWatchProvider` autoDispose olmayan bir `family` olduğu için geri dönüşte aynı notifier'a düşüp `_running` guard'ı tutuyor. Yani yeniden kurulma yolu gerçek ama kaynaktan görünmüyor — `start()`'a bir print koyup üretmek gerekiyor. **Etkisi:** o ekranda bekleyen her kullanıcı saatte ~1200 istek. #169'u (events feed kartları) çalıştırırken gözlendi; o değişiklik ne bu ekrana ne bu uca dokunuyor. | **Gözlendi, teşhis edilmedi — üretilmesi gerekiyor** |
| FX-50 | **A free business could read a community's blurred identity by saving the Kolab first** — the Explore deck computes `hideCreatorIdentity` and passes it to `ExploreDetailSheet`, but `_openSavedOpportunity` (the Saved tab) opened the same sheet without it, so the parameter fell back to its `false` default and the name and logo rendered unblurred. §2.5 says a free business sees a community's name and logo blurred; "unless it saved it" was never part of that. Fixed by computing the same flag from `opportunity.creatorProfile.isCommunity` — `hasSub` is already true for a community viewer, so it can only ever hide a community from a free business — and the sheet's new header link is null whenever the flag is set. | **Fixed on `feat/profile-links-everywhere` — needs device QA with a free business account** |
| FX-49 | **Onboarding city step: the Continue button was drawn on top of the search field when the keyboard opened** — reported on device by Volkan, "Where are you located?" (Step 3 of 4). The user could not see what they were typing and got stuck; "Popular Cities:" was clipped behind the keyboard. Issue **#163**. **Cause:** `Scaffold > SafeArea > Column` with a fixed header, a fixed title block, a fixed field and a fixed footer around a single `Expanded` list, and `resizeToAvoidBottomInset` left at its default `true`. On a 393×852 phone: `852 − 59 − 34 − 296 (chrome) − 100 (CTA) − 336 (keyboard) = 27dp` for the list; on anything smaller it goes negative, `Expanded` collapses to 0 and the `Column` overflows, so the pinned CTA paints over the field. **The same structure — so the same bug — was in all three city steps** (`community_step3`, `attendee_step2`, `business_product_cities`). **Fix:** one shared `OnboardingSearchStep` shell that owns the keyboard: `resizeToAvoidBottomInset: false` so the body height never changes, and the keyboard reserved **inside** the flexible child (`Expanded > Padding(bottom: inset)`) rather than as a rigid sibling — a `SizedBox` sibling still overflowed for the frame where the inset was full but the chrome had not finished folding, which the test suite caught. **Plus the UX the step should have had:** while the field holds focus the step counter, progress dots, title, subtitle and footer all fold away (200ms), handing ~200dp back to the results — six rows instead of half a row; picking a city unfocuses, which brings the enabled Continue back under the thumb; a drag on the list dismisses the keyboard; and the empty/error states moved off centre to just under the field, where "No cities found" is actually visible. **i18n debt cleared in passing:** `onboarding_header.dart` hardcoded `'Step X of Y'`, `'Back'` and `'Skip'` — now `onboardingStepOf` (new, en/es/ca) plus the existing `commonBack`/`commonSkip`. `flutter analyze` 0 errors; suite **343 pass / 10 pre-existing failures** vs a `git stash`-verified `336 / 10` baseline on master — +7, all new. | **Fixed on `fix/onboarding-city-step-keyboard` — pending device QA on the three steps** |
| FX-48 | **A community reward's name wrapped one letter per line** — reported from the Rewards tab of Real Run Club (a member view), where the card grew taller than the screen with the title running vertically down its left edge. Root cause is not the text: `ElevatedButton`/`FilledButton`/`OutlinedButton` all carry `minimumSize: Size(double.infinity, …)` from the theme so form buttons stretch, and a `Row` measures its non-flex children with an **unbounded** width — so the Redeem button claimed the whole row and the `Expanded` title beside it got 0px. A 0-wide `Text` still paints, one character per line. Fixed by bounding the button in both copies of that tile (`community_detail_screen.dart` `_MemberRewardTile`, `personal_rewards_screen.dart`) — the only two instances of the pattern in `lib`, found by an AST-ish indent walk, not grep. A **guardrail test** (rule 4 in `test/lints/button_style_lint_test.dart`) now fails CI on any theme-sized button placed directly in a Row without a bounded `minimumSize`; it was verified red on the pre-fix tree and green after. The same class of collapse is also asserted at the pixel level in `test/features/community/widgets/community_page_sections_test.dart`. | ~~Fixed 2026-08-24~~ — code + guardrail landed on `integration/all-open-prs`; **device QA pending** |
| FX-46 | **App Store rejection — referral-code field on the subscription paywall reads as an alternate purchase mechanism** (Guideline 3.1.1), reported by Daniel in the Kolabing Telegram group 2026-08-17. `ReferralCodeField` sat right next to the IAP/Stripe purchase button in both `SubscriptionPaywall` (modal) and `SubscriptionScreen` (subscribe CTA). Branch `fix/paywall-remove-referral-code-field` (#123): removed the field + its controller/error/helper state + the now-dead `_setValidatedReferralCode` handlers from both screens; call sites no longer pass `referralCode` into `purchase()`/`createCheckoutSession()`. Register/signup field (`business_final_screen.dart`) untouched. `flutter analyze` clean (1 pre-existing info), 13/13 subscription tests green, 2 paywall tests updated (assert `findsNothing`), full suite's 10 failures pre-existing on master (verified via `git stash`). **Separate gap surfaced, not fixed here:** the register-time referral field was already a backend no-op (`RegisterBusinessRequest` doesn't accept `referral_code`), so the referral-at-purchase reward path (`ReferralService::rewardFirstPaidSubscription`) is now unreachable from any UI — needs a real product decision (persist + auto-apply server-side, or accept it's dormant). | App built, branch pushed, PR pending — **pending device QA + resubmit** |
| FX-45 | **Paywall quoted the storefront's currency ($) instead of euro.** Product decision (Volkan, 2026-08-14) **reverses the display half of FX-44**: Kolabing quotes one currency everywhere — Stripe/web, marketing copy and the ASC base price are all euro — so the app must never switch symbol per storefront. Seen live on the simulator (Xcode + `ios/Runner/Configuration.storekit`, US test storefront): the plan cards read **$99.99 / 3 months**, **≈ $33.33/mo**, **$49.99 / month**. Branch `fix/subscription-currency-and-cancel` (#119): new single helper `IAPState.formatEuro(double)`; `displayPriceFor`, `priceString` and `perMonthEquivalent` now build `€<amount>` from `ProductDetails.rawPrice` instead of the store's localized string / `currencySymbol`; the FX-44 comments in `iap_provider.dart` + `subscription_paywall.dart` rewritten; the FX-44 US-storefront regression test inverted (`$44.99` → renders `€44.99`) + a new per-month-equivalent case. Re-verified on the iPhone 17 Pro simulator: **€99.99 / 3 months**, **≈ €33.33/mo**, **€49.99 / month**. 13/13 subscription tests green, `flutter analyze` clean (1 pre-existing `directives_ordering` info). **Trade-off to resolve in ASC:** Apple converts €49.99 per storefront and bills in the storefront's currency ($44.99 in the US), so forcing "€" quotes a currency the user is not billed in — an App Review 3.1.2 risk. The clean fix is to **limit App Store availability to euro-zone storefronts**; until then the euro symbol is a deliberate product choice. | ✅ Merged to master 2026-08-14 (PR #118, `0c54e83`). Decision taken: euro stays, ASC availability limited to euro-zone storefronts — **pending that ASC change + device QA** |
| FX-44 | **Paywall quoted € in front of a non-EUR StoreKit price.** `IAPState.formatEur` prepended `€` to `ProductDetails.rawPrice` and ignored `currencyCode`, so on any storefront where Apple converts the €49.99 base the app quoted a currency the user is not billed in. Caught live on the simulator (US storefront): StoreKit returned `price="$44.99" code=USD`, the paywall rendered **€44.99**; `perMonthEquivalent` showed `≈ €30.00/mo` the same way. Branch `fix/subscription-currency-and-cancel` (#117): displayed prices now come from StoreKit's own localized `ProductDetails.price`, the derived per-month figure is built with the product's `currencySymbol`, and the hardcoded `€49.99`/`€99.99` survive only as the not-loaded / Android-Stripe fallbacks (`kFallbackMonthlyPrice`). +1 regression test pinning a US-storefront product. **Also in this branch:** `Subscription.source` is now nullable so "backend omitted it" is distinguishable from an explicit `stripe` — on iOS the cancel button routes to Apple's subscription management unless the subscription is *explicitly* Stripe (previously a missing `source` defaulted to `stripe` and fired an API cancel that cannot touch a StoreKit subscription), and a failed `launchUrl` now surfaces a localized message instead of a dead button. `showManageSubscriptions` is not reachable — `in_app_purchase_storekit 0.4.8+1` does not expose it, so the `apps.apple.com/account/subscriptions` deep link stays. **Not code:** Apple auto-converted €49.99 → **$44.99** for the US storefront; per-storefront price points must be set manually in ASC if USD should also read 49.99 (3-month is $89.99 today). | Superseded on the display side by **FX-45** (euro everywhere, 2026-08-14); the `Subscription.source` / cancel-through-Apple half stands. ✅ Merged to master 2026-08-14 (PR #118, `0c54e83`). Per-storefront USD price points are moot now that availability is being limited to euro-zone storefronts — **pending device QA** |
| FX-43 | **App Review 5.1.1(iv) — "Allow" pre-prompt button, and a location permission with no feature behind it** (submission `c1e3874e-d038-49b8-b022-4262095d48c8`, v1.5 build 34, rejected 2026-08-12). Apple: *"A custom message appears before the permission request, and to proceed users press a 'Allow' button. Use words like 'Continue' or 'Next' instead."* — `permission_screen.dart:323`. Branch `fix/appreview-5-1-1-permission-copy` (#115): row action → `commonContinue`, dismiss button → `commonDone`, title no longer says "Allow". **Deeper fix:** new `kLocationPermissionPromptEnabled=false` hides the Location row entirely — nothing in the shipped app reads device location (the only `getCurrentPosition` caller, `EventDiscoveryScreen`, is never routed), so the ask was a standing 5.1.1 risk; while the flag is off the app never touches the location permission, not even to read its status. Also: 12 ARB keys en/es/ca replacing ~10 hardcoded literals on the screen, unused `NSLocationAlwaysAndWhenInUseUsageDescription` dropped, `LocationAccuracy.high`→`reduced`, `Configuration.storekit` 39.99→49.99, build 34→35. `flutter analyze` 0 errors; 10 test failures all pre-existing on master (verified). **Still open (ASC):** remove **Coarse Location** from the privacy labels while the flag is off; #101 IAP review screenshots + attach to version; #100 expired demo account. **Follow-up:** ship the nearby-kolabs location feature, then flip the flag and re-declare Coarse Location. | ✅ Merged to master 2026-08-14 (PR #116, `af28da8`) — **pending ASC actions (#101 attach IAP to the version, #100 demo account, #97 privacy labels incl. dropping Coarse Location) + device screenshots + resubmit** |
| FX-42 | **Monthly price said €49 in the app but the App Store tier is €49.99.** PR #109 moved every user-visible string from `€39.99` to `€49`, but the price point actually set in App Store Connect is **49,99 €** — so the iOS paywall (which reads StoreKit's live price) showed 49,99 € while the Android/Stripe box, the plan-detail row, the subscribe button and the offline fallbacks showed €49. Branch `chore/pricing-49-99`: `iap_provider.priceString` + both `subscription_paywall` fallbacks → `€49.99`; `subscriptionPriceMonthly` / `subscriptionSubscribePricedButton` → `49.99 EUR` in en/es/ca (+ gen-l10n); test expectation + `docs/release/APP-REVIEW-2026-08-04-resolution.md` + `docs/ROLES-AND-PERMISSIONS.md` updated. The **ASC price point was set to 49,99 € by Volkan on 2026-08-07**, and iOS build **1.5.1 (34)** carrying these strings was archived + uploaded the same day (#113). **Still open:** backend `config/subscriptions.php` (kolabing-v2) — the Stripe/web path still quotes the old number — and the on-device paywall walkthrough (needs a Business account with no active subscription). | Fixed in code 2026-08-07, ASC tier applied — **pending backend config bump + device QA** |
| FX-41 | **Audit follow-ups — redacted body logs (#44)** (branch `fix/audit-followups-routes-logs`, #93). #44: removed 4 `debugPrint` calls in `opportunity_service.dart` that logged full request/response bodies (browse/create/PUT/publish) — status codes are still logged adjacent, so no debug signal lost. The #15/#16 half of this branch (next-action routes) **landed first via FX-40**, whose version is kept on merge: `complete_profile`→Profile tab (null-safe `onSwitchTab`), `review_pending_applications`→`/business/applications`, `leave_review`→`/business/finished` (a real route, instead of this branch's tab-2 switch). `flutter analyze` 0 new. | Fixed in code 2026-08-07 |
| FX-39 | **App Store rejection — missing Terms of Use (EULA) link on the subscription paywall** (Guideline 3.1.2; App Review submission `18720871-a906-4023-8ac0-c0054e1818f1`, v1.5, rejected 2026-07-31). The auto-renewable-subscription surfaces showed plan/length/price but **no functional EULA link, no Privacy Policy link, and no auto-renew disclosure** in the binary. Branch `fix/subscription-eula-privacy-links-3-1-2`: new reusable `SubscriptionLegalFooter` (`lib/features/subscription/widgets/`) — auto-renew disclosure + tappable **Terms of Use (EULA)** + **Privacy Policy** links reusing the canonical locale-aware `LegalLinks` URLs (external launch, same pattern as `TermsConsentCheckbox`) — wired into both `SubscriptionPaywall` (modal) and `SubscriptionScreen` (full screen), under each subscribe CTA. i18n en/es/ca (3 keys) + hand-added generated getters (no Flutter SDK on the build box — `flutter gen-l10n`/`analyze`/`format` must be run by the reviewer). +2 footer widget tests + paywall-test assertion. **Also required (App Store Connect metadata, not code — Daniel/Volkan):** add the EULA link to the App Description (or the custom-EULA field) and resubmit — the automated check flagged the *metadata* link specifically. | Fixed in code 2026-08-02 — **pending toolchain run (gen-l10n/analyze/format) + ASC metadata EULA link + resubmit** |
| FX-40 | **QA-batch (2026-07-28 review) app-side — F1 public-profile 404, F2 dead next-action cards, #8 chat "Tap to open"** (from the live iOS QA scorecard). Branch `fix/qa-batch-f1-f2-chat-preview`. **F1:** the event→host-community tap for a **business viewer** pushed `events.community_id` (a `communities.id`) into `/profile/{id}`, which binds `profiles.id` → 404 on profile+collaborations+gallery. Added `Event.hostProfileId` (from `host_profile_id`); `event_detail_screen._openHostCommunity` now pushes `/profile/{hostProfileId}` and **falls back to the community-keyed screen when the id is absent** (never 404s pre-backend). **F2:** `business_dashboard_screen._onNextActionTap` wired `complete_profile`→Profile tab (onSwitchTab 4), `review_pending_applications`→`/business/applications`, `leave_review`→new `/business/finished`; also fixed a **latent bug** — `/business/applications` built `BusinessMainScreen(initialTab: 3)` (Chats!) → now `initialTab: 2, initialKolabsSubTab: 1` (My Kolabs → Requests). **#8:** added `ChatThread.lastMessagePreview` (from backend `last_message.content`); chat list shows the preview, keeping "Tap to open" only as a has-messages-but-no-preview fallback. +2 chat_thread parse tests. **Backend halves (F1 `host_profile_id`, #8 `last_message`, #9 seeder) = kolabing-v2 PR.** No SDK on the build box → gen-l10n/analyze/format + tests by the reviewer. | Fixed in code 2026-08-02 — **pending toolchain run + backend PR + device e2e** |
| FX-38 | **Copy cluster — legacy sponsor/partner wording, "No Venue", community-size copy** (audit #19, #20, #21). #19: reworded `selectionCardCommunityDescription` ("Looking for brands to Kolab with"), `offeringSponsorshipTitle` ("Collaboration budget"), `intentSelectionFindVenueTitle` ("Find a Venue or Brand"), `businessProductOfferingHint` ("giveaway prizes") in en/es/ca. #20: `VenueMode` labels localized via new `venueModeBusinessVenue/CommunityVenue/NoVenue` ARB keys + `VenueModeL10n.label(l10n)` extension (`opportunity_l10n.dart`), replacing the hardcoded `displayName` at all 5 UI sites; "No Venue" → "No venue yet" (also fixes the i18n gap). #21: `exploreDetailScaleCommunity`/`Expected` → "{count} members" / "~{count} expected to attend" (en/es/ca). `flutter analyze` 0 new; `flutter gen-l10n` clean. Branch `fix/copy-cluster` (#90). | Fixed in code 2026-07-28 — pending PR review |
| FX-37 | **Stale nav + design-token docs synced to code** (audit #50, #51, docs-only). `CLAUDE.md` nav description fixed (5 tabs, not 4: Home·Explore·My Kolabs·Chats + role-specific 5th — Business Profile / Community Community). Design tokens in `CLAUDE.md`, `DESIGN.md`, `README.md` corrected to the real "Atmospheric Editorial" palette (primary `#FFE28C`, bg `#FAF5EA`, fonts **Anton**/**Inter** via `google_fonts`, not `#FFD861`/`#F7F8FA`/Rubik/Open Sans/DM Sans) with pointers naming `colors.dart`/`typography.dart` as the single source of truth. Branch `docs/sync-nav-and-design-tokens` (#88). | ✅ Merged to master 2026-07-28 (PR #89) |
| FX-36 | **Unused `supabase_flutter` dependency removed** (audit #46, chore). Backend is Laravel/Sanctum; `grep -rn supabase lib/ test/` was zero. Dropped `supabase_flutter: ^2.3.0` from `pubspec.yaml`; `flutter pub get` removed 19 transitive deps; `flutter analyze` shows no new errors/warnings. Branch `chore/remove-unused-supabase` (#86). | Fixed in code 2026-07-28 — pending PR review |
| FX-35 | **Business self-view profile lacked a reputation block** (audit #13, remaining half / M-04). Branch `feat/business-self-reputation` (#84): extracted `_ReputationSummaryCard` → shared public `ReputationSummaryCard` (`lib/features/profile/widgets/`) and added a **completed-Kolabs** stat (shown when >0) so it renders on BOTH the public profile and the business's own profile. Self-view fetches the owner's own public profile via `publicProfileProvider(businessProfile.id)` (no backend change) and shows the card after the header (loading/error → nothing; no-reviews → empty state). i18n `reputationCompletedKolabsCount` en/es/ca; +5 widget tests; `flutter analyze` 0 new. Out of scope: partner-status badge (blocked on PR #70 / audit #14). | Fixed in code 2026-07-28 — **pending PR review + screenshots + device e2e** |
| FX-34 | **P0 apply-flow block — Apply enabled on date-exhausted/closed Kolabs** (audit #1–#5). App (#82, branch `fix/apply-date-availability-gate`): new single-source-of-truth `opportunityApplicationsOpen()` (status + future-selectable-date, recurring-aware) now (a) excludes closed Kolabs from the Explore discovery deck (Saved unfiltered), (b) disables the Apply CTA + shows an "Applications closed" label in `ExploreDetailSheet` + `CommunityOfferDetailScreen` (no paywall fallthrough), (c) early-returns `ApplyModal.show()` with a snackbar; i18n en/es/ca; +6 unit tests. Backend (kolabing-v2 #107, branch `fix/apply-reject-closed-kolab`): `Kolab::hasSelectableDatesFrom()` + `validateCanApply()` rejection (HTTP 400) closes the direct-API hole; +3 feature tests. Deviations from spec: aligned to the endpoint's existing 400+message pattern (no `error_code`); `KolabStatus` has no `Completed` (published-check already excludes closed). Out of scope: free-text `availability`→structured dates refactor. | ✅ Merged to master 2026-07-28 (app PR #83, backend PR #108) — **pending screenshots + device e2e** |
| FX-36 | **App Store rejection 2026-08-04 (v1.5 b33) — resolution prep.** Two Apple issues, **both ASC/account-level, not app-code bugs** (diagnosed against the code): (1) Guideline **5.1.2(i)** — App Privacy labels over-declare data "Used to Track You" but the app has no ATT prompt; the app genuinely does not track (only Firebase/OneSignal/PostHog-first-party/IAP; no ad SDK/IDFA) → fix = correct the ASC privacy labels (spec in `docs/release/APP-REVIEW-2026-08-04-resolution.md`), do **not** add ATT. (2) Guideline **2.1(b)** — IAP sandbox bug; Apple points at the Paid Applications Agreement; StoreKit flow reviewed and correct → fix = Paid Apps Agreement "In Effect" + subscription product config. Also aligned all user-visible `€39.99`→`€49` (en/es/ca + paywall/status fallbacks). | Doc + €49 code ready — **ASC actions + resubmit pending on Daniel (Account Holder)** |
| FX-33 | **UX-polish batch found in mobile audit** (#80, audit #23–#36) — see full breakdown in the `Last updated` note above: onboarding draft persistence, photo-picker Cancel, stale validation errors, FAB overlap, missing retry/empty-state CTAs, a real small-screen overflow bug + 7 stale test fixes, remaining hardcoded strings, dark-mode static pass, community-dashboard reorder (Find-a-Kolab up, XP/badges/referral down, compact referral, earned-badges-only). Branch `fix/ux-audit-batch2` (PR #81). | Fixed in code 2026-07-21 — **pending screenshots + device e2e** |
| FX-32 | **Trust-surface / review-CTA / i18n gaps found in mobile audit** (#77) — see full breakdown in the `Last updated` note above: own-name pollution on My Kolabs cards + chat inbox, buried "optional feedback" review CTA, ~18 hardcoded English strings on the business dashboard + nav, dead A/B/C dashboard variant switcher. Branch `fix/p1-trust-review-i18n-cleanup`. | Fixed in code 2026-07-19 — **pending screenshots + device e2e** |
| FX-31 | **Tapping a push notification (app killed) doesn't open the target screen — flashes a loading spinner then lands on the dashboard** (#55) — cold-start race: `getInitialMessage` (FCM) / OneSignal click both `kolabingRouter.push(route)` the target on top of the still-visible splash ~500ms after launch, but splash then `context.go(destination)` at ~2.5s **replaces the whole stack**, discarding it. **Fix (branch `fix/notification-tap-stuck-loading`):** new `NotificationNavGate` stashes a tap that arrives during cold start and replays it (push on top of the destination) once splash calls `markAppReadyForNotificationNav()`; background/foreground taps still navigate immediately. Earlier `loadNotifications` stuck-`isLoading` hypothesis ruled out (every auth-session bump pairs with `clearSignedOutState`). `flutter analyze` 0 new issues, 6 gate unit tests green. **Backend follow-up:** `collaboration_completed` has no deeplink in the contract → completion pushes fall back to `/notifications`; needs a real `/collaboration/{id}` deeplink. | Fixed in code 2026-07-04 — **pending device e2e** (kill app → tap notification → opens target screen) |
| FX-1 | ~~Accepted applications wrongly appeared under Requests → Sent~~ — accepted items now filtered out of Requests (Sent + Received); they belong in Active. | ✅ Fixed 2026-05-31 (pending device re-verify) |
| FX-2 | ~~"Collab"/"collaboration" shown in user-facing copy~~ — renamed to "Kolab" across 61 visible strings; backend contract strings deliberately untouched. | ✅ Fixed 2026-05-31 |
| FX-3 | ~~Bottom nav RenderFlex overflow~~ (pre-existing, commit 846dcac). | ✅ Fixed |
| FX-4 | ~~Kolab detail 3-dots (⋮) menu did nothing~~ — it had an empty `onPressed`. Removed; its actions (Reschedule, Complete) already exist inline and are correctly hidden on terminal kolabs, so it was fully redundant. | ✅ Fixed 2026-05-31 |
| FX-5 | Active sub-tab empty / Finished showed only 1 → fixed earlier (CollaborationPartner null-safety + hub refresh-on-open). | ✅ Fixed 2026-05-31 |
| FX-6 | ~~Completion sheet dead-end + invisible spinner~~ — `kolab_completion_sheet.dart` was non-dismissible with no escape, so a feedback POST that kept failing trapped the user even though `/complete` had already succeeded; added a "Finish later" exit shown after a feedback error. Also the loading spinner had no colour (yellow-on-yellow, invisible) → set to `onSurface`. | ✅ Fixed 2026-06-01 (pending device re-verify) |
| FX-7 | ~~XP ladders disagree: `xp_level.dart` (Trusted @ 250) vs `xp_tier.dart` (Trusted @ 100) — two ladders rendered on the same wallet screen (header badge/progress used the 3-tier; progress card + wallet model used the 5-level).~~ Consolidated onto the canonical 5-level `XpLevel`: `wallet_screen` `_buildTierBadge`/`_buildTierProgress` now use `XpLevel.fromXp`; deleted `utils/xp_tier.dart`. Header + progress card now agree. (Backend-independent; NF-5 will later replace the surviving ladder with server values.) | ✅ Fixed 2026-06-01 (pending device re-verify) |
| FX-8 | Hardcoded "+10/+50 XP" mission labels (`wallet_screen.dart`, completion/review sheets) are independent of what the server actually awards → can mislead users. Resolved by NF-5 (`xp_earn_rules` drives both). | Open — blocked on NF-5 config endpoint |
| FX-9 | **Stale chat/profile across account switch** — after switching from a community account to an attendee, the chat inbox (and profile) showed the *previous* account's data (e.g. attendee inbox listed the community's "Eixample 46" kolab chat); "only on manual refresh it goes normal". Root cause was **two-fold**: (1) `invalidateUserScopedProviders` ran on logout but **not on a fresh login**, so kept-alive Notifiers (chat, badge, profile, community) kept the prior user's data; and (2) the chat inbox provider is kept-alive and served cached threads on first open. **Fix (3 layers):** (a) `session_reset.dart` also invalidates `chatThreadsProvider`/`chatUnreadProvider` + `communityManageProvider`/`myMembershipsProvider`; (b) `auth_provider.dart` calls `_invalidateAfterSignIn()` on signInWithEmail/Google/Apple so every account switch tears down the prior user's providers; (c) `ChatsScreen` now reloads threads+unread on mount so the inbox can never render stale data. | ✅ Fixed 2026-06-04 (commits d646713, 0f21d54, then reload-on-mount). Verified on iOS sim: attendee home + profile render clean (no community leftover) after relaunch. **Still to confirm on device:** the in-process community→attendee switch on the chat inbox specifically (sim UI-automation couldn't reliably hit the app-bar chat icon). |
| FX-10 | **Event sign-up 500 for EVERYONE** — `POST /events/{id}/signup` threw on prod for every event/user. Root cause: `EventSignupService::signup` ran `lockForUpdate()->count()`, which Postgres forbids (`FOR UPDATE` + aggregate); SQLite tests passed so CI was green while prod was 100% down. **Fix (`kolabing-v2` `EventSignupService::signup`):** lock the **event row** (`Event::whereKey()->lockForUpdate()->first()`) to serialize, then `count()` **without** a lock; dropped the lock off the `$existing` select too. Preventive: `BACKEND-SCHEMA.md` Golden Rule 7 (never lock an aggregate; validate on Postgres). Ticket: `docs/tickets/2026-06-10-backend-signup-500-postgres-lock.md`. | Fixed in code (`kolabing-v2`, uncommitted) — **pending push + deploy + Postgres re-verify** |
| FX-11 | **Communities paywalled / asked for premium when posting** — golden-rule violation (communities are never paywalled). | ✅ Fixed `4fcbef1` (role-gated `SubscriptionPaywall.checkAndShow`); 2026-06-10 also removed the now-redundant `checkAndShow` round-trip from `community_main_screen._onFabPressed` + its import. **Pending device re-verify:** community FAB → create flow opens, no premium sheet. |
| FX-12 | **Gallery & Past Events were a static placeholder** on the community Details tab. | Fixed 2026-06-10: added `communityPastEventsProvider` (`GET /events?community_id&time=past`) + wired `_DetailsTab` `_GallerySection` to a real photo strip + past-events list (i18n en/es/ca). `flutter analyze` clean (info-only). **Pending:** e2e verify on sim once a community has past events + `event_photos`. |
| FX-13 | **Creating a community blanked the leader's profile picture** — `POST /communities` created the community with `avatar_url = null`, so the community-profile surface read the blank primary community. | Fixed in code (`kolabing-v2` `CommunityService::create`, uncommitted): inherit `owner->communityProfile->profile_photo` when no avatar is supplied (mirrors `update()`'s same-image sync). **Pending push + deploy + verify.** |
| FX-14 | **Attendee sign-up / onboarding unreachable** — the Attendee card on `user_type_selection_screen` was hard-gated `isEnabled:false` + `COMING SOON` and `_handleCardTap` returned early, so tapping did nothing (route + screen + `AuthService.registerAttendee` all already existed). | Fixed 2026-06-10: card enabled + selectable; tap routes to `KolabingRoutes.attendeeRegister`. **Pending sim verify:** Sign up → Attendee → register → account created. ⚠️ Reconcile with `feat/nf16-events-attendee-app`'s richer onboarding. |
| FX-15 | **Attendee "not all chats appear available"** — NOT a data/sync bug. Tier-gating works as designed but the default tier shipped with `chat_channels: []` and `createCustomChat` never granted new chats to any tier, so non-manager members saw only the main chat. **Decision (Daniel 2026-06-10): default-open.** Fixed in code (`kolabing-v2` `ChatService::createCustomChat` + `grantChatToAllTiers`): a new ungated chat appends its slug to every tier so all members see it immediately; leader can restrict later via the Access picker. Diagnosis + decision: `docs/tickets/2026-06-10-chat-visibility-tier-gating-diagnosis.md`. | Fixed in code (`kolabing-v2`, uncommitted) — **pending push + deploy + verify** |
| FX-16 | **Member "View profile" shows "Unknown" + empty screen** — the public-profile stack is business/community-shaped; `PublicProfileResource` reads `display_name` from `attendeeProfile->name` which doesn't exist (attendee name lives on `profiles`), and the roster passes no optimistic name. Scoped as **Phase 0** of the member-hub work: `docs/tickets/2026-06-10-member-profile-hub-and-friends.md`. | Diagnosed + planned — Phase 0 fix not yet built (backend display_name fallback + app optimistic name + attendee layout) |
| FX-17 | **Active tab title invisible (yellow-on-yellow)** — `community_detail_screen` TabBar had no `labelColor`, so the selected tab inherited the yellow primary on the yellow app bar. | Fixed 2026-06-10: set `labelColor`/`indicatorColor` to `onSurface` (dark) + muted unselected. **Pending sim verify** (needs rebuild). |
| FX-18 | **"Going ✓ · tap to leave" RSVP button unreadable** — `event_hub_screen` `_rsvpButton` used the **deprecated** `KolabingColors.success` (dark olive `#56624D`) as bg with dark `onSurface` text → dark-on-dark. | Fixed 2026-06-10: going state now uses `activeBg` (light mint `#D4EDDA`) + `activeText` (dark green `#155724`), the non-deprecated active-state tokens. **Pending sim verify** (needs rebuild). |
| FX-19 | **Attendee onboarding: city not stored** — after completing onboarding the selected `city_id` isn't persisted (Edit Profile shows no/ wrong city). Likely the onboarding submit (`PUT /onboarding/attendee`) or the city field on the attendee/profile read-back. Reported by Daniel 2026-06-10. | Open — to fix |
| FX-20 | **Attendee onboarding: no "add profile picture" step/option** — the photo picker in the onboarding flow ("You" step) doesn't appear, so a new attendee can't set an avatar during onboarding. Reported 2026-06-10. | Fixed 2026-06-10 (90922d3): added explicit "Add a photo" tap label under the step1 avatar (picker already existed; likely a stale build). **Pending verify.** |
| FX-21 | **Attendee home "Nearby Events" controls low-contrast** — the `10 km` radius indicator (top-right of NEARBY EVENTS) and the "Adjust Radius" button are yellow/soft on a light bg → barely visible. Reported 2026-06-10. | ~~OBSOLETE 2026-08-22~~ — the attendee home was rebuilt as a city-scoped event feed with filter chips (`42ac9cf`); the radius control and the Points/Challenges/Events stat cards this described no longer exist on that screen. |
| FX-22 | **Attendee home stat icons off-brand** — the Points/Challenges/Events icons (star/target/calendar) don't match the new brand / the onboarding community-type icon set; should be replaced + aligned with the onboarding/community icons (and the new SVG-icon table once it lands). Reported 2026-06-10. | ~~OBSOLETE 2026-08-22~~ — the attendee home was rebuilt as a city-scoped event feed with filter chips (`42ac9cf`); the radius control and the Points/Challenges/Events stat cards this described no longer exist on that screen. |
| FX-23 | **No persistent "Explore communities" entry on the attendee home** — once an attendee has joined a community there's no visible button to browse/join more; needs an always-present "Explore communities" CTA (→ `discoverCommunitiesScreen`), not only the empty state. Reported 2026-06-10. | Open — to fix |
| FX-25 | **Event host-community tap opened mock business profile, not the real community** — `event_detail_screen._buildPartnerChild` pushed `/profile/$communityId` into `PublicProfileScreen` (profile-id-keyed), which fed a COMMUNITY id to `getPublicProfile` → fell back to mock "Kolabing Community / Istanbul / Past Kolabs". Reported by Daniel 2026-06-11 (`docs/tickets/2026-06-11-attendee-community-profile-contract.md`). | Fixed 2026-06-11: new community-keyed `AttendeeCommunityProfileScreen` + route `/community/:id/profile` (`KolabingRoutes.communityProfileById`) fetches the real community via `communityByIdProvider`→`CommunityService.getCommunity` + upcoming events via `communityUpcomingEventsProvider`. Header (brand band + avatar + name · dynamic 17-slug type label via new `communityTypeLabelProvider`, never the placeholder enum · member count), About, UPCOMING EVENTS strip (EventCard → `/event/:id`) with "See all →" → `CommunityDetailScreen(initialTabIndex: 1)` (Events sub-tab). State-aware sticky CTA from `Community.isMember`/`joinPolicy`/`myJoinRequestStatus` (new null-safe fields): open→Join (`joinCommunity`), invite_only→Request (`requestToJoin`, new self-gated POST `/communities/{id}/join-requests`, 404/405→soft notice), pending→"Requested" disabled, member→Open community. `_buildPartnerChild` repointed viewer-scoped: business viewer keeps `/profile/:id` (Send-Kolab), attendee/community gets the new screen. i18n en/es/ca (+14 keys), gen-l10n ok, `flutter analyze` **0 errors**. Self-gated: `is_member`/`my_join_request_status`/`join_policy` parse + the join-requests endpoint (backend in parallel). NOT committed. **Pending:** e2e once backend exposes the viewer fields + join-requests route. |
| FX-24 | **Discovered-event detail had no RSVP and no community link** — opening an event from Explore / the home events feed routes to `EventDetailScreen` (`/event/:id`, `isReadOnly` default), which had NO sign-up action and NO tap into the host community. Attendees could neither RSVP nor open the community. Reported by Daniel (release) 2026-06-11. | Fixed 2026-06-11: `event_detail_screen.dart` now (1) renders an RSVP button for `event.isUpcoming` (reuses event_hub's `_rsvpButton` pattern + `EventService.signup`/`cancelSignup` → `POST`/`DELETE /events/{id}/signup`, mint `activeBg`/`activeText` going state, waitlist-when-full + position line), holding a local mutable `_event` updated on toggle and reloading `communityUpcomingEventsProvider`; (2) makes the host partner row tappable → `context.push('/profile/$communityId')` when `event.communityId` is set (chevron affordance, inert when null). Detail fetch (`eventDetailProvider`→`getEvent`) is authoritative for signup state. i18n: reused `eventHub*` RSVP keys + new `eventDetailViewCommunity` (en/es/ca). `flutter gen-l10n` ok, analyze 0 errors. NOT committed. **Pending e2e verify** on device + confirm backend allows public/non-member signup. |
| FX-26 | **PostHog / Pods Xcode build warnings (cosmetic)** — release build (`flutter build ipa`) + Xcode show: `posthog_flutter-5.26.0/darwin/...` "no rule to process file" (a non-source file in the pod), plus several "[CP] Copy XCFrameworks / [CP] Pods Resources / [firebase_crashlytics] … will be run during every build because it does not specify any outputs" run-script warnings (OneSignalXCFramework, posthog, crashlytics). Reported by Daniel 2026-06-11 (Xcode 8 errors shown were the FX-27 rsync archive bug; these are the ⚠️ warnings). **Harmless** — the build/IPA succeed. Cleanup: silence the posthog "no rule" warning + add output paths to the offending CP run-script phases (or accept as upstream-pod noise). | Open — cosmetic, low priority |
| FX-29 | ~~**Chat "View opportunity" showed an APPLY NOW page in an accepted-application chat**~~ — `chat_screen._showOptionsMenu` → View opportunity routed **everyone** to the public offer detail (`CommunityOfferDetailScreen`), which renders an APPLY NOW CTA. Wrong in both directions: the opportunity **creator** can't apply to their own kolab (they want the counterparty's submission), and the **applicant** already applied (accepted). The payload's `has_applied`/`is_own` flags are unreliable here, and `user_id` vs `profile_id` live in different id-spaces so id comparison fails — so the decision uses **role match** (a chat is always one business + one community, so the viewer is the creator iff their role matches the creator's role; fallback: business=creator). Creator → `/application/:id` (`ApplicationReviewScreen` — counterparty's message/availability + accepted-status bar, no Apply/Accept/Decline); applicant → offer detail with new `canApply=false` flag (threaded via `?canApply=false` query param on the 3 offer routes) that hides the bottom APPLY NOW button outright. Extracted `chatViewOpportunityRoute` helper + unit test. Verified on iOS sim (dev) both directions. | ✅ Fixed 2026-06-21 (#34, branch `fix/chat-view-opportunity-creator`) |
| FX-30 | **"Yes, complete Kolab" → instant "Something went wrong"** (confirmed, Daniel) — the completion sheet POSTed `/complete` BEFORE any feedback, but the backend feedback gate only lets `/complete` pass once BOTH user types have POSTed `/feedback`. So `/complete` returned 422 (`awaiting_own_feedback`/`awaiting_partner_feedback`) which the provider rethrew as a generic exception → red `commonErrorGeneric`; feedback was also collected via the wrong `/review` endpoint (the gate was never satisfied → deadlock). **Fix (branch `fix/kolab-completion-flow`):** reordered to **feedback-first** — confirm → required feedback form (rating + expectation_match + would_recommend, all required; optional role-aware metrics: business posts_reels/stories_posted/revenue, community posts_reels/benefits) → `POST /feedback` (correct payload, role-resolved from the signed-in user; "already submitted" swallowed) → `POST /complete`. Provider now parses `error_code`+`message` into `CollaborationCompletionException` and the sheet branches: 200→celebration; `awaiting_partner_feedback`→**soft-success** "Thanks! Your feedback is in… completes once {partner} confirms" (not a red error); `cannot_complete`/`invalid_status_transition`→treat as already-completed + refresh; `awaiting_own_feedback`/other→keep form open with backend message. Pre-completion now states the mutual rule. Separate public `/review` flow untouched. i18n en/es/ca (+13 keys), gen-l10n ok, `flutter analyze` 0 new errors. NOT pushed. | Fixed in code 2026-06-22 — **pending device e2e** (both parties confirm → completed) |
| FX-28 | ~~**Community Dashboard "Something went wrong here" error card**~~ — after a session reset / re-login the dashboard's XP summary, referral and badges cards rendered the generic build-error fallback. Root cause (prod Sentry FLUTTER-12/13/11/10, iOS 18.7.3): `WalletNotifier` used `late final RewardsService _service;` **assigned inside `build()`**; Riverpod re-runs a Notifier's `build()` on the **same instance** when invalidated, and `session_reset.dart` invalidates `walletProvider` on every auth change → second build reassigns a `late final` → `LateInitializationError` → provider error state → every watcher rethrows `ProviderException`. Fix: replaced the `late final`+in-build assignment with a lazy getter (`get _service => ref.read(...)`, matching `DashboardNotifier`); applied the same fix to `GalleryNotifier` (same defect, same invalidation site). Regression test `wallet_provider_rebuild_test.dart` (invalidate→rebuild) fails on old code, passes on fix. | ✅ Fixed 2026-06-21 (#32, branch `fix/wallet-late-init-rebuild`) — **pending device re-verify** (re-login → community dashboard renders no error card) |
| FX-27 | **Xcode interactive Archive fails (rsync/dSYM) — OneSignal "[CP] Copy XCFrameworks"** — Product ▸ Archive in Xcode 16 errors with `rsync_sender`/`rsync_downloader`/`rsync_receiver` + `dSYMs/OneSignalExtension.framework.dSYM/… No such file or directory` (known Xcode-16 + CocoaPods framework-copy bug). Blocks the team's usual "upload through Xcode" flow. **Workaround used for the 1.5.0 release:** `flutter build ipa --release` (clean path, succeeds) then **distribute the resulting `build/ios/archive/Runner.xcarchive` from Xcode Organizer** (`open build/ios/archive/Runner.xcarchive`). Reported by Daniel 2026-06-11. | Open — workaround in place; proper fix = Podfile `post_install` build-setting tweak so ⌘-Archive works directly |

## Maintenance rules (for Claude — enforced via CLAUDE.md)

1. **Read this file at the start of every session** and list its current contents back to the user before starting work.
2. **Three sections only:** New Features, Incomplete Features, Fixes. Keep each item in the section that matches its true state.
3. **Move items as state changes:**
   - A New Feature that work has begun on → move to **Incomplete Features**.
   - An Incomplete Feature verified working end-to-end → remove it (or mark done and drop on next cleanup).
4. **Bugs:** when a bug is detected, add it to **Fixes** immediately. When the fix is **confirmed** (tested/verified, not just written), strike it through with the date, then remove on a later cleanup. Never delete an unconfirmed fix.
5. **One row = one item.** Keep notes terse; link to files/endpoints where useful.
6. Update the `Last updated:` date at the top whenever you change this file.
