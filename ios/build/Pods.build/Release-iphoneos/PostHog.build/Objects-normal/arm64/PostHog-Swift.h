// Generated by Apple Swift version 6.2.3 effective-5.10 (swiftlang-6.2.3.3.21 clang-1700.6.3.2)
#ifndef POSTHOG_SWIFT_H
#define POSTHOG_SWIFT_H
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wgcc-compat"

#if !defined(__has_include)
# define __has_include(x) 0
#endif
#if !defined(__has_attribute)
# define __has_attribute(x) 0
#endif
#if !defined(__has_feature)
# define __has_feature(x) 0
#endif
#if !defined(__has_warning)
# define __has_warning(x) 0
#endif

#if __has_include(<swift/objc-prologue.h>)
# include <swift/objc-prologue.h>
#endif

#pragma clang diagnostic ignored "-Wauto-import"
#if defined(__OBJC__)
#include <Foundation/Foundation.h>
#endif
#if defined(__cplusplus)
#include <cstdint>
#include <cstddef>
#include <cstdbool>
#include <cstring>
#include <stdlib.h>
#include <new>
#include <type_traits>
#else
#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>
#include <string.h>
#endif
#if defined(__cplusplus)
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wnon-modular-include-in-framework-module"
#if defined(__arm64e__) && __has_include(<ptrauth.h>)
# include <ptrauth.h>
#else
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wreserved-macro-identifier"
# ifndef __ptrauth_swift_value_witness_function_pointer
#  define __ptrauth_swift_value_witness_function_pointer(x)
# endif
# ifndef __ptrauth_swift_class_method_pointer
#  define __ptrauth_swift_class_method_pointer(x)
# endif
#pragma clang diagnostic pop
#endif
#pragma clang diagnostic pop
#endif

#if !defined(SWIFT_TYPEDEFS)
# define SWIFT_TYPEDEFS 1
# if __has_include(<uchar.h>)
#  include <uchar.h>
# elif !defined(__cplusplus)
typedef unsigned char char8_t;
typedef uint_least16_t char16_t;
typedef uint_least32_t char32_t;
# endif
typedef float swift_float2  __attribute__((__ext_vector_type__(2)));
typedef float swift_float3  __attribute__((__ext_vector_type__(3)));
typedef float swift_float4  __attribute__((__ext_vector_type__(4)));
typedef double swift_double2  __attribute__((__ext_vector_type__(2)));
typedef double swift_double3  __attribute__((__ext_vector_type__(3)));
typedef double swift_double4  __attribute__((__ext_vector_type__(4)));
typedef int swift_int2  __attribute__((__ext_vector_type__(2)));
typedef int swift_int3  __attribute__((__ext_vector_type__(3)));
typedef int swift_int4  __attribute__((__ext_vector_type__(4)));
typedef unsigned int swift_uint2  __attribute__((__ext_vector_type__(2)));
typedef unsigned int swift_uint3  __attribute__((__ext_vector_type__(3)));
typedef unsigned int swift_uint4  __attribute__((__ext_vector_type__(4)));
#endif

#if !defined(SWIFT_PASTE)
# define SWIFT_PASTE_HELPER(x, y) x##y
# define SWIFT_PASTE(x, y) SWIFT_PASTE_HELPER(x, y)
#endif
#if !defined(SWIFT_METATYPE)
# define SWIFT_METATYPE(X) Class
#endif
#if !defined(SWIFT_CLASS_PROPERTY)
# if __has_feature(objc_class_property)
#  define SWIFT_CLASS_PROPERTY(...) __VA_ARGS__
# else
#  define SWIFT_CLASS_PROPERTY(...) 
# endif
#endif
#if !defined(SWIFT_RUNTIME_NAME)
# if __has_attribute(objc_runtime_name)
#  define SWIFT_RUNTIME_NAME(X) __attribute__((objc_runtime_name(X)))
# else
#  define SWIFT_RUNTIME_NAME(X) 
# endif
#endif
#if !defined(SWIFT_COMPILE_NAME)
# if __has_attribute(swift_name)
#  define SWIFT_COMPILE_NAME(X) __attribute__((swift_name(X)))
# else
#  define SWIFT_COMPILE_NAME(X) 
# endif
#endif
#if !defined(SWIFT_METHOD_FAMILY)
# if __has_attribute(objc_method_family)
#  define SWIFT_METHOD_FAMILY(X) __attribute__((objc_method_family(X)))
# else
#  define SWIFT_METHOD_FAMILY(X) 
# endif
#endif
#if !defined(SWIFT_NOESCAPE)
# if __has_attribute(noescape)
#  define SWIFT_NOESCAPE __attribute__((noescape))
# else
#  define SWIFT_NOESCAPE 
# endif
#endif
#if !defined(SWIFT_RELEASES_ARGUMENT)
# if __has_attribute(ns_consumed)
#  define SWIFT_RELEASES_ARGUMENT __attribute__((ns_consumed))
# else
#  define SWIFT_RELEASES_ARGUMENT 
# endif
#endif
#if !defined(SWIFT_WARN_UNUSED_RESULT)
# if __has_attribute(warn_unused_result)
#  define SWIFT_WARN_UNUSED_RESULT __attribute__((warn_unused_result))
# else
#  define SWIFT_WARN_UNUSED_RESULT 
# endif
#endif
#if !defined(SWIFT_NORETURN)
# if __has_attribute(noreturn)
#  define SWIFT_NORETURN __attribute__((noreturn))
# else
#  define SWIFT_NORETURN 
# endif
#endif
#if !defined(SWIFT_CLASS_EXTRA)
# define SWIFT_CLASS_EXTRA 
#endif
#if !defined(SWIFT_PROTOCOL_EXTRA)
# define SWIFT_PROTOCOL_EXTRA 
#endif
#if !defined(SWIFT_ENUM_EXTRA)
# define SWIFT_ENUM_EXTRA 
#endif
#if !defined(SWIFT_CLASS)
# if __has_attribute(objc_subclassing_restricted)
#  define SWIFT_CLASS(SWIFT_NAME) SWIFT_RUNTIME_NAME(SWIFT_NAME) __attribute__((objc_subclassing_restricted)) SWIFT_CLASS_EXTRA
#  define SWIFT_CLASS_NAMED(SWIFT_NAME) __attribute__((objc_subclassing_restricted)) SWIFT_COMPILE_NAME(SWIFT_NAME) SWIFT_CLASS_EXTRA
# else
#  define SWIFT_CLASS(SWIFT_NAME) SWIFT_RUNTIME_NAME(SWIFT_NAME) SWIFT_CLASS_EXTRA
#  define SWIFT_CLASS_NAMED(SWIFT_NAME) SWIFT_COMPILE_NAME(SWIFT_NAME) SWIFT_CLASS_EXTRA
# endif
#endif
#if !defined(SWIFT_RESILIENT_CLASS)
# if __has_attribute(objc_class_stub)
#  define SWIFT_RESILIENT_CLASS(SWIFT_NAME) SWIFT_CLASS(SWIFT_NAME) __attribute__((objc_class_stub))
#  define SWIFT_RESILIENT_CLASS_NAMED(SWIFT_NAME) __attribute__((objc_class_stub)) SWIFT_CLASS_NAMED(SWIFT_NAME)
# else
#  define SWIFT_RESILIENT_CLASS(SWIFT_NAME) SWIFT_CLASS(SWIFT_NAME)
#  define SWIFT_RESILIENT_CLASS_NAMED(SWIFT_NAME) SWIFT_CLASS_NAMED(SWIFT_NAME)
# endif
#endif
#if !defined(SWIFT_PROTOCOL)
# define SWIFT_PROTOCOL(SWIFT_NAME) SWIFT_RUNTIME_NAME(SWIFT_NAME) SWIFT_PROTOCOL_EXTRA
# define SWIFT_PROTOCOL_NAMED(SWIFT_NAME) SWIFT_COMPILE_NAME(SWIFT_NAME) SWIFT_PROTOCOL_EXTRA
#endif
#if !defined(SWIFT_EXTENSION)
# define SWIFT_EXTENSION(M) SWIFT_PASTE(M##_Swift_, __LINE__)
#endif
#if !defined(OBJC_DESIGNATED_INITIALIZER)
# if __has_attribute(objc_designated_initializer)
#  define OBJC_DESIGNATED_INITIALIZER __attribute__((objc_designated_initializer))
# else
#  define OBJC_DESIGNATED_INITIALIZER 
# endif
#endif
#if !defined(SWIFT_ENUM_ATTR)
# if __has_attribute(enum_extensibility)
#  define SWIFT_ENUM_ATTR(_extensibility) __attribute__((enum_extensibility(_extensibility)))
# else
#  define SWIFT_ENUM_ATTR(_extensibility) 
# endif
#endif
#if !defined(SWIFT_ENUM)
# define SWIFT_ENUM(_type, _name, _extensibility) enum _name : _type _name; enum SWIFT_ENUM_ATTR(_extensibility) SWIFT_ENUM_EXTRA _name : _type
# if __has_feature(generalized_swift_name)
#  define SWIFT_ENUM_NAMED(_type, _name, SWIFT_NAME, _extensibility) enum _name : _type _name SWIFT_COMPILE_NAME(SWIFT_NAME); enum SWIFT_COMPILE_NAME(SWIFT_NAME) SWIFT_ENUM_ATTR(_extensibility) SWIFT_ENUM_EXTRA _name : _type
# else
#  define SWIFT_ENUM_NAMED(_type, _name, SWIFT_NAME, _extensibility) SWIFT_ENUM(_type, _name, _extensibility)
# endif
#endif
#if !defined(SWIFT_UNAVAILABLE)
# define SWIFT_UNAVAILABLE __attribute__((unavailable))
#endif
#if !defined(SWIFT_UNAVAILABLE_MSG)
# define SWIFT_UNAVAILABLE_MSG(msg) __attribute__((unavailable(msg)))
#endif
#if !defined(SWIFT_AVAILABILITY)
# define SWIFT_AVAILABILITY(plat, ...) __attribute__((availability(plat, __VA_ARGS__)))
#endif
#if !defined(SWIFT_WEAK_IMPORT)
# define SWIFT_WEAK_IMPORT __attribute__((weak_import))
#endif
#if !defined(SWIFT_DEPRECATED)
# define SWIFT_DEPRECATED __attribute__((deprecated))
#endif
#if !defined(SWIFT_DEPRECATED_MSG)
# define SWIFT_DEPRECATED_MSG(...) __attribute__((deprecated(__VA_ARGS__)))
#endif
#if !defined(SWIFT_DEPRECATED_OBJC)
# if __has_feature(attribute_diagnose_if_objc)
#  define SWIFT_DEPRECATED_OBJC(Msg) __attribute__((diagnose_if(1, Msg, "warning")))
# else
#  define SWIFT_DEPRECATED_OBJC(Msg) SWIFT_DEPRECATED_MSG(Msg)
# endif
#endif
#if defined(__OBJC__)
#if !defined(IBSegueAction)
# define IBSegueAction 
#endif
#endif
#if !defined(SWIFT_EXTERN)
# if defined(__cplusplus)
#  define SWIFT_EXTERN extern "C"
# else
#  define SWIFT_EXTERN extern
# endif
#endif
#if !defined(SWIFT_CALL)
# define SWIFT_CALL __attribute__((swiftcall))
#endif
#if !defined(SWIFT_INDIRECT_RESULT)
# define SWIFT_INDIRECT_RESULT __attribute__((swift_indirect_result))
#endif
#if !defined(SWIFT_CONTEXT)
# define SWIFT_CONTEXT __attribute__((swift_context))
#endif
#if !defined(SWIFT_ERROR_RESULT)
# define SWIFT_ERROR_RESULT __attribute__((swift_error_result))
#endif
#if defined(__cplusplus)
# define SWIFT_NOEXCEPT noexcept
#else
# define SWIFT_NOEXCEPT 
#endif
#if !defined(SWIFT_C_INLINE_THUNK)
# if __has_attribute(always_inline)
# if __has_attribute(nodebug)
#  define SWIFT_C_INLINE_THUNK inline __attribute__((always_inline)) __attribute__((nodebug))
# else
#  define SWIFT_C_INLINE_THUNK inline __attribute__((always_inline))
# endif
# else
#  define SWIFT_C_INLINE_THUNK inline
# endif
#endif
#if defined(_WIN32)
#if !defined(SWIFT_IMPORT_STDLIB_SYMBOL)
# define SWIFT_IMPORT_STDLIB_SYMBOL __declspec(dllimport)
#endif
#else
#if !defined(SWIFT_IMPORT_STDLIB_SYMBOL)
# define SWIFT_IMPORT_STDLIB_SYMBOL 
#endif
#endif
#if defined(__OBJC__)
#if __has_feature(objc_modules)
#if __has_warning("-Watimport-in-framework-header")
#pragma clang diagnostic ignored "-Watimport-in-framework-header"
#endif
@import CoreFoundation;
@import Foundation;
@import ObjectiveC;
@import UIKit;
#endif

#endif
#pragma clang diagnostic ignored "-Wproperty-attribute-mismatch"
#pragma clang diagnostic ignored "-Wduplicate-method-arg"
#if __has_warning("-Wpragma-clang-attribute")
# pragma clang diagnostic ignored "-Wpragma-clang-attribute"
#endif
#pragma clang diagnostic ignored "-Wunknown-pragmas"
#pragma clang diagnostic ignored "-Wnullability"
#pragma clang diagnostic ignored "-Wdollar-in-identifier-extension"
#pragma clang diagnostic ignored "-Wunsafe-buffer-usage"

#if __has_attribute(external_source_symbol)
# pragma push_macro("any")
# undef any
# pragma clang attribute push(__attribute__((external_source_symbol(language="Swift", defined_in="PostHog",generated_declaration))), apply_to=any(function,enum,objc_interface,objc_category,objc_protocol))
# pragma pop_macro("any")
#endif

#if defined(__OBJC__)

@class PostHogEvent;
/// ObjC wrapper for the events <code>beforeSend</code> block. Use with
/// <code>PostHogConfig.setBeforeSend(_:)</code>.
SWIFT_CLASS("_TtC7PostHog20BoxedBeforeSendBlock")
@interface BoxedBeforeSendBlock : NSObject
/// Wrapped event callback.
@property (nonatomic, readonly, copy) PostHogEvent * _Nullable (^ _Nonnull block)(PostHogEvent * _Nonnull);
/// Creates a boxed event callback for Objective-C callers.
/// \param block Callback that can mutate or drop an event.
///
- (nonnull instancetype)block:(PostHogEvent * _Nullable (^ _Nonnull)(PostHogEvent * _Nonnull))block OBJC_DESIGNATED_INITIALIZER SWIFT_METHOD_FAMILY(init);
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

@class PostHogLogRecord;
/// ObjC wrapper for the logs <code>beforeSend</code> block. Use with
/// <code>PostHogLogsConfig.setBeforeSend(_:)</code>.
SWIFT_CLASS("_TtC7PostHog23BoxedBeforeSendLogBlock")
@interface BoxedBeforeSendLogBlock : NSObject
/// Wrapped log callback.
@property (nonatomic, readonly, copy) PostHogLogRecord * _Nullable (^ _Nonnull block)(PostHogLogRecord * _Nonnull);
/// Creates a boxed log callback for Objective-C callers.
/// \param block Callback that can mutate or drop a log record.
///
- (nonnull instancetype)block:(PostHogLogRecord * _Nullable (^ _Nonnull)(PostHogLogRecord * _Nonnull))block OBJC_DESIGNATED_INITIALIZER SWIFT_METHOD_FAMILY(init);
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

@class NSURL;
@class NSString;
enum PostHogDataMode : NSInteger;
@class PostHogRageClickConfig;
@class NSUUID;
@protocol PostHogPropertiesSanitizer;
enum PostHogPersonProfiles : NSInteger;
@class PostHogSessionReplayConfig;
@class PostHogErrorTrackingConfig;
@class PostHogLogsConfig;
@class PostHogSurveysConfig;
@class NSURLSessionConfiguration;
/// Runtime configuration for a <code>PostHogSDK</code> instance.
/// Create a config with your project token, mutate any options you need, then pass it to
/// <code>PostHogSDK.shared.setup(_:)</code> or <code>PostHogSDK.with(_:)</code>. Options that control queues,
/// integrations, or resource attributes should be set before setup; later mutations may not
/// affect already-installed SDK components.
SWIFT_CLASS_NAMED("PostHogConfig")
@interface PostHogConfig : NSObject
/// PostHog ingestion host used for all SDK network requests.
/// Defaults to <code>PostHogConfig.defaultHost</code> when the initializer host is empty or invalid.
@property (nonatomic, readonly, copy) NSURL * _Nonnull host;
/// Your PostHog project token.
/// You can find it at:
/// https://us.posthog.com/settings/project-details#variables
/// This field was formerly named <code>apiKey</code>.
@property (nonatomic, readonly, copy) NSString * _Nonnull projectToken;
/// Obsolete alias for <code>projectToken</code>.
@property (nonatomic, readonly, copy) NSString * _Nonnull apiKey SWIFT_DEPRECATED_MSG("Use projectToken instead. This will be removed in the next major version.");
/// Number of queued events that triggers an automatic flush.
/// Lower values send data sooner but can increase battery and network usage.
/// Default: <code>20</code> (<code>5</code> on tvOS).
@property (nonatomic) NSInteger flushAt;
/// Maximum number of events kept in the on-disk queue before older events are dropped.
/// Default: <code>1000</code> (<code>100</code> on tvOS).
@property (nonatomic) NSInteger maxQueueSize;
/// Maximum number of events included in a single batch request.
/// Default: <code>50</code>.
@property (nonatomic) NSInteger maxBatchSize;
/// Interval, in seconds, between periodic queue flush checks.
/// Lower values deliver events closer to real time but can increase battery usage.
/// Default: <code>30</code>.
@property (nonatomic) NSTimeInterval flushIntervalSeconds;
/// Maximum number of consecutive flush attempts before the entire queue is
/// dropped to avoid infinite retries against a permanently-broken backend.
/// Increments on every retriable failure including HTTP 413 cap halving;
/// resets on a successful 2xx response. Default 3.
@property (nonatomic) NSInteger maxRetries;
/// Required network connectivity mode for flushing queued data.
/// Only <code>.wifi</code> currently restricts flushing; <code>.cellular</code> behaves the same as <code>.any</code>.
/// Default: <code>.any</code>.
@property (nonatomic) enum PostHogDataMode dataMode;
/// Whether feature flag lookups automatically capture <code>$feature_flag_called</code> events.
/// Individual lookup calls can override this value with their <code>sendFeatureFlagEvent</code> parameter.
/// Default: <code>true</code>.
@property (nonatomic) BOOL sendFeatureFlagEvent;
/// Whether feature flags are loaded automatically during SDK setup.
/// Default: <code>true</code>.
@property (nonatomic) BOOL preloadFeatureFlags;
/// Deprecated no-op for remote config loading.
/// Remote config is now always loaded; setting this property has no effect.
/// <ul>
///   <li>
///     Deprecated: Remote config is always loaded. This option will be removed in a future version.
///   </li>
/// </ul>
@property (nonatomic) BOOL remoteConfig SWIFT_DEPRECATED_MSG("Remote config is now always loaded. This option is a no-op and will be removed in a future version.");
/// Whether the SDK automatically captures application lifecycle events.
/// When enabled, the SDK records events such as <code>Application Installed</code>,
/// <code>Application Updated</code>, <code>Application Opened</code>, and background/foreground transitions.
/// Default: <code>true</code>.
@property (nonatomic) BOOL captureApplicationLifecycleEvents;
/// Automatically captures a <code>$screen</code> event whenever a <code>UIViewController</code> appears
/// (via <code>viewDidAppear</code> swizzling).
/// <code>$screen_name</code> stamping on subsequent events is a related effect: any
/// successful <code>screen()</code> call — whether fired by this auto-capture path <em>or
/// invoked manually</em> via <code>PostHogSDK.shared.screen(...)</code> — caches the screen
/// name so it lands as <code>$screen_name</code> on every later event the SDK captures.
/// To opt out of <code>$screen_name</code> stamping entirely, set this to <code>false</code> <em>and</em>
/// avoid calling <code>screen(...)</code> manually.
/// Default: <code>true</code>
@property (nonatomic) BOOL captureScreenViews;
/// Enable method swizzling for SDK functionality that depends on it
/// When disabled, functionality that require swizzling (like autocapture, screen views, session replay, surveys) will not be installed.
/// Note: Disabling swizzling will limit session rotation logic to only detect application open and background events.
/// Session rotation will still work, just with reduced granularity for detecting user activity.
/// Default: true
@property (nonatomic) BOOL enableSwizzling;
/// Enables UIKit element interaction autocapture on iOS and Mac Catalyst.
/// Requires <code>enableSwizzling = true</code>.
/// Default: <code>false</code>.
@property (nonatomic) BOOL captureElementInteractions;
/// Rage click detection configuration.
@property (nonatomic, readonly, strong) PostHogRageClickConfig * _Nonnull rageClickConfig;
/// Enables verbose SDK diagnostic logging.
/// Default: <code>false</code>.
@property (nonatomic) BOOL debug;
/// Starts the SDK in an opted-out state when set before setup.
/// While opted out, capture calls are ignored and integrations are not installed.
/// Use <code>PostHogSDK.optIn()</code> and <code>PostHogSDK.optOut()</code> to change the persisted state at runtime.
/// Default: <code>false</code>.
@property (nonatomic) BOOL optOut;
/// Hook used to customize newly generated anonymous IDs.
/// The SDK passes its generated UUID v7 and stores the UUID returned by this closure.
/// Existing stored anonymous IDs are not regenerated.
/// \param uuid The SDK-generated anonymous UUID.
///
///
/// returns:
/// The UUID to persist as the anonymous ID.
@property (nonatomic, copy) NSUUID * _Nonnull (^ _Nonnull getAnonymousId)(NSUUID * _Nonnull);
/// Flag to reuse the anonymous Id between <code>reset()</code> and next <code>identify()</code> calls
/// If enabled, the anonymous Id will be reused for all anonymous users on this device,
/// essentially creating a “Guest user Id” as long as this option is enabled.
/// Note:
/// Events captured <em>before</em> call to <em>identify()</em> won’t be linked to the identified user
/// Events captured <em>after</em>  call to <em>reset()</em> won’t be linked to the identified user
/// Defaults to false.
@property (nonatomic) BOOL reuseAnonymousId;
/// Hook that allows to sanitize the event properties
/// The hook is called before the event is cached or sent over the wire
@property (nonatomic, strong) id <PostHogPropertiesSanitizer> _Nullable propertiesSanitizer SWIFT_DEPRECATED_MSG("Use beforeSend instead");
/// Determines the behavior for processing user profiles.
@property (nonatomic) enum PostHogPersonProfiles personProfiles;
/// Automatically set common device and app properties as person properties for feature flag evaluation.
/// When enabled, the SDK will automatically set the following person properties:
/// <ul>
///   <li>
///     $app_version: App version from bundle
///   </li>
///   <li>
///     $app_build: App build number from bundle
///   </li>
///   <li>
///     $app_namespace: App bundle identifier
///   </li>
///   <li>
///     $os_name: Operating system name (iOS, macOS, etc.)
///   </li>
///   <li>
///     $os_version: Operating system version
///   </li>
///   <li>
///     $device_type: Device type (Mobile, Tablet, Desktop, etc.)
///   </li>
///   <li>
///     $lib: SDK name
///   </li>
///   <li>
///     $lib_version: SDK version
///   </li>
/// </ul>
/// This helps ensure feature flags that rely on these properties work correctly
/// without waiting for server-side processing of identify() calls.
/// Default: true
@property (nonatomic) BOOL setDefaultPersonProperties;
/// Evaluation contexts for feature flags.
/// When configured, only feature flags that have at least one matching evaluation tag
/// will be evaluated. Feature flags with no evaluation tags will always be evaluated
/// for backward compatibility.
/// Example usage:
/// \code
/// config.evaluationContexts = ["production", "web", "checkout"]
///
/// \endcodeThis helps ensure feature flags are only evaluated in the appropriate contexts
/// for your SDK instance.
/// Default: nil (all flags are evaluated)
@property (nonatomic, copy) NSArray<NSString *> * _Nullable evaluationContexts;
/// Deprecated alias for <code>evaluationContexts</code>.
/// <ul>
///   <li>
///     Deprecated: Use <code>evaluationContexts</code> instead. This property will be removed in a future version.
///   </li>
/// </ul>
@property (nonatomic, copy) NSArray<NSString *> * _Nullable evaluationEnvironments SWIFT_DEPRECATED_MSG("Use evaluationContexts instead. This property will continue to work but will be removed in a future version.");
/// The identifier of the App Group that should be used to store shared analytics data.
/// PostHog will try to get the physical location of the App Group’s shared container, otherwise fallback to the default location
/// Default: nil
@property (nonatomic, copy) NSString * _Nullable appGroupIdentifier;
/// Session replay snapshot endpoint path.
/// warning:
/// This value is managed by the SDK from remote configuration and should not
/// be changed by application code.
@property (nonatomic, copy) NSString * _Nonnull snapshotEndpoint;
/// When set, PostHog injects tracing headers into <code>URLSession</code> requests whose
/// destination hostname exactly matches one of the configured hostnames.
/// Injected headers on iOS:
/// <ul>
///   <li>
///     <code>X-POSTHOG-DISTINCT-ID</code>
///   </li>
///   <li>
///     <code>X-POSTHOG-SESSION-ID</code>
///   </li>
/// </ul>
/// Notes:
/// requires:
/// <code>enableSwizzling = true</code>
/// <ul>
///   <li>
///     Hostname matching is exact and does not include ports or subdomain wildcards
///   </li>
///   <li>
///     iOS does not send <code>X-POSTHOG-WINDOW-ID</code> because mobile apps do not have a per-window/tab concept
///   </li>
///   <li>
///     Existing values for these headers will be overwritten
///   </li>
/// </ul>
@property (nonatomic, copy) NSArray<NSString *> * _Nullable tracingHeaders;
/// Enable Recording of Session Replays for iOS
/// Note: Ingestion controls (sampling, feature flags, and event triggers) are currently applied using AND logic.
/// All configured conditions must be satisfied for recording to start.
/// Default: false
@property (nonatomic) BOOL sessionReplay;
/// Session Replay configuration
@property (nonatomic, readonly, strong) PostHogSessionReplayConfig * _Nonnull sessionReplayConfig;
/// Configuration for error tracking.
/// See known limitations: https://posthog.com/docs/error-tracking/installation/ios#limitations
@property (nonatomic, readonly, strong) PostHogErrorTrackingConfig * _Nonnull errorTrackingConfig;
/// Configuration for the logs subsystem (manual <code>captureLog</code> capture).
/// Mutate fields on <code>config.logs</code> before calling <code>PostHogSDK.setup(_:)</code>.
@property (nonatomic, readonly, strong) PostHogLogsConfig * _Nonnull logs;
/// Enable mobile surveys
/// Default: true
/// Note: Event triggers will only work with the instance that first enables surveys.
/// In case of multiple instances, please make sure you are capturing events on the instance that has config.surveys = true
@property (nonatomic) BOOL surveys SWIFT_AVAILABILITY(visionos,unavailable,message="Surveys are only available on iOS 15+") SWIFT_AVAILABILITY(tvos,unavailable,message="Surveys are only available on iOS 15+") SWIFT_AVAILABILITY(macos,unavailable,message="Surveys are only available on iOS 15+") SWIFT_AVAILABILITY(watchos,unavailable,message="Surveys are only available on iOS 15+") SWIFT_AVAILABILITY(ios,introduced=15.0);
/// Configuration for mobile survey presentation and localization.
/// Mutate fields on <code>config.surveysConfig</code> or replace this object before calling setup.
/// Available on iOS 15 and later.
@property (nonatomic, strong) PostHogSurveysConfig * _Nonnull surveysConfig SWIFT_AVAILABILITY(visionos,unavailable,message="Surveys are only available on iOS 15+") SWIFT_AVAILABILITY(tvos,unavailable,message="Surveys are only available on iOS 15+") SWIFT_AVAILABILITY(macos,unavailable,message="Surveys are only available on iOS 15+") SWIFT_AVAILABILITY(watchos,unavailable,message="Surveys are only available on iOS 15+") SWIFT_AVAILABILITY(ios,introduced=15.0);
/// Optional custom URLSessionConfiguration for network requests
/// If not set, uses URLSessionConfiguration.default
/// Useful for testing, proxying, or custom network configurations
@property (nonatomic, strong) NSURLSessionConfiguration * _Nullable urlSessionConfiguration;
/// Creates a configuration using the default PostHog host.
/// \param projectToken Your PostHog project token. Leading and trailing whitespace is trimmed.
///
- (nonnull instancetype)projectToken:(NSString * _Nonnull)projectToken OBJC_DESIGNATED_INITIALIZER SWIFT_METHOD_FAMILY(init);
/// Creates a configuration with an explicit PostHog ingestion host.
/// \param projectToken Your PostHog project token. Leading and trailing whitespace is trimmed.
///
/// \param host PostHog ingestion host, for example <code>"https://us.i.posthog.com"</code> or
/// <code>"https://eu.i.posthog.com"</code>. Empty or invalid values fall back to <code>defaultHost</code>.
///
- (nonnull instancetype)projectToken:(NSString * _Nonnull)projectToken host:(NSString * _Nonnull)host OBJC_DESIGNATED_INITIALIZER SWIFT_METHOD_FAMILY(init);
/// Creates a configuration using the deprecated <code>apiKey</code> name.
/// <ul>
///   <li>
///     Deprecated: Use <code>init(projectToken:)</code> instead.
///   </li>
/// </ul>
/// \param apiKey Your PostHog project token.
///
- (nonnull instancetype)apiKey:(NSString * _Nonnull)apiKey SWIFT_METHOD_FAMILY(init) SWIFT_DEPRECATED_MSG("Use init(projectToken:) instead. This will be removed in the next major version.");
/// Creates a configuration using the deprecated <code>apiKey</code> name and an explicit host.
/// <ul>
///   <li>
///     Deprecated: Use <code>init(projectToken:host:)</code> instead.
///   </li>
/// </ul>
/// \param apiKey Your PostHog project token.
///
/// \param host PostHog ingestion host. Empty or invalid values fall back to <code>defaultHost</code>.
///
- (nonnull instancetype)apiKey:(NSString * _Nonnull)apiKey host:(NSString * _Nonnull)host SWIFT_METHOD_FAMILY(init) SWIFT_DEPRECATED_MSG("Use init(projectToken:host:) instead. This will be removed in the next major version.");
/// Replaces the event <code>beforeSend</code> chain from Objective-C boxed callbacks.
/// \param blocks Ordered Objective-C callback boxes.
///
- (void)setBeforeSend:(NSArray<BoxedBeforeSendBlock *> * _Nonnull)blocks;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// Network connectivity mode required before queued data may be flushed.
typedef SWIFT_ENUM_NAMED(NSInteger, PostHogDataMode, "PostHogDataMode", closed) {
/// Flush only while the device is connected to Wi-Fi.
  PostHogDataModeWifi = 0,
/// Legacy cellular mode.
/// Currently behaves the same as <code>.any</code>; only <code>.wifi</code> applies a stricter flush restriction.
  PostHogDataModeCellular = 1,
/// Flush while any network connection is available.
  PostHogDataModeAny = 2,
};

enum PostHogDisplaySurveyTextContentType : NSInteger;
/// Base class for all survey question types
SWIFT_CLASS("_TtC7PostHog28PostHogDisplaySurveyQuestion")
@interface PostHogDisplaySurveyQuestion : NSObject
/// The question ID, empty if none
@property (nonatomic, readonly, copy) NSString * _Nonnull id;
/// The main question text to display
@property (nonatomic, readonly, copy) NSString * _Nonnull question;
/// Optional additional description or context for the question
@property (nonatomic, readonly, copy) NSString * _Nullable questionDescription;
/// Content type for the question description (HTML or plain text)
@property (nonatomic, readonly) enum PostHogDisplaySurveyTextContentType questionDescriptionContentType;
/// Whether the question can be skipped
@property (nonatomic, readonly) BOOL isOptional;
/// Optional custom text for the question’s action button
@property (nonatomic, readonly, copy) NSString * _Nullable buttonText;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// Represents a multiple or single choice question where users can select one or more options
SWIFT_CLASS("_TtC7PostHog28PostHogDisplayChoiceQuestion")
@interface PostHogDisplayChoiceQuestion : PostHogDisplaySurveyQuestion
@end

/// Represents a question with a clickable link
SWIFT_CLASS("_TtC7PostHog26PostHogDisplayLinkQuestion")
@interface PostHogDisplayLinkQuestion : PostHogDisplaySurveyQuestion
@end

/// Represents an open-ended question where users can input free-form text
SWIFT_CLASS("_TtC7PostHog26PostHogDisplayOpenQuestion")
@interface PostHogDisplayOpenQuestion : PostHogDisplaySurveyQuestion
@end

/// Represents a rating question where users can select a rating from a scale
SWIFT_CLASS("_TtC7PostHog28PostHogDisplayRatingQuestion")
@interface PostHogDisplayRatingQuestion : PostHogDisplaySurveyQuestion
@end

/// A model representing a PostHog survey to be displayed to users
SWIFT_CLASS("_TtC7PostHog20PostHogDisplaySurvey")
@interface PostHogDisplaySurvey : NSObject
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// Model that describes the appearance customization of a PostHog survey
SWIFT_CLASS("_TtC7PostHog30PostHogDisplaySurveyAppearance")
@interface PostHogDisplaySurveyAppearance : NSObject
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// Type of rating display for survey rating questions
typedef SWIFT_ENUM(NSInteger, PostHogDisplaySurveyRatingType, closed) {
/// Display numeric rating options
  PostHogDisplaySurveyRatingTypeNumber = 0,
/// Display emoji rating options
  PostHogDisplaySurveyRatingTypeEmoji = 1,
};

/// Content type for text-based survey elements
typedef SWIFT_ENUM(NSInteger, PostHogDisplaySurveyTextContentType, closed) {
/// Content should be rendered as HTML
  PostHogDisplaySurveyTextContentTypeHtml = 0,
/// Content should be rendered as plain text
  PostHogDisplaySurveyTextContentTypeText = 1,
};

/// Configuration for error tracking and exception capture.
/// This class controls how exceptions are captured and processed,
/// including which stack trace frames are marked as “in-app” code.
SWIFT_CLASS("_TtC7PostHog26PostHogErrorTrackingConfig")
@interface PostHogErrorTrackingConfig : NSObject
/// Whether crash autocapture is enabled.
/// When enabled, fatal crashes are persisted and sent as <code>$exception</code> events on the next launch.
/// Default: <code>false</code>.
@property (nonatomic) BOOL autoCapture SWIFT_AVAILABILITY(visionos,unavailable,message="Crash autocapture is not available on visionOS") SWIFT_AVAILABILITY(watchos,unavailable,message="Crash autocapture is not available on watchOS");
/// List of package/bundle identifiers to be considered in-app frames
/// Takes precedence over <code>inAppExcludes</code>.
/// If a frame’s module matches any prefix in this list,
/// it will be marked as in-app.
/// Example:
/// \code
/// config.errorTrackingConfig.inAppIncludes = [
///     "MyApp",
///     "SharedUtils"
/// ]
///
/// \endcode<em>Default behavior:</em>
/// <ul>
///   <li>
///     Automatically includes main bundle identifier
///   </li>
///   <li>
///     Automatically includes executable name
///   </li>
/// </ul>
/// <em>Precedence:</em> Priority 1 (highest)
@property (nonatomic, copy) NSArray<NSString *> * _Nonnull inAppIncludes;
/// List of package/bundle identifiers to be excluded from in-app frames
/// Frames matching these prefixes will be marked as not in-app,
/// unless they also match <code>inAppIncludes</code> (which takes precedence).
/// Example:
/// \code
/// config.errorTrackingConfig.inAppExcludes = [
///     "Alamofire",
///     "SDWebImage"
/// ]
///
/// \endcode<em>Precedence:</em> Priority 2 (after inAppIncludes)
@property (nonatomic, copy) NSArray<NSString *> * _Nonnull inAppExcludes;
/// Configures whether stack trace frames are considered in-app by default
/// when the origin cannot be determined or no explicit includes/excludes match.
/// <ul>
///   <li>
///     If <code>true</code> (default): Frames are in-app unless explicitly excluded (allowlist approach)
///   </li>
///   <li>
///     If <code>false</code>: Frames are external unless explicitly included (denylist approach)
///   </li>
/// </ul>
/// <em>Default behavior when true:</em>
/// <ul>
///   <li>
///     Known system frameworks (Foundation, UIKit, etc.) are excluded
///   </li>
///   <li>
///     All other packages are in-app unless in <code>inAppExcludes</code>
///   </li>
/// </ul>
/// <em>Precedence:</em> Priority 4 (final fallback)
/// Default: true
@property (nonatomic) BOOL inAppByDefault;
/// Creates an error tracking configuration with default in-app frame detection.
- (nonnull instancetype)init OBJC_DESIGNATED_INITIALIZER;
@end

@class NSDate;
/// Event object passed through <code>BeforeSendBlock</code> before it is queued.
/// Mutate fields in your before-send callback to redact or enrich the event, or return <code>nil</code>
/// from the callback to drop it. Instances are created by the SDK; application code normally
/// captures events through <code>PostHogSDK.capture</code> instead of constructing this type.
SWIFT_CLASS_NAMED("PostHogEvent")
@interface PostHogEvent : NSObject
/// Event name, such as <code>"user_signed_up"</code> or <code>"$screen"</code>.
@property (nonatomic, copy) NSString * _Nonnull event;
/// Distinct ID associated with the event.
@property (nonatomic, copy) NSString * _Nonnull distinctId;
/// Event properties that will be serialized with the event.
@property (nonatomic, copy) NSDictionary<NSString *, id> * _Nonnull properties;
/// Event timestamp.
@property (nonatomic, copy) NSDate * _Nonnull timestamp;
/// Unique event identifier generated by the SDK.
@property (nonatomic, readonly, copy) NSUUID * _Nonnull uuid;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// Result object containing all information about a feature flag evaluation.
/// This class provides access to the flag’s enabled state, variant, and payload in a
/// single convenient object.
/// <h2>Example Usage</h2>
/// \code
/// if let result = PostHogSDK.shared.getFeatureFlagResult("my-feature") {
///     if result.enabled {
///         // Feature is enabled
///         if let variant = result.variant {
///             // Handle specific variant
///             print("Variant: \(variant)")
///         }
///         if let payload = result.payload as? [String: Any] {
///             // Use the payload data
///         }
///     }
/// }
///
/// \endcode
SWIFT_CLASS_NAMED("PostHogFeatureFlagResult")
@interface PostHogFeatureFlagResult : NSObject
/// The key of the feature flag.
@property (nonatomic, readonly, copy) NSString * _Nonnull key;
/// Whether the feature flag is enabled.
/// This is <code>true</code> if the flag value is:
/// <ul>
///   <li>
///     A boolean <code>true</code>
///   </li>
///   <li>
///     A non-null string (variant)
///   </li>
/// </ul>
@property (nonatomic, readonly) BOOL enabled;
/// The variant name if this is a multivariate flag, otherwise <code>nil</code>.
@property (nonatomic, readonly, copy) NSString * _Nullable variant;
/// The payload associated with this feature flag, if any.
/// The payload can be any JSON-serializable value (string, number, boolean, array, or dictionary).
@property (nonatomic, readonly) id _Nullable payload;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

enum PostHogLogLevel : NSInteger;
/// A model representing a processed console log entry for session replay.
/// Describes a single console log entry after it has been processed by <code>PostHogSessionReplayConsoleLogConfig.logSanitizer</code>.
/// Each instance contains the log message content and its determined severity level.
SWIFT_CLASS("_TtC7PostHog15PostHogLogEntry")
@interface PostHogLogEntry : NSObject
/// The severity level of the log entry.
/// This determines how the log will be displayed in the session replay and
/// whether it will be captured based on <code>minLogLevel</code> setting.
@property (nonatomic, readonly) enum PostHogLogLevel level;
/// The actual content of the log message.
/// This is the processed and sanitized log message
@property (nonatomic, readonly, copy) NSString * _Nonnull message;
/// Creates a new console log entry.
/// \param level The severity level of the log entry
///
/// \param message The processed log message content
///
- (nonnull instancetype)initWithLevel:(enum PostHogLogLevel)level message:(NSString * _Nonnull)message OBJC_DESIGNATED_INITIALIZER;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// The severity level of a console log entry.
/// Used to categorize logs by their severity in session replay.
typedef SWIFT_ENUM(NSInteger, PostHogLogLevel, closed) {
/// Informational messages, debugging output, and general logs
  PostHogLogLevelInfo = 0,
/// Warning messages indicating potential issues or deprecation notices
  PostHogLogLevelWarn = 1,
/// Error messages indicating failures or critical issues
  PostHogLogLevelError = 2,
};

enum PostHogLogSeverity : NSInteger;
@class NSNumber;
/// A captured log entry passed to your <code>beforeSend</code> callback. Mutate the
/// fields below to redact, enrich, or rewrite the record; return <code>nil</code> to
/// drop it.
/// Instances are created by the SDK at capture time. Mutations made after the
/// callback returns have no effect — the record is encoded into the on-disk
/// queue as soon as <code>beforeSend</code> finishes.
SWIFT_CLASS("_TtC7PostHog16PostHogLogRecord")
@interface PostHogLogRecord : NSObject
/// The log message body. Required; empty bodies are dropped at capture time.
@property (nonatomic, copy) NSString * _Nonnull body;
/// Severity of the log entry. Reassign to re-classify (e.g. demote a
/// noisy <code>.warn</code> to <code>.debug</code>).
@property (nonatomic) enum PostHogLogSeverity level;
/// Free-form attributes attached to this record. Values must be
/// JSON-serializable (<code>String</code>, <code>Int</code>, <code>Double</code>, <code>Bool</code>, arrays/dicts of
/// the same, <code>NSNumber</code>); anything else is silently dropped at flush
/// time. Use this to add structured context (request ids, build numbers,
/// experiment buckets) or to remove keys you don’t want sent.
/// <em>From ObjC</em>: bridges to an immutable <code>NSDictionary</code>. To mutate, take
/// a <code>mutableCopy</code>, edit it, and assign back to the property — in-place
/// mutation on the returned dictionary will fail.
@property (nonatomic, copy) NSDictionary<NSString *, id> * _Nonnull attributes;
/// W3C trace id (32 hex characters) if you’ve correlated this log with a
/// distributed trace. <code>nil</code> when no trace context is associated. Set or
/// rewrite for tracing integrations.
@property (nonatomic, copy) NSString * _Nullable traceId;
/// W3C span id (16 hex characters) if this log belongs to a span. <code>nil</code>
/// when no span is active.
@property (nonatomic, copy) NSString * _Nullable spanId;
/// W3C trace flags. Lower 8 bits are the bitfield; bit 0 is the
/// <code>sampled</code> flag. <code>nil</code> omits the field on the wire; <code>0</code> explicitly
/// emits zero.
@property (nonatomic, strong) NSNumber * _Nullable traceFlags;
/// PostHog distinct id of the user at capture time, or <code>nil</code> if no user
/// is identified. Set to <code>nil</code> (or to a hash) to redact.
@property (nonatomic, copy) NSString * _Nullable distinctId;
/// PostHog session id at capture time, or <code>nil</code> if no session is active.
/// Set to <code>nil</code> to disassociate the record from the session.
@property (nonatomic, copy) NSString * _Nullable sessionId;
/// Last screen name observed by automatic screen-view tracking, or <code>nil</code>
/// if screen tracking is disabled or no screen has been seen yet. Set to
/// <code>nil</code> (or rewrite) to redact navigation context.
@property (nonatomic, copy) NSString * _Nullable screenName;
/// Feature flag keys that were active at capture time. Empty when no
/// flags are loaded. Useful for correlating logs with experiment
/// cohorts; clear or filter to redact.
/// <em>From ObjC</em>: bridges to an immutable <code>NSArray</code>. Replace the
/// property to mutate; in-place mutation will fail.
@property (nonatomic, copy) NSArray<NSString *> * _Nonnull featureFlagKeys;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// Severity level for a captured log record sent to PostHog’s logs ingestion.
/// Cases are declared in severity order so <code>>=</code> comparisons on the raw value
/// give the expected result from both Swift and ObjC. ObjC consumers can
/// safely write <code>if (severity >= PostHogLogSeverityWarn) { ... }</code> when <code>severity</code>
/// is a <code>PostHogLogSeverity</code> value.
/// Maps to OpenTelemetry severity numbers (<code>TRACE=1</code>, <code>DEBUG=5</code>, <code>INFO=9</code>,
/// <code>WARN=13</code>, <code>ERROR=17</code>, <code>FATAL=21</code>) for the OTLP wire format.
typedef SWIFT_ENUM(NSInteger, PostHogLogSeverity, closed) {
/// Finest-grained tracing detail.
  PostHogLogSeverityTrace = 0,
/// Diagnostic information useful while debugging.
  PostHogLogSeverityDebug = 1,
/// Informational messages and general logs.
  PostHogLogSeverityInfo = 2,
/// Warning messages indicating potential issues or deprecation notices.
  PostHogLogSeverityWarn = 3,
/// Error messages indicating failures or critical issues.
  PostHogLogSeverityError = 4,
/// An unrecoverable failure; the app likely cannot continue.
  PostHogLogSeverityFatal = 5,
};

/// Convenience facade for capturing log records. Use <code>PostHogSDK.shared.logger</code>
/// to call <code>trace</code>, <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>, or <code>fatal</code>.
SWIFT_CLASS("_TtC7PostHog13PostHogLogger")
@interface PostHogLogger : NSObject
/// Capture a <code>.trace</code> record. Finest-grained detail; usually only enabled
/// while diagnosing.
/// \param body The log message. Required and non-empty.
///
- (void)traceWithBody:(NSString * _Nonnull)body;
/// Capture a <code>.trace</code> record with structured attributes.
/// \param body The log message. Required and non-empty.
///
/// \param attributes Per-record attributes. Values must be JSON-serializable.
///
- (void)traceWithBody:(NSString * _Nonnull)body attributes:(NSDictionary<NSString *, id> * _Nullable)attributes;
/// Capture a <code>.debug</code> record. Diagnostic detail useful during development.
/// \param body The log message. Required and non-empty.
///
- (void)debugWithBody:(NSString * _Nonnull)body;
/// Capture a <code>.debug</code> record with structured attributes.
/// \param body The log message. Required and non-empty.
///
/// \param attributes Per-record attributes. Values must be JSON-serializable.
///
- (void)debugWithBody:(NSString * _Nonnull)body attributes:(NSDictionary<NSString *, id> * _Nullable)attributes;
/// Capture an <code>.info</code> record. Default level for regular runtime events.
/// \param body The log message. Required and non-empty.
///
- (void)infoWithBody:(NSString * _Nonnull)body;
/// Capture an <code>.info</code> record with structured attributes.
/// \param body The log message. Required and non-empty.
///
/// \param attributes Per-record attributes. Values must be JSON-serializable.
///
- (void)infoWithBody:(NSString * _Nonnull)body attributes:(NSDictionary<NSString *, id> * _Nullable)attributes;
/// Capture a <code>.warn</code> record. Something unexpected happened but the operation
/// continued.
/// \param body The log message. Required and non-empty.
///
- (void)warnWithBody:(NSString * _Nonnull)body;
/// Capture a <code>.warn</code> record with structured attributes.
/// \param body The log message. Required and non-empty.
///
/// \param attributes Per-record attributes. Values must be JSON-serializable.
///
- (void)warnWithBody:(NSString * _Nonnull)body attributes:(NSDictionary<NSString *, id> * _Nullable)attributes;
/// Capture an <code>.error</code> record. An operation failed; the app may continue.
/// \param body The log message. Required and non-empty.
///
- (void)errorWithBody:(NSString * _Nonnull)body;
/// Capture an <code>.error</code> record with structured attributes.
/// \param body The log message. Required and non-empty.
///
/// \param attributes Per-record attributes. Values must be JSON-serializable.
///
- (void)errorWithBody:(NSString * _Nonnull)body attributes:(NSDictionary<NSString *, id> * _Nullable)attributes;
/// Capture a <code>.fatal</code> record. An unrecoverable failure; the app likely
/// cannot continue.
/// \param body The log message. Required and non-empty.
///
- (void)fatalWithBody:(NSString * _Nonnull)body;
/// Capture a <code>.fatal</code> record with structured attributes.
/// \param body The log message. Required and non-empty.
///
/// \param attributes Per-record attributes. Values must be JSON-serializable.
///
- (void)fatalWithBody:(NSString * _Nonnull)body attributes:(NSDictionary<NSString *, id> * _Nullable)attributes;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// Configuration for the PostHog logs subsystem. Mutate fields on <code>config.logs</code>
/// before calling <code>PostHogSDK.setup(_:)</code>.
SWIFT_CLASS("_TtC7PostHog17PostHogLogsConfig")
@interface PostHogLogsConfig : NSObject
/// How often the queue checks for records to flush. Set before
/// <code>PostHogSDK.setup(_:)</code>; later mutations are ignored.
@property (nonatomic) NSTimeInterval flushIntervalSeconds;
/// Maximum number of records held on disk. When full, the oldest record is
/// dropped (FIFO).
@property (nonatomic) NSInteger maxBufferSize;
/// Threshold at which the queue triggers a flush automatically. Smaller
/// than <code>maxBatchSize</code> lets the queue fire smaller batches on a steady
/// cadence rather than waiting for the full cap.
@property (nonatomic) NSInteger flushAt;
/// Initial maximum number of records sent in a single request. May be
/// reduced under server backpressure.
@property (nonatomic) NSInteger maxBatchSize;
/// OpenTelemetry <code>service.name</code> resource attribute. Defaults to the host
/// app’s bundle identifier. Set before <code>PostHogSDK.setup(_:)</code>; later
/// mutations are ignored.
@property (nonatomic, copy) NSString * _Nonnull serviceName;
/// OpenTelemetry <code>service.version</code> resource attribute. Defaults to the
/// host app’s <code>CFBundleShortVersionString</code>, or empty if unavailable.
/// Empty values are omitted from the wire payload. Set before
/// <code>PostHogSDK.setup(_:)</code>; later mutations are ignored.
@property (nonatomic, copy) NSString * _Nonnull serviceVersion;
/// OpenTelemetry <code>deployment.environment</code> resource attribute. Omitted from
/// the payload when nil. Set before <code>PostHogSDK.setup(_:)</code>; later
/// mutations are ignored.
@property (nonatomic, copy) NSString * _Nullable environment;
/// Additional OpenTelemetry resource attributes attached to every batch.
/// SDK-managed keys (<code>telemetry.sdk.*</code>, <code>os.*</code>, <code>service.name</code>) win on key
/// collision so users can’t shadow them. Set before
/// <code>PostHogSDK.setup(_:)</code>; later mutations are ignored.
/// <em>From ObjC</em>: bridges to an immutable <code>NSDictionary</code>. Replace the
/// property to change it; in-place mutation on the returned dictionary
/// will fail.
@property (nonatomic, copy) NSDictionary<NSString *, id> * _Nonnull resourceAttributes;
/// Maximum number of records accepted per <code>rateCapWindowSeconds</code>. Set to
/// <code>0</code> (or any non-positive value) to disable the cap. Set before
/// <code>PostHogSDK.setup(_:)</code>; later mutations are ignored.
@property (nonatomic) NSInteger rateCapMaxLogs;
/// Tumbling window in seconds used by the rate cap. Must be positive;
/// non-positive values disable the cap. Set before <code>PostHogSDK.setup(_:)</code>;
/// later mutations are ignored.
@property (nonatomic) NSTimeInterval rateCapWindowSeconds;
/// Replaces the log <code>beforeSend</code> chain from Objective-C boxed callbacks.
/// \param blocks Ordered Objective-C callback boxes.
///
- (void)setBeforeSend:(NSArray<BoxedBeforeSendLogBlock *> * _Nonnull)blocks;
/// Creates a logs configuration with default queueing, resource, and rate-cap options.
- (nonnull instancetype)init OBJC_DESIGNATED_INITIALIZER;
@end

/// A model representing the next state of the survey progression.
SWIFT_CLASS("_TtC7PostHog25PostHogNextSurveyQuestion")
@interface PostHogNextSurveyQuestion : NSObject
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// Determines the behavior for processing user profiles.
/// <ul>
///   <li>
///     <code>never</code>: We won’t process persons for any event. This means that anonymous users will not be merged once
///     they sign up or login, so you lose the ability to create funnels that track users from anonymous to identified.
///     All events (including <code>$identify</code>) will be sent with <code>$process_person_profile: False</code>.
///   </li>
///   <li>
///     <code>always</code>: We will process persons data for all events.
///   </li>
///   <li>
///     <code>identifiedOnly</code>: (default): we will only process persons when you call <code>identify</code>, <code>alias</code>, and <code>group</code>, Anonymous users won’t get person profiles.
///   </li>
/// </ul>
typedef SWIFT_ENUM_NAMED(NSInteger, PostHogPersonProfiles, "PostHogPersonProfiles", closed) {
/// Never process person profiles. All events are sent with <code>$process_person_profile: false</code>.
  PostHogPersonProfilesNever = 0,
/// Always process person profiles for all events.
  PostHogPersonProfilesAlways = 1,
/// Process person profiles only after <code>identify</code>, <code>alias</code>, or <code>group</code> creates one.
  PostHogPersonProfilesIdentifiedOnly = 2,
};

/// Protocol to sanitize the event properties
SWIFT_PROTOCOL_NAMED("PostHogPropertiesSanitizer")
@protocol PostHogPropertiesSanitizer
/// Sanitizes the event properties
/// note:
/// <code>inout</code> cannot be used in Swift protocols, so clone the properties before mutating them.
/// \code
/// private class ExampleSanitizer: PostHogPropertiesSanitizer {
///     public func sanitize(_ properties: [String: Any]) -> [String: Any] {
///         var sanitizedProperties = properties
///         // Perform sanitization
///         // For example, removing keys with empty values
///         for (key, value) in properties {
///             if let stringValue = value as? String, stringValue.isEmpty {
///                 sanitizedProperties.removeValue(forKey: key)
///             }
///         }
///         return sanitizedProperties
///     }
/// }
///
/// \endcode\param properties the event properties to sanitize
///
///
/// returns:
/// the sanitized properties
- (NSDictionary<NSString *, id> * _Nonnull)sanitize:(NSDictionary<NSString *, id> * _Nonnull)properties SWIFT_WARN_UNUSED_RESULT;
@end

/// Configuration for iOS/macCatalyst rage click autocapture.
/// Rage click detection captures <code>$rageclick</code> when a user rapidly taps the same area.
SWIFT_CLASS_NAMED("PostHogRageClickConfig")
@interface PostHogRageClickConfig : NSObject
/// Enable rage click detection for iOS/macCatalyst.
/// When enabled, rapid taps in the same area will be captured as <code>$rageclick</code> events.
/// Works independently of <code>captureElementInteractions</code> — you can enable rage click
/// detection without enabling full autocapture.
/// Default: true
@property (nonatomic) BOOL enabled;
/// Manhattan distance threshold in logical points.
/// Taps within this distance from the previous tap are considered “in the same area”.
/// Uses Manhattan distance: <code>|x1 - x2| + |y1 - y2|</code>.
/// Default: 30
@property (nonatomic) CGFloat thresholdPoints;
/// Maximum time interval between consecutive taps to count as part of the same sequence.
/// If more than this interval passes between two taps, the sequence resets.
/// Default: 1.0 (seconds)
@property (nonatomic) NSTimeInterval timeoutInterval;
/// Number of consecutive taps within the threshold to qualify as a rage click.
/// Default: 3
@property (nonatomic) NSInteger minimumTapCount;
- (nonnull instancetype)init OBJC_DESIGNATED_INITIALIZER;
@end

@class NSUserActivity;
@class UIOpenURLContext;
@class NSException;
/// Main entry point for capturing analytics, feature flags, logs, surveys, and session replay.
/// Use <code>PostHogSDK.shared</code> for the default singleton instance, or <code>PostHogSDK.with(_:)</code>
/// to create an additional configured instance.
SWIFT_CLASS("_TtC7PostHog10PostHogSDK")
@interface PostHogSDK : NSObject
/// Logger facade exposing <code>trace/debug/info/warn/error/fatal(_:attributes:)</code>.
/// <code>nil</code> before <code>setup(_:)</code> is called.
@property (nonatomic, readonly, strong) PostHogLogger * _Nullable logger;
/// Shared singleton SDK instance used by most applications.
/// Call <code>setup(_:)</code> once with a <code>PostHogConfig</code> before using capture APIs.
SWIFT_CLASS_PROPERTY(@property (nonatomic, class, readonly, strong) PostHogSDK * _Nonnull shared;)
+ (PostHogSDK * _Nonnull)shared SWIFT_WARN_UNUSED_RESULT;
/// Enables or disables SDK debug logging at runtime.
/// \param enabled Pass <code>true</code> to enable verbose logs or <code>false</code> to disable them.
///
- (void)debug:(BOOL)enabled;
/// Initializes this SDK instance with the provided configuration.
/// Call this once, as early as possible in app startup. Calls made before setup completes are ignored.
/// Calling setup again on the same instance logs a warning and has no effect.
/// \param config The configuration to install.
///
- (void)setup:(PostHogConfig * _Nonnull)config;
/// Returns the current PostHog distinct ID.
/// Before <code>identify(_:)</code>, this is the anonymous ID. After identify, this is the identified user’s ID.
///
/// returns:
/// The current distinct ID, or an empty string when the SDK is not set up.
- (NSString * _Nonnull)getDistinctId SWIFT_WARN_UNUSED_RESULT;
/// Returns the anonymous ID generated for this install.
///
/// returns:
/// The anonymous ID, or an empty string when the SDK is not set up.
- (NSString * _Nonnull)getAnonymousId SWIFT_WARN_UNUSED_RESULT;
/// Returns the stable device identifier used for device-level feature flag bucketing.
/// This ID persists across <code>identify()</code> and <code>reset()</code> calls, only changing on a fresh
/// app install, manual cache clearing, or OS-initiated storage cleanup.
///
/// returns:
/// The stable device ID, or an empty string when the SDK is not set up.
- (NSString * _Nonnull)getDeviceId SWIFT_WARN_UNUSED_RESULT;
/// Returns the current session ID without rotating or creating a session.
///
/// returns:
/// The current session ID, or <code>nil</code> if no session is active or the SDK is not set up.
- (NSString * _Nullable)getSessionId SWIFT_WARN_UNUSED_RESULT;
/// Starts or resumes the current analytics session.
- (void)startSession;
/// Ends the current analytics session.
- (void)endSession;
/// Captures a deep link opened event for a URL.
/// \param url The URL that was opened.
///
- (void)captureDeepLinkWithUrl:(NSURL * _Nonnull)url;
/// Capture deep link events from an array of URLs.
/// Use this method with macOS <code>NSApplicationDelegate.application(_:open:)</code>.
/// File URLs are automatically filtered out; every other URL in the array is captured.
/// \param urls The URLs that were opened.
///
- (void)captureDeepLinkWithUrls:(NSArray<NSURL *> * _Nonnull)urls;
/// Capture a deep link from an NSUserActivity (universal links).
/// Use this method with <code>application(_:continue:restorationHandler:)</code> in your
/// app delegate, or with SwiftUI’s <code>.onContinueUserActivity()</code> modifier.
/// Only activities of type <code>NSUserActivityTypeBrowsingWeb</code> are processed.
/// \param userActivity The user activity containing the universal link.
///
- (void)captureDeepLinkWithUserActivity:(NSUserActivity * _Nonnull)userActivity;
/// Capture a deep link with UIKit open URL options.
/// Use this method with <code>application(_:open:options:)</code> in your UIApplicationDelegate.
/// The source application is extracted as the referrer.
/// \param url The URL that was opened.
///
/// \param options The options dictionary from UIKit containing source application info.
///
- (void)captureDeepLinkWithUrl:(NSURL * _Nonnull)url options:(NSDictionary<UIApplicationOpenURLOptionsKey, id> * _Nonnull)options;
/// Capture a deep link from UIKit scene-based URL contexts.
/// Use this method with <code>scene(_:openURLContexts:)</code> in your UISceneDelegate.
/// Only the first URL context is captured.
/// \param openURLContexts The set of URL contexts from the scene delegate.
///
- (void)captureDeepLinkWithOpenURLContexts:(NSSet<UIOpenURLContext *> * _Nonnull)openURLContexts SWIFT_AVAILABILITY(tvos,introduced=13.0) SWIFT_AVAILABILITY(ios,introduced=13.0);
/// Trigger an immediate flush of every queue — events, session-replay
/// snapshots, and structured logs.
- (void)flush;
/// Resets local identity, super properties, feature flag cache, and session state.
/// Call this when a user logs out. The next captured event will use a new anonymous identity
/// unless <code>PostHogConfig.reuseAnonymousId</code> is enabled.
- (void)reset;
/// Registers super properties that are included with every subsequent event.
/// New values overwrite existing values for the same keys and persist across app restarts
/// until removed with <code>unregister(_:)</code> or cleared by <code>reset()</code>.
/// \param properties Event properties to persist and attach to future captures.
///
- (void)registerProperties:(NSDictionary<NSString *, id> * _Nonnull)properties;
/// Removes a previously registered super property.
/// \param key The property key to stop attaching to future events.
///
- (void)unregisterProperties:(NSString * _Nonnull)key;
/// Identifies the current user with a stable distinct ID.
/// By default, the first successful identify call links prior anonymous events to the provided ID.
/// If <code>PostHogConfig.reuseAnonymousId</code> is <code>true</code>, prior anonymous events are not linked.
/// Empty IDs, opted-out users, and configurations with <code>personProfiles == .never</code> are ignored.
/// \param distinctId Stable user identifier from your application.
///
- (void)identify:(NSString * _Nonnull)distinctId;
/// Identifies the current user and sets person properties.
/// \param distinctId Stable user identifier from your application.
///
/// \param userProperties Properties to set on the person profile. Existing values are overwritten.
///
- (void)identifyWithDistinctId:(NSString * _Nonnull)distinctId userProperties:(NSDictionary<NSString *, id> * _Nullable)userProperties;
/// Identifies the current user and sets person properties.
/// \param distinctId Stable user identifier from your application.
///
/// \param userProperties Properties to set on the person profile. Existing values are overwritten.
///
/// \param userPropertiesSetOnce Properties to set only if they do not already exist.
///
- (void)identifyWithDistinctId:(NSString * _Nonnull)distinctId userProperties:(NSDictionary<NSString *, id> * _Nullable)userProperties userPropertiesSetOnce:(NSDictionary<NSString *, id> * _Nullable)userPropertiesSetOnce;
/// Sets properties on the person profile associated with the current distinct_id.
/// Updates user properties that are stored with the person profile in PostHog.
/// If <code>personProfiles</code> is set to <code>never</code>, this call will be ignored.
/// This method sends a <code>$set</code> event to PostHog.
/// \param userPropertiesToSet Properties to store about the user. These will overwrite existing values.
///
- (void)setPersonPropertiesWithUserPropertiesToSet:(NSDictionary<NSString *, id> * _Nullable)userPropertiesToSet;
/// Sets properties on the person profile associated with the current distinct_id.
/// Updates user properties that are stored with the person profile in PostHog.
/// If <code>personProfiles</code> is set to <code>never</code>, this call will be ignored.
/// This method sends a <code>$set</code> event to PostHog.
/// \param userPropertiesToSet Properties to store about the user. These will overwrite existing values.
///
/// \param userPropertiesToSetOnce Properties to store about the user only if not previously set.
/// Note: For feature flag evaluations, if the same key is present in both parameters,
/// the value from userPropertiesToSet will take precedence.
///
- (void)setPersonPropertiesWithUserPropertiesToSet:(NSDictionary<NSString *, id> * _Nullable)userPropertiesToSet userPropertiesToSetOnce:(NSDictionary<NSString *, id> * _Nullable)userPropertiesToSetOnce;
/// Captures a custom event for the current user.
/// \param event Event name to capture.
///
- (void)capture:(NSString * _Nonnull)event;
/// Captures a custom event with event properties.
/// \param event Event name to capture.
///
/// \param properties Event properties attached only to this event.
///
- (void)captureWithEvent:(NSString * _Nonnull)event properties:(NSDictionary<NSString *, id> * _Nullable)properties;
/// Captures a custom event and updates person properties.
/// \param event Event name to capture.
///
/// \param properties Event properties attached only to this event.
///
/// \param userProperties Person properties to set. Existing values are overwritten.
///
- (void)captureWithEvent:(NSString * _Nonnull)event properties:(NSDictionary<NSString *, id> * _Nullable)properties userProperties:(NSDictionary<NSString *, id> * _Nullable)userProperties;
/// Captures a custom event and updates person properties.
/// \param event Event name to capture.
///
/// \param properties Event properties attached only to this event.
///
/// \param userProperties Person properties to set. Existing values are overwritten.
///
/// \param userPropertiesSetOnce Person properties to set only if they do not already exist.
///
- (void)captureWithEvent:(NSString * _Nonnull)event properties:(NSDictionary<NSString *, id> * _Nullable)properties userProperties:(NSDictionary<NSString *, id> * _Nullable)userProperties userPropertiesSetOnce:(NSDictionary<NSString *, id> * _Nullable)userPropertiesSetOnce;
/// Captures a custom event with group context.
/// \param event Event name to capture.
///
/// \param properties Event properties attached only to this event.
///
/// \param userProperties Person properties to set. Existing values are overwritten.
///
/// \param userPropertiesSetOnce Person properties to set only if they do not already exist.
///
/// \param groups Group type/key pairs to attach to this event.
///
- (void)captureWithEvent:(NSString * _Nonnull)event properties:(NSDictionary<NSString *, id> * _Nullable)properties userProperties:(NSDictionary<NSString *, id> * _Nullable)userProperties userPropertiesSetOnce:(NSDictionary<NSString *, id> * _Nullable)userPropertiesSetOnce groups:(NSDictionary<NSString *, NSString *> * _Nullable)groups;
/// Captures a custom event for an explicit distinct ID.
/// \param event Event name to capture.
///
/// \param distinctId Optional distinct ID override. Defaults to the current SDK distinct ID.
///
/// \param properties Event properties attached only to this event.
///
/// \param userProperties Person properties to set. Existing values are overwritten.
///
/// \param userPropertiesSetOnce Person properties to set only if they do not already exist.
///
/// \param groups Group type/key pairs to attach to this event.
///
- (void)captureWithEvent:(NSString * _Nonnull)event distinctId:(NSString * _Nullable)distinctId properties:(NSDictionary<NSString *, id> * _Nullable)properties userProperties:(NSDictionary<NSString *, id> * _Nullable)userProperties userPropertiesSetOnce:(NSDictionary<NSString *, id> * _Nullable)userPropertiesSetOnce groups:(NSDictionary<NSString *, NSString *> * _Nullable)groups;
/// Captures a custom event for an explicit distinct ID and timestamp.
/// \param event Event name to capture.
///
/// \param distinctId Optional distinct ID override. Defaults to the current SDK distinct ID.
///
/// \param properties Event properties attached only to this event.
///
/// \param userProperties Person properties to set. Existing values are overwritten.
///
/// \param userPropertiesSetOnce Person properties to set only if they do not already exist.
///
/// \param groups Group type/key pairs to attach to this event.
///
/// \param timestamp Optional event timestamp. Defaults to the current time.
///
- (void)captureWithEvent:(NSString * _Nonnull)event distinctId:(NSString * _Nullable)distinctId properties:(NSDictionary<NSString *, id> * _Nullable)properties userProperties:(NSDictionary<NSString *, id> * _Nullable)userProperties userPropertiesSetOnce:(NSDictionary<NSString *, id> * _Nullable)userPropertiesSetOnce groups:(NSDictionary<NSString *, NSString *> * _Nullable)groups timestamp:(NSDate * _Nullable)timestamp;
/// Capture a structured log record at <code>.info</code> severity.
/// Log records ship to PostHog via a separate <code>/i/v1/logs</code> endpoint and
/// are persisted to disk so they survive app restarts. The call performs
/// a <em>synchronous disk write on the calling thread</em>, then the network
/// send happens asynchronously — same contract as <code>capture(_:)</code> for
/// events. Avoid calling from the main thread on hot paths. Records run
/// through <code>config.logs.beforeSend</code> and the per-window rate cap before
/// enqueueing — both can drop a record silently. An empty or
/// whitespace-only <code>body</code> is always dropped.
/// \param body The log message. Required and non-empty.
///
- (void)captureLogWithBody:(NSString * _Nonnull)body;
/// Capture a structured log record at the given severity.
/// \param body The log message. Required and non-empty.
///
/// \param level Severity. Use <code>.trace</code>/<code>.debug</code> for diagnostic detail,
/// <code>.info</code> for regular events, <code>.warn</code>/<code>.error</code>/<code>.fatal</code> for problems.
///
- (void)captureLogWithBody:(NSString * _Nonnull)body level:(enum PostHogLogSeverity)level;
/// Capture a structured log record with caller-supplied attributes.
/// \param body The log message. Required and non-empty.
///
/// \param level Severity.
///
/// \param attributes Per-record attributes (request id, duration, etc.).
/// Values must be JSON-serializable; non-serializable entries are
/// dropped.
///
- (void)captureLogWithBody:(NSString * _Nonnull)body level:(enum PostHogLogSeverity)level attributes:(NSDictionary<NSString *, id> * _Nullable)attributes;
/// Capture a structured log record with W3C trace context. Intended for
/// callers correlating logs with distributed traces; most apps should use
/// the simpler overload.
/// \param body The log message. Required and non-empty.
///
/// \param level Severity.
///
/// \param attributes Per-record attributes. Same rules as the simpler overload.
///
/// \param traceId 32-character lowercase hex W3C trace id.
///
/// \param spanId 16-character lowercase hex W3C span id.
///
/// \param traceFlags W3C trace flags bitfield (bit 0 is the <code>sampled</code> flag).
/// <code>nil</code> omits the field on the wire; <code>0</code> emits an explicit zero.
///
- (void)captureLogWithBody:(NSString * _Nonnull)body level:(enum PostHogLogSeverity)level attributes:(NSDictionary<NSString *, id> * _Nullable)attributes traceId:(NSString * _Nullable)traceId spanId:(NSString * _Nullable)spanId traceFlags:(NSNumber * _Nullable)traceFlags;
/// Records a screen view by capturing a <code>$screen</code> event.
/// \param screenTitle The screen name to record.
///
- (void)screen:(NSString * _Nonnull)screenTitle;
/// Records a screen view by capturing a <code>$screen</code> event with <code>screenTitle</code>.
/// The title is also cached and automatically attached as <code>$screen_name</code> to
/// every subsequent event (until <code>reset()</code> or <code>close()</code> clears it).
/// To override the auto-attached value on a specific event, pass <code>$screen_name</code>
/// in that event’s <code>properties</code> dictionary.
/// \param screenTitle The screen name to record.
///
/// \param properties Additional properties to attach to this <code>$screen</code> event.
///
- (void)screenWithTitle:(NSString * _Nonnull)screenTitle properties:(NSDictionary<NSString *, id> * _Nullable)properties;
/// Assigns an additional distinct ID to the current user.
/// Use alias when a user should be connected to another identifier that was previously used
/// to track them. This sends a <code>$create_alias</code> event.
/// \param alias The additional distinct ID to associate with the current user.
///
- (void)alias:(NSString * _Nonnull)alias;
/// Associates subsequent events with a group.
/// \param type Group type, such as <code>"company"</code> or <code>"organization"</code>.
///
/// \param key Group key from your application.
///
- (void)groupWithType:(NSString * _Nonnull)type key:(NSString * _Nonnull)key;
/// Associates subsequent events with a group and optionally updates group properties.
/// This sends a <code>$groupidentify</code> event and reloads feature flags if the group value changed.
/// \param type Group type, such as <code>"company"</code> or <code>"organization"</code>.
///
/// \param key Group key from your application.
///
/// \param groupProperties Optional properties to set on the group profile.
///
- (void)groupWithType:(NSString * _Nonnull)type key:(NSString * _Nonnull)key groupProperties:(NSDictionary<NSString *, id> * _Nullable)groupProperties;
/// Sets person properties that will be included in feature flag evaluation requests.
/// This method allows you to override server-side person properties for immediate feature flag evaluation,
/// solving the race condition where person properties from <code>identify()</code> calls may not have been processed
/// by the server yet.
/// Properties are merged additively with existing properties. Feature flags are automatically reloaded
/// after setting properties.
/// <h2>Example Usage</h2>
/// \code
/// // Set properties and automatically reload flags
/// PostHogSDK.shared.setPersonPropertiesForFlags([
///     "$app_version": "2.93.0",
///     "plan": "premium"
/// ])
///
/// // Now feature flags will be evaluated with these properties
/// let flagValue = PostHogSDK.shared.isFeatureEnabled("new_feature")
///
/// \endcodeseealso:
/// <code>setPersonPropertiesForFlags(_:reloadFeatureFlags:)</code> to control flag reloading behavior
/// \param properties Dictionary of person properties to include in flag evaluation
///
- (void)setPersonPropertiesForFlags:(NSDictionary<NSString *, id> * _Nonnull)properties;
/// Sets person properties that will be included in feature flag evaluation requests.
/// This method allows you to override server-side person properties for immediate feature flag evaluation,
/// solving the race condition where person properties from <code>identify()</code> calls may not have been processed
/// by the server yet.
/// Properties are merged additively with existing properties.
/// <h2>Example Usage</h2>
/// \code
/// // Set properties without automatically reloading flags
/// PostHogSDK.shared.setPersonPropertiesForFlags([
///     "$app_version": "2.93.0",
///     "plan": "premium"
/// ], reloadFeatureFlags: false)
///
/// // Manually reload flags later
/// PostHogSDK.shared.reloadFeatureFlags()
///
/// \endcode\param properties Dictionary of person properties to include in flag evaluation
///
/// \param reloadFeatureFlags Whether to automatically reload feature flags after setting properties
///
- (void)setPersonPropertiesForFlagsWithProperties:(NSDictionary<NSString *, id> * _Nonnull)properties reloadFeatureFlags:(BOOL)reloadFeatureFlags;
/// Resets all person properties that were set for feature flag evaluation.
/// After calling this method, feature flag evaluation will only use server-side person properties
/// and will not include any locally overridden properties.
/// <h2>Example Usage</h2>
/// \code
/// // Clear all locally set person properties for flags
/// PostHogSDK.shared.resetPersonPropertiesForFlags()
///
/// // Feature flags will now use only server-side properties
/// let flagValue = PostHogSDK.shared.isFeatureEnabled("feature")
///
/// \endcode
- (void)resetPersonPropertiesForFlags;
/// Resets all person properties that were set for feature flag evaluation.
/// After calling this method, feature flag evaluation will only use server-side person properties
/// and will not include any locally overridden properties.
/// <h2>Example Usage</h2>
/// \code
/// // Clear all locally set person properties for flags
/// PostHogSDK.shared.resetPersonPropertiesForFlags(reloadFeatureFlags: true)
///
/// // Feature flags will now use only server-side properties
/// let flagValue = PostHogSDK.shared.isFeatureEnabled("feature")
///
/// \endcode\param reloadFeatureFlags Whether to automatically reload feature flags after resetting properties
///
- (void)resetPersonPropertiesForFlagsWithReloadFeatureFlags:(BOOL)reloadFeatureFlags;
/// Sets properties for a specific group type to include when evaluating feature flags.
/// These properties supplement the standard group information sent to PostHog for flag evaluation,
/// providing additional context that can be used in flag targeting conditions.
/// <h2>Example Usage</h2>
/// \code
/// PostHogSDK.shared.setGroupPropertiesForFlags("organization", properties: [
///     "plan": "enterprise",
///     "seats": 50,
///     "industry": "technology"
/// ])
///
/// \endcodenote:
/// This method automatically reloads feature flags to apply the new properties.
/// seealso:
/// <code>setGroupPropertiesForFlags(_:properties:reloadFeatureFlags:)</code> to control flag reloading behavior
/// \param groupType The group type identifier (e.g., “organization”, “team”)
///
/// \param properties Dictionary of properties to set for this group type
///
- (void)setGroupPropertiesForFlags:(NSString * _Nonnull)groupType properties:(NSDictionary<NSString *, id> * _Nonnull)properties;
/// Sets properties for a specific group type to include when evaluating feature flags.
/// These properties supplement the standard group information sent to PostHog for flag evaluation,
/// providing additional context that can be used in flag targeting conditions.
/// <h2>Example Usage</h2>
/// \code
/// // Set properties without automatically reloading flags
/// PostHogSDK.shared.setGroupPropertiesForFlags("organization", properties: [
///     "plan": "enterprise",
///     "seats": 50
/// ], reloadFeatureFlags: false)
///
/// // Manually reload flags later
/// PostHogSDK.shared.reloadFeatureFlags()
///
/// \endcode\param groupType The group type identifier (e.g., “organization”, “team”)
///
/// \param properties Dictionary of properties to set for this group type
///
/// \param reloadFeatureFlags Whether to automatically reload feature flags after setting properties
///
- (void)setGroupPropertiesForFlags:(NSString * _Nonnull)groupType properties:(NSDictionary<NSString *, id> * _Nonnull)properties reloadFeatureFlags:(BOOL)reloadFeatureFlags;
/// Clears all group properties for feature flag evaluation.
/// <h2>Example Usage</h2>
/// \code
/// // Clear all group properties
/// PostHogSDK.shared.resetGroupPropertiesForFlags()
///
/// \endcode
- (void)resetGroupPropertiesForFlags;
/// Clears all group properties for feature flag evaluation.
/// <h2>Example Usage</h2>
/// \code
/// // Clear all group properties
/// PostHogSDK.shared.resetGroupPropertiesForFlags(reloadFeatureFlags: true)
///
/// \endcode\param reloadFeatureFlags Whether to automatically reload feature flags after clearing all group properties.
///
- (void)resetGroupPropertiesForFlagsWithReloadFeatureFlags:(BOOL)reloadFeatureFlags;
/// Clears group properties for feature flag evaluation for a specific group type.
/// <h2>Example Usage</h2>
/// \code
/// // Clear properties for specific group type
/// PostHogSDK.shared.resetGroupPropertiesForFlags("organization")
///
/// \endcode\param groupType The group type to clear properties for
///
- (void)resetGroupPropertiesForFlagsWithGroupType:(NSString * _Nonnull)groupType;
/// Clears group properties for feature flag evaluation for a specific group type.
/// <h2>Example Usage</h2>
/// \code
/// // Clear properties for specific group type
/// PostHogSDK.shared.resetGroupPropertiesForFlags("organization", reloadFeatureFlags: true)
///
/// \endcode\param groupType The group type to clear properties for
///
/// \param reloadFeatureFlags Whether to automatically reload feature flags after clearing properties for this group type.
///
- (void)resetGroupPropertiesForFlagsWithGroupType:(NSString * _Nonnull)groupType reloadFeatureFlags:(BOOL)reloadFeatureFlags;
/// Reloads feature flags for the current user and group context.
- (void)reloadFeatureFlags;
/// Reloads feature flags and invokes a callback when finished.
/// \param callback Invoked when the reload finishes, or immediately if the reload
/// is skipped (SDK disabled/opted-out, or no remote config available).
///
- (void)reloadFeatureFlagsWithCallback:(void (^ _Nonnull)(void))callback;
/// Captures a <code>$feature_view</code> event for the specified feature flag.
/// \param flag The key of the feature flag being viewed.
///
/// \param flagVariant The variant of the feature flag being viewed. If <code>nil</code>, the SDK
/// looks up the current flag value and skips capture when no value is available.
///
- (void)captureFeatureViewWithFlag:(NSString * _Nonnull)flag flagVariant:(NSString * _Nullable)flagVariant;
/// Captures a <code>$feature_interaction</code> event for the specified feature flag.
/// \param flag The key of the feature flag being interacted with.
///
/// \param flagVariant The variant of the feature flag being interacted with. If <code>nil</code>, the SDK
/// looks up the current flag value and skips capture when no value is available.
///
- (void)captureFeatureInteractionWithFlag:(NSString * _Nonnull)flag flagVariant:(NSString * _Nullable)flagVariant;
/// Returns the feature flag result containing the flag value, variant, and payload.
/// This is the recommended method for retrieving feature flags as it provides
/// all flag information in a single call and properly tracks flag usage.
/// \param key The feature flag key
///
///
/// returns:
/// A <code>PostHogFeatureFlagResult</code> containing the flag’s enabled state,
/// variant (if any), and payload (if any), or <code>nil</code> if the flag doesn’t exist.
- (PostHogFeatureFlagResult * _Nullable)getFeatureFlagResult:(NSString * _Nonnull)key SWIFT_WARN_UNUSED_RESULT;
/// Returns the feature flag result and optionally captures a usage event.
/// \param key The feature flag key.
///
/// \param sendFeatureFlagEvent Whether to capture <code>$feature_flag_called</code> for this lookup.
///
///
/// returns:
/// A result containing enabled state, variant, and payload, or <code>nil</code> if unavailable.
- (PostHogFeatureFlagResult * _Nullable)getFeatureFlagResultWithKey:(NSString * _Nonnull)key sendFeatureFlagEvent:(BOOL)sendFeatureFlagEvent SWIFT_WARN_UNUSED_RESULT;
/// Returns a feature flag value.
/// Boolean flags return <code>Bool</code>. Multivariate flags return their variant <code>String</code>.
/// \param key The feature flag key.
///
///
/// returns:
/// <code>Bool</code>, <code>String</code>, or <code>nil</code> if the flag is unavailable.
- (id _Nullable)getFeatureFlag:(NSString * _Nonnull)key SWIFT_WARN_UNUSED_RESULT;
/// Returns a feature flag value and optionally captures a usage event.
/// \param key The feature flag key.
///
/// \param sendFeatureFlagEvent Whether to capture <code>$feature_flag_called</code> for this lookup.
///
///
/// returns:
/// <code>Bool</code>, <code>String</code>, or <code>nil</code> if the flag is unavailable.
- (id _Nullable)getFeatureFlagWithKey:(NSString * _Nonnull)key sendFeatureFlagEvent:(BOOL)sendFeatureFlagEvent SWIFT_WARN_UNUSED_RESULT;
/// Returns whether a feature flag is enabled.
/// Multivariate flags are considered enabled when a variant string is returned.
/// \param key The feature flag key.
///
///
/// returns:
/// <code>true</code> when the flag is enabled, otherwise <code>false</code>.
- (BOOL)isFeatureEnabled:(NSString * _Nonnull)key SWIFT_WARN_UNUSED_RESULT;
/// Returns whether a feature flag is enabled and optionally captures a usage event.
/// \param key The feature flag key.
///
/// \param sendFeatureFlagEvent Whether to capture <code>$feature_flag_called</code> for this lookup.
///
///
/// returns:
/// <code>true</code> when the flag is enabled, otherwise <code>false</code>.
- (BOOL)isFeatureEnabledWithKey:(NSString * _Nonnull)key sendFeatureFlagEvent:(BOOL)sendFeatureFlagEvent SWIFT_WARN_UNUSED_RESULT;
/// Returns the payload for a feature flag.
/// warning:
/// This method does not send the <code>$feature_flag_called</code> event.
/// Use <code>getFeatureFlagResult(_:)</code> instead for proper analytics tracking.
/// \param key The feature flag key.
///
///
/// returns:
/// The flag payload, or <code>nil</code> if the flag or payload is unavailable.
- (id _Nullable)getFeatureFlagPayload:(NSString * _Nonnull)key SWIFT_WARN_UNUSED_RESULT SWIFT_DEPRECATED_MSG("Use getFeatureFlagResult(_:) instead which properly tracks feature flag usage");
/// Opts the current user back into data capture.
/// This persists the opt-in state and installs integrations that were disabled while opted out.
- (void)optIn;
/// Opts the current user out of data capture.
/// This persists the opt-out state, stops integrations, and causes future capture calls to be ignored.
- (void)optOut;
/// Returns whether this SDK instance is currently opted out.
///
/// returns:
/// <code>true</code> when opted out or when the SDK is not set up.
- (BOOL)isOptOut SWIFT_WARN_UNUSED_RESULT;
/// Shuts down this SDK instance and clears its in-memory state.
/// Queues are stopped, integrations are uninstalled, and this instance must be set up again before reuse.
- (void)close;
/// Starts session recording.
/// This method will have no effect if PostHog is not enabled, or if session replay is disabled in your project settings.
/// Ingestion controls are not overridden when calling this method. The recording will not start if:
/// <ul>
///   <li>
///     The session is not sampled,
///   </li>
///   <li>
///     Event triggers are configured and have not been activated for the current session.
///   </li>
/// </ul>
/// <h2>Note:</h2>
/// <ul>
///   <li>
///     Calling this method will resume the current session or create a new one if it doesn’t exist
///   </li>
/// </ul>
- (void)startSessionRecording;
/// Starts session recording.
/// This method will have no effect if PostHog is not enabled, or if session replay is disabled in your project settings.
/// Ingestion controls are not overridden when calling this method. The recording will not start if:
/// <ul>
///   <li>
///     The session is not sampled,
///   </li>
///   <li>
///     Event triggers are configured and have not been activated for the current session.
///   </li>
/// </ul>
/// \param resumeCurrent 
/// Whether to resume recording of current session (true) or start a new session (false).
///
- (void)startSessionRecordingWithResumeCurrent:(BOOL)resumeCurrent;
/// Stops the current session recording if one is in progress.
/// This method will have no effect if PostHog is not enabled
- (void)stopSessionRecording;
/// Creates and sets up an additional SDK instance.
/// \param config Configuration for the new instance.
///
///
/// returns:
/// A configured <code>PostHogSDK</code> instance.
+ (PostHogSDK * _Nonnull)with:(PostHogConfig * _Nonnull)config SWIFT_WARN_UNUSED_RESULT;
/// Returns whether session replay is currently recording.
///
/// returns:
/// <code>true</code> only when replay is active, a session ID exists, and remote config allows recording.
- (BOOL)isSessionReplayActive SWIFT_WARN_UNUSED_RESULT;
/// Returns whether UIKit element autocapture is enabled in local state.
///
/// returns:
/// <code>true</code> when the SDK is set up and <code>captureElementInteractions</code> is enabled.
/// This does not verify that the swizzling-backed integration was installed.
- (BOOL)isAutocaptureActive SWIFT_WARN_UNUSED_RESULT;
/// Returns whether rage click autocapture is enabled in local state.
///
/// returns:
/// <code>true</code> when the SDK is set up and rage click detection is enabled.
/// This does not verify that the swizzling-backed integration was installed.
- (BOOL)isRageClickActive SWIFT_WARN_UNUSED_RESULT;
/// Capture a Swift Error or NSError
/// Captures an error as a <code>$exception</code> event with full stack trace and error chain information.
/// The error will be marked as handled by default.
/// Example:
/// \code
/// do {
///     try FileManager.default.removeItem(at: badFileUrl)
/// } catch {
///     PostHogSDK.shared.captureException(error)
/// }
///
/// \endcode\param error The error to capture (can be any Error or NSError)
///
/// \param properties Optional additional properties to attach to the event
///
- (void)captureExceptionWithError:(NSError * _Nonnull)error properties:(NSDictionary<NSString *, id> * _Nullable)properties;
/// Capture a Swift Error or NSError without additional properties
/// Convenience overload for Objective-C callers so <code>properties:</code> doesn’t need to be passed as <code>nil</code>.
/// \param error The error to capture (can be any Error or NSError)
///
- (void)captureExceptionWithError:(NSError * _Nonnull)error;
/// Capture an NSException
/// Captures an NSException as a <code>$exception</code> event with full stack trace.
/// This is useful for Objective-C code that uses NSException.
/// Example:
/// \code
/// @try {
///     [self riskyOperation];
/// } @catch (NSException *exception) {
///     [[PostHogSDK shared] captureExceptionWithNSException:exception properties:nil];
/// }
///
/// \endcode\param exception The NSException to capture
///
/// \param properties Optional additional properties to attach to the event
///
- (void)captureExceptionWithNSException:(NSException * _Nonnull)exception properties:(NSDictionary<NSString *, id> * _Nullable)properties;
/// Capture an NSException without additional properties
/// Convenience overload for Objective-C callers so <code>properties:</code> doesn’t need to be passed as <code>nil</code>.
/// \param exception The NSException to capture
///
- (void)captureExceptionWithNSException:(NSException * _Nonnull)exception;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

@interface PostHogSDK (SWIFT_EXTENSION(PostHog))
/// Posted on the main queue after <code>setup(_:)</code> finishes successfully.
/// The notification object is <code>nil</code>.
SWIFT_CLASS_PROPERTY(@property (nonatomic, class, readonly) NSNotificationName _Nonnull didStartNotification;)
+ (NSNotificationName _Nonnull)didStartNotification SWIFT_WARN_UNUSED_RESULT;
/// Posted after a feature flag reload finishes and cached flags are updated.
/// The notification object is <code>nil</code>.
SWIFT_CLASS_PROPERTY(@property (nonatomic, class, readonly) NSNotificationName _Nonnull didReceiveFeatureFlags;)
+ (NSNotificationName _Nonnull)didReceiveFeatureFlags SWIFT_WARN_UNUSED_RESULT;
@end

/// Manages the active PostHog session ID and session rotation state.
/// warning:
/// This class is public for backwards compatibility, but is intended for
/// SDK-internal use only. Application code should use <code>PostHogSDK.getSessionId()</code>,
/// <code>startSession()</code>, and <code>endSession()</code> instead of interacting with this manager directly.
SWIFT_CLASS("_TtC7PostHog21PostHogSessionManager")
@interface PostHogSessionManager : NSObject
/// Session manager used by <code>PostHogSDK.shared</code>.
SWIFT_CLASS_PROPERTY(@property (nonatomic, class, readonly, strong) PostHogSessionManager * _Nonnull shared;)
+ (PostHogSessionManager * _Nonnull)shared SWIFT_WARN_UNUSED_RESULT;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
/// Overrides the current session ID.
/// Use with care: changing the session ID affects analytics session attribution and session replay.
/// \param sessionId Session ID to use for subsequent events.
///
- (void)setSessionId:(NSString * _Nonnull)sessionId;
@end

@class PostHogSessionReplayConsoleLogConfig;
/// Configuration for iOS session replay capture.
/// Mutate fields on <code>config.sessionReplayConfig</code> before calling <code>PostHogSDK.setup(_:)</code>.
SWIFT_CLASS_NAMED("PostHogSessionReplayConfig")
@interface PostHogSessionReplayConfig : NSObject
/// Enable masking of all text and text input fields
/// Default: true
@property (nonatomic) BOOL maskAllTextInputs;
/// Enable masking of all images to a placeholder
/// Default: true
@property (nonatomic) BOOL maskAllImages;
/// Enable masking of all sandboxed system views
/// These may include UIImagePickerController, PHPickerViewController and CNContactPickerViewController
/// Default: true
@property (nonatomic) BOOL maskAllSandboxedViews;
/// Deprecated placeholder for photo-library image masking.
/// This property has no effect. To manually mask user photos, see the privacy controls
/// documentation: https://posthog.com/docs/session-replay/privacy?tab=iOS
/// Default: <code>false</code>.
@property (nonatomic) BOOL maskPhotoLibraryImages SWIFT_DEPRECATED_MSG("This property has no effect and will be removed in the next major release. To learn how to manually mask user photos please see our Privacy controls documentation: https://posthog.com/docs/session-replay/privacy?tab=iOS");
/// Enable capturing network telemetry
/// Default: true
/// Note: When enabled, can be disabled remotely via project settings (requires <code>PostHogConfig.remoteConfig</code> to be enabled)
@property (nonatomic) BOOL captureNetworkTelemetry;
/// By default Session replay will capture all the views on the screen as a wireframe,
/// By enabling this option, PostHog will capture the screenshot of the screen.
/// The screenshot may contain sensitive information, use with caution.
/// Default: false
@property (nonatomic) BOOL screenshotMode;
/// Schedule screenshot image capture on a background queue when <code>screenshotMode</code> is enabled.
/// Warning: Enabling this option will trigger Main Thread Checker warnings and may
/// briefly freeze the app the first time a screenshot is captured. Consider disabling
/// Main Thread Checker in your scheme’s run diagnostics when enabling this.
/// Experimental.
/// Default: false
@property (nonatomic) BOOL screenshotModeBackgroundCapture;
/// Debouncer delay used to reduce the number of snapshots captured and reduce performance impact
/// This is used for capturing the view as a wireframe or screenshot
/// The lower the number more snapshots will be captured but higher the performance impact
/// Defaults to 1s
@property (nonatomic) NSTimeInterval debouncerDelay SWIFT_DEPRECATED_MSG("Deprecated in favor of 'throttleDelay' which provides identical functionality. Will be removed in the next major release.");
/// Throttle delay used to reduce the number of snapshots captured and reduce performance impact
/// This is used for capturing the view as a wireframe or screenshot
/// The lower the number more snapshots will be captured but higher the performance impact
/// Defaults to 1s
/// Note: Previously <code>debouncerDelay</code>
@property (nonatomic) NSTimeInterval throttleDelay;
/// Enable capturing console output for session replay.
/// When enabled, logs from the following sources will be captured:
/// <ul>
///   <li>
///     Standard output (stdout)
///   </li>
///   <li>
///     Standard error (stderr)
///   </li>
///   <li>
///     OSLog messages
///   </li>
///   <li>
///     NSLog messages
///   </li>
/// </ul>
/// Each log entry will be tagged with a level (info/warning/error) based on the message content
/// and the source.
/// Defaults to <code>false</code>
/// Note: When enabled, can be disabled remotely via project settings (requires <code>PostHogConfig.remoteConfig</code> to be enabled)
@property (nonatomic) BOOL captureLogs;
/// Further configuration for capturing console output
@property (nonatomic, strong) PostHogSessionReplayConsoleLogConfig * _Nonnull captureLogsConfig;
/// Session recording sample rate, between 0.0 and 1.0.
/// 1.0 means every session will be recorded, 0.0 means no sessions will be recorded.
/// Sampling is deterministic based on the session ID, so the same session will always
/// produce the same sampling decision.
/// When set, takes precedence over the remote config sample rate.
/// When <code>nil</code>, the remote config sample rate is used (if available), otherwise all sessions are recorded.
/// Values outside the 0.0–1.0 range are ignored and treated as <code>nil</code>.
/// Defaults to <code>nil</code>
@property (nonatomic, strong) NSNumber * _Nullable sampleRate;
- (nonnull instancetype)init OBJC_DESIGNATED_INITIALIZER;
@end

/// Configuration for console log capture inside session replay recordings.
/// Used when <code>PostHogSessionReplayConfig.captureLogs</code> is enabled.
SWIFT_CLASS("_TtC7PostHog36PostHogSessionReplayConsoleLogConfig")
@interface PostHogSessionReplayConsoleLogConfig : NSObject
/// Block to process and format captured console output for session replay.
/// This block is called whenever console output is captured. It allows you to:
/// <ol>
///   <li>
///     Filter or modify log messages before they are sent to session replay
///   </li>
///   <li>
///     Determine the appropriate log level (info/warn/error) for each message
///   </li>
///   <li>
///     Format, sanitize, or skip log messages (e.g. remove sensitive data or PII)
///   </li>
/// </ol>
/// The default implementation:
/// <ul>
///   <li>
///     Detect log level (best effort)
///   </li>
///   <li>
///     Process OSLog messages to remove metadata
///   </li>
/// </ul>
/// \param output The raw console output to process.
///
///
/// returns:
/// A <code>PostHogLogEntry</code> to capture, or <code>nil</code> to skip the log output.
@property (nonatomic, copy) PostHogLogEntry * _Nullable (^ _Nonnull logSanitizer)(NSString * _Nonnull);
/// The minimum log level to capture in session replay.
/// Only log messages with this level or higher will be captured.
/// For example, if set to <code>.warn</code>:
/// <ul>
///   <li>
///     <code>.error</code> messages will be captured
///   </li>
///   <li>
///     <code>.warn</code> messages will be captured
///   </li>
///   <li>
///     <code>.info</code> messages will be skipped
///   </li>
/// </ul>
/// Defaults to <code>.error</code> to minimize noise in session replays.
@property (nonatomic) enum PostHogLogLevel minLogLevel;
- (nonnull instancetype)init OBJC_DESIGNATED_INITIALIZER;
@end

enum PostHogSurveyResponseType : NSInteger;
/// A model representing a user’s response to a survey question
SWIFT_CLASS("_TtC7PostHog21PostHogSurveyResponse")
@interface PostHogSurveyResponse : NSObject
/// The response kind: link, rating, open-ended, single-choice, or multiple-choice.
@property (nonatomic, readonly) enum PostHogSurveyResponseType type;
/// The text response (for open questions)
@property (nonatomic, readonly, copy) NSString * _Nullable textValue;
/// The selected options (for multiple or single choice questions)
@property (nonatomic, readonly, copy) NSArray<NSString *> * _Nullable selectedOptions;
/// Creates a response for a link question.
/// \param clicked Whether the link was clicked.
///
///
/// returns:
/// A response with type <code>.link</code> and <code>linkClicked</code> populated.
+ (PostHogSurveyResponse * _Nonnull)link:(BOOL)clicked SWIFT_WARN_UNUSED_RESULT;
/// Creates a response for an open-ended question.
/// \param openEnded The text response.
///
///
/// returns:
/// A response with type <code>.openEnded</code> and <code>textValue</code> populated.
+ (PostHogSurveyResponse * _Nonnull)openEnded:(NSString * _Nullable)openEnded SWIFT_WARN_UNUSED_RESULT;
/// Creates a response for a single-choice question.
/// \param singleChoice The selected option.
///
///
/// returns:
/// A response with type <code>.singleChoice</code> and <code>selectedOptions</code> containing the option when non-nil.
+ (PostHogSurveyResponse * _Nonnull)singleChoice:(NSString * _Nullable)singleChoice SWIFT_WARN_UNUSED_RESULT;
/// Creates a response for a multiple-choice question.
/// \param multipleChoice The selected options.
///
///
/// returns:
/// A response with type <code>.multipleChoice</code> and <code>selectedOptions</code> populated.
+ (PostHogSurveyResponse * _Nonnull)multipleChoice:(NSArray<NSString *> * _Nullable)multipleChoice SWIFT_WARN_UNUSED_RESULT;
- (nonnull instancetype)init SWIFT_UNAVAILABLE;
+ (nonnull instancetype)new SWIFT_UNAVAILABLE_MSG("-init is unavailable");
@end

/// Type of response submitted for a survey question.
typedef SWIFT_ENUM(NSInteger, PostHogSurveyResponseType, closed) {
/// Response for a link question.
  PostHogSurveyResponseTypeLink = 0,
/// Response for a rating question.
  PostHogSurveyResponseTypeRating = 1,
/// Response for an open-ended text question.
  PostHogSurveyResponseTypeOpenEnded = 2,
/// Response for a single-choice question.
  PostHogSurveyResponseTypeSingleChoice = 3,
/// Response for a multiple-choice question.
  PostHogSurveyResponseTypeMultipleChoice = 4,
};

/// Configuration for mobile survey rendering.
/// Mutate fields on <code>PostHogConfig.surveysConfig</code> before SDK setup to customize survey UI,
/// lifecycle handling, or localization.
SWIFT_CLASS("_TtC7PostHog20PostHogSurveysConfig")
@interface PostHogSurveysConfig : NSObject
/// Optional explicit override for the language used when rendering surveys.
/// When set, surveys with matching entries in <code>translations</code> will be rendered in
/// this language regardless of the device locale or any <code>language</code> person property.
/// Format: a language tag such as <code>"fr"</code>, <code>"pt-BR"</code>, <code>"zh-CN"</code>. Matching is
/// case-insensitive and falls back to the base language (e.g. <code>"pt"</code> if <code>"pt-BR"</code>
/// is requested but only <code>"pt"</code> is provided).
/// Blank or <code>nil</code> values are treated as unset.
/// Default: <code>nil</code>.
@property (nonatomic, copy) NSString * _Nullable overrideDisplayLanguage;
- (nonnull instancetype)init OBJC_DESIGNATED_INITIALIZER;
@end

/// Delegate used by the SDK to present surveys and receive survey lifecycle callbacks.
SWIFT_PROTOCOL("_TtP7PostHog22PostHogSurveysDelegate_")
@protocol PostHogSurveysDelegate
/// Called when an activated PostHog survey needs to be rendered on the app’s UI
/// \param survey The survey to be displayed to the user
///
/// \param onSurveyShown To be called when the survey is successfully displayed to the user.
///
/// \param onSurveyResponse Call when the user submits a response to a question; use the returned state to advance or complete the survey.
///
/// \param onSurveyClosed To be called when the survey is dismissed
///
- (void)renderSurvey:(PostHogDisplaySurvey * _Nonnull)survey onSurveyShown:(void (^ _Nonnull)(PostHogDisplaySurvey * _Nonnull))onSurveyShown onSurveyResponse:(PostHogNextSurveyQuestion * _Nullable (^ _Nonnull)(PostHogDisplaySurvey * _Nonnull, NSInteger, PostHogSurveyResponse * _Nonnull))onSurveyResponse onSurveyClosed:(void (^ _Nonnull)(PostHogDisplaySurvey * _Nonnull))onSurveyClosed;
/// Called when surveys are stopped to clean up any UI elements and reset the survey display state.
/// This method should handle the dismissal of any active surveys and cleanup of associated resources.
- (void)cleanupSurveys;
@end

#endif
#if __has_attribute(external_source_symbol)
# pragma clang attribute pop
#endif
#if defined(__cplusplus)
#endif
#pragma clang diagnostic pop
#endif
