<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ draws its own splash screen and ignores `android:windowBackground`
  (#181). Without these, the OS would put the launcher icon on the theme's
  background — a white flash before the black splash. Same black, same mark,
  both light and dark system themes.
-->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_image</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
